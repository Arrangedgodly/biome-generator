---
target: index.html
total_score: 24
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 4
timestamp: 2026-08-28T04-43-47Z
slug: index-html
---
# Design Critique — Run 2, Procedural Biome Generator (index.html surface)

Method: dual-agent (A: agent_2431f928 · B: agent_659be100). Re-run on unchanged tree after the 27/40 baseline; fresh-eyes findings added.

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | Preset aria-pressed set but styled by ZERO CSS rules — active preset invisible |
| 2 | Match System / Real World | 2 | Generate replays same map; Elevation controls freq+roughness, not altitude |
| 3 | User Control and Freedom | 3 | Skip/cancel/retry real; error disables Export/Share needlessly; focus stays put |
| 4 | Consistency and Standards | 3 | Token discipline holds; chips spec unimplemented; stale comments |
| 5 | Error Prevention | 3 | Negative seeds pass parseInt; URL restore clamps >=0 (determinism edge) |
| 6 | Recognition Rather Than Recall | 2 | No legend, no pole labels, no stage sequence |
| 7 | Flexibility and Efficiency | 2 | 2 interactions per roll; no accelerators |
| 8 | Aesthetic and Minimalist Design | 4 | Verified end-to-end — best dimension |
| 9 | Error Recovery | 3 | Full-state retry real; copy claims auto-retry that doesn't exist |
| 10 | Help and Documentation | 1 | Tagline is the entire help; reduced-motion users lose all teaching content |
| **Total** | | **24/40** | Good band, lower edge (baseline run: 27/40 same code — reviewer variance in 1,2,10) |

## Design Specificity Verdict

LLM: chrome 8/10, pedagogy 3/10, control semantics 3/10. Light Table verifiably executed in CSS (one shadow, map-derived accent, cut-out insets). Staged pedagogy narrated in three proper nouns then abandoned; Elevation and Generate mis-state their semantics. Neighbors could copy the panel in an afternoon but not the pedagogy — the part the UI under-builds.
Deterministic scan: CLEAN, 0 findings both runs (src/ 48 files + index.html) — confirms run-1 DESIGN.md corrections landed. index.html run degraded-mode (regex undercount banner).
Visual overlays: unavailable (browser backend broken; one cheap re-probe failed).

## What's Working

1. Design system real, not aspirational — every falsifiable DESIGN.md rule holds in CSS.
2. First-load choreography correctly prioritized — thesis plays unattended.
3. High-stakes moments engineered: 3-tier share fallback, full-state retry on fresh worker.

## Priority Issues

1. [P0] Preset selection state invisible (NEW): aria-pressed set, zero styling rules; DESIGN.md persistent-pressed-border never implemented; panel slider-coincidence vs app sticky-id can visibly disagree. Fix: one CSS rule + unify state ownership. ($impeccable polish)
2. [P1] No biome legend (confirmed, stronger — also the static teaching artifact reduced-motion users lose entirely). ($impeccable delight)
3. [P1] Generate/dice hierarchy inverted (confirmed; user decision recorded: Generate will re-roll). ($impeccable clarify)
4. [P1] WCAG AA fail on Generate hover (NEW): #3d8ab8 + white ~3.8:1 < 4.5:1 vs committed bar. Fix: darker hover twin or border-brighten instead of fill. ($impeccable polish)
5. [P1] Recruiter surface missing at both ends (confirmed, upgraded): first impression is the chat-app link preview — currently bare. OG/meta/favicon + footer source link. ($impeccable harden)

## Persona Red Flags

Jordan: Generate replays identical map; Elevation reshapes counter-intuitively; boot animation is the one thing done for him.
Sam: solid keyboard flow (native widgets, 3px rings, aria-live); gaps: no role="img", error focus never moves to alert, readout "0.50" vs slider "50%", hover contrast fail, reduced-motion = zero teaching content.
Alex: 2 interactions per roll, no accelerators; hackable URL hash invisible.
The Recruiter: bare chat preview; no footer/maker credit; narration below the thumb on mobile; no path from delight to source.

## Minor Observations

Stale comments (main.ts "No auto-generate on load" false; style.css "disabled placeholders"); negative-seed parseInt edge vs URL clamp; prefers-reduced-motion sampled once at boot; unconditional cursor:pointer on idle canvas; Skip 50% of Generate row while disabled ~95%; dead .dice class; DESIGN.md stage-line doc drift.

## Questions to Consider

- Why do reduced-motion users get none of the thesis while the chrome claims WCAG 2.2 AA?
- The first impression happens in the link preview before the page loads — why doesn't the app own it?
- If determinism is the promise, shouldn't preset-state truthfulness be part of it?
