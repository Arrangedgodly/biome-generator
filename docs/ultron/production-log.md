# Production Log — Procedural Biome Generator

Evidence trail for plan tasks (T1–T18). One entry per task: outcome, files, validation, deviations, delegation, approval state.

---

## T1 — Project scaffold · Frontend

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** general-purpose subagent (hand-scaffold; `create-vite` skipped to avoid non-empty-dir interactive prompts — directory contains `docs/`).
- **Outcome:** Runnable app shell: Vite dev server, strict TypeScript, git repo initialized, dark 512² canvas page with `image-rendering: pixelated`.
- **Files created:** `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`, `.gitignore`, `src/main.ts`, `src/style.css`, `src/vite-env.d.ts`, `package-lock.json`.
- **Committed decisions applied:** D4 — Vite 8 (`vite@8.2.2`), vanilla-ts, `base: '/biome-generator/'` from day one; D5 — `worker.format: 'iife'` pinned. TypeScript `5.9.3`, strict + `noUnusedLocals`/`noUnusedParameters`.
- **Validation evidence:**
  - `npm install` OK (vite 8.2.2, typescript 5.9.3 resolved).
  - `npm run build` exit 0 (tsc --noEmit zero errors; 5 modules, built 21ms — re-verified independently by production master after subagent report).
  - Dev-server smoke: `curl -L http://localhost:5173/` → HTTP 200, HTML contains "Procedural Biome Generator". Factual note: Vite 8 dev serves under `base` (302 root → `/biome-generator/`).
  - `git init -b main` OK; no commits made (per instructions); `.gitignore` effective (node_modules, dist excluded); `docs/` untouched.
- **Deviations:** None in files. Procedural only: smoke-test curl of bare `/` returned 302 (redirect to base) rather than 200; followed redirect → 200. Dev-server-under-base is Vite 8 behavior, worth remembering for T18 and manual testing.
- **Follow-ups:** None blocking. Open choice for user: initial git commit now or later (harness rule: commit only on request).
- **Next unblocked task:** T2 (noise module) and T4 (biome classifier) — both depend only on T1.
- **Approval:** ✅ approved by user 2026-08-27. No adjustments requested. T1 → completed.

---

## T2 — Noise module · Frontend

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** general-purpose subagent.
- **Outcome:** Pure, dependency-free noise math: `createSimplex2D(seed)` (Gustavson 2005 port) + `createFbm2D(opts)` factory / `fbm2D(x, y, opts)` memoized wrapper (defaults 5 octaves / lacunarity 2.0 / gain 0.5; per-octave instances seeded `seed + 1013·octave`; Σ-amplitude normalization) + `mulberry32` seeded PRNG.
- **Files created:** `src/noise/random.ts`, `src/noise/simplex.ts`, `src/noise/fbm.ts`, `src/noise/index.ts`, `src/noise/simplex.test.ts`, `src/noise/fbm.test.ts`. Modified: `package.json` (+`test` script, +`vitest@^4.1.11` devDep — production-level enabler for the test-first acceptance criteria, recorded per preflight).
- **Validation evidence:**
  - `npm test` → 2 suites, **17/17 passed** (~250ms; 512² mean test included). Independently re-run by production master.
  - `npm run build` → exit 0 (strict tsc + vite). Independently re-run.
  - Production master line-by-line review of `simplex.ts` against Gustavson's paper: F2/G2 constants, skew/unskew, corner ordering, x1/x2 offsets, `t = 0.5 − x² − y²` with t⁴ falloff, `perm[ii + perm[jj]] % 12` hashing, 70× scale — all faithful; perm-table indexing provably in-bounds (≤511).
  - Golden values: 16 snapshots at seed 0, precision 10 (full table in subagent report; e.g. `(0.1, 0.2) → −0.8593460940`). Exact `0` at `(−0.5, −0.5)` verified as genuine algorithm output, not a bug.
  - Statistics: sampled range ±0.9979, mean ≈ 1.4e-4 — matches D1 expectations (zero-mean, bounded).
  - Cross-check: structure diffed against jwagner/simplex-noise.js (faithful Gustavson port) — constants, hashing, falloff, gradients, scaling all match.
- **Deviations (minor, accepted):** 0-based octave seed index; `octaves` defensively clamped ≥1; perm table as `Uint8Array`; both factory and memoized-wrapper exported; backward Fisher–Yates (canonical). None affect D1 commitments or acceptance criteria.
- **Next unblocked task:** T3 (field pipeline — depends on T2) and T4 (biome classifier — depends on T1 only).
- **Approval:** ✅ approved by user 2026-08-27. No adjustments requested. T2 → completed.

---

## T3 — Field generation pipeline · Frontend

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** general-purpose subagent.
- **Outcome:** `generateFields(seed, params, resolution)` → `{elevation, moisture: Float32Array, resolution}` — elevation: fBm(3.5 freq, 5 oct) → centered coords → island falloff → clamp → pow-1.5 redistribution; moisture: independently seeded fBm (offset +7919, 2.5 freq, 3 oct), no falloff. `DEFAULT_FIELD_PARAMS` exported for T12 preset tuning. Two fBm factories per call, zero per-pixel allocations.
- **Files created:** `src/generation/fields.ts`, `src/generation/index.ts`, `src/generation/fields.test.ts`. No existing files modified.
- **Validation evidence:**
  - `npm test` → 3 suites, **26/26 passed** (~690ms). Independently re-run.
  - `npm run build` → exit 0. Independently re-run.
  - Perf @512²: **59.8ms** (target <300ms; D2 budget <300ms mid-hardware — huge headroom).
  - Elevation range at defaults: [0.040, 0.793]; moisture [0.124, 0.887]; both within [0,1].
  - Determinism: element-wise identical across runs; different seed diverges.
  - Moisture/elevation Pearson r = −0.105 (|r| < 0.3 bound) — fields decorrelated.
- **⚠ Deviation (production-level adjustment, outcome preserved):** `falloffStrength` default recalibrated **0.45 → 0.03**. The planning value produced 92–94% ocean across 10 seeds (all-ocean disaster — exactly what the ocean-fraction sanity test guards against); E[falloff subtraction] = 0.45·5/9 ≈ 0.25 on a mean-0.5 field, stacked with pow-1.5 downshift. Resolution hierarchy applied: D1 commits falloff blend *without* a strength; D3 expects tuning; falloff was never committed → the free parameter yielded. 0.03 chosen empirically: ocean fraction 0.60–0.70 across seeds, ≥2× seed-spread margin inside the 0.25–0.75 band. Documented as a `CALIBRATION NOTE` in `fields.ts` with measured table; single-constant revert possible.
- **Next unblocked task:** T5 (worker protocol — depends on T3) and T4 (biome classifier — depends on T1). Critical path next: T4 (renderer T7 needs it) or T5.
- **Approval:** ✅ approved by user 2026-08-27 ("approve") — deviation (falloffStrength 0.03) accepted. T3 → completed.

---

## T4 — Biome classifier · Frontend

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** general-purpose subagent.
- **Outcome:** `classify(e, m, biases)` — D3's 12-biome elevation-first table as a flat ordered rule chain (biases applied to inputs before banding, no clamping); `BIOMES` metadata (name + committed hex + precomputed WCAG luminance); tunable constants `ELEVATION_BANDS` / `MOISTURE_BANDS` / `MOUNTAIN_MOISTURE_SPLIT`; `NO_BIASES` default.
- **Files:** created `src/generation/biomes.ts`, `src/generation/biomes.test.ts`; edited `src/generation/index.ts` (append re-exports). Nothing else touched.
- **Validation evidence:**
  - `npm test` → 4 suites, **57/57 passed** (31 new). Independently re-run.
  - `npm run build` → exit 0. Independently re-run.
  - Production master branch-by-branch review of `classify` against D3's committed matrix — all 12 rules faithful (deepOcean/ocean/beach/snow moisture-agnostic; lowland & highland 4-column moisture splits; mountain 0.5 split; bands lower-inclusive).
  - Boundary matrix: 25 boundary tests with expected ids transcribed from the research table (not derived from the implementation); reachability: 0.01-step grid produces exactly the 12 committed ids; bias-shift and extreme-input fuzz pass.
  - Luminance constants verified by WCAG 2.2 recomputation from hex (max deviation 0.000082) — palette typos guarded at the data layer (full ΔL-adjacency suite remains T9's).
- **Deviations:** none.
- **Next unblocked task:** T5 (worker protocol) on the critical path; T7 (renderer) now also unblocked (needs T4).
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — independent branch review vs D3 table, 57/57 tests re-run, probe values matched, test-quality check passed.

---

## T5 — Worker + message protocol · Frontend

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** general-purpose subagent.
- **Outcome:** Map-worker layer per D5: `WorkerRequest`/`WorkerResponse` wire types (`generate` / `recompute-elevation` in, `fields` / `elevation` / `error` out with transferable `Float32Array`s); pure `handleWorkerMessage(msg, post)` (validation → typed error responses, never throws out; dispatch wrapped in try/catch; posts fresh buffers with transfer lists `[elevation.buffer, moisture.buffer]` / `[elevation.buffer]` and never touches them after posting); thin iife-safe `map.worker.ts` entry; `MapWorkerClient` with injected `workerFactory`, independent generate/elevation channels, monotonic requestIds, latest-wins superseding (old pending rejects `SupersededError` immediately; stale responses dropped silently), per-response detach guard (`byteLength === resolution²×4` else `DetachedBufferError`), `worker.onerror`/`onmessageerror`/`terminate()` ⇒ all pending reject `WorkerCrashError`. New pure `generateElevation(seed, params, resolution)` added to `fields.ts`; `generateFields` now delegates to it, so elevation-only recompute and full generation are identical by construction (moisture untouched by recompute).
- **Files:** created `src/worker/protocol.ts`, `src/worker/handler.ts`, `src/worker/map.worker.ts`, `src/worker/client.ts`, `src/worker/index.ts`, `src/worker/handler.test.ts`, `src/worker/client.test.ts`; edited `src/generation/fields.ts` (+`generateElevation`, `generateFields` refactor), `src/generation/index.ts` (+re-export), `src/main.ts` (one build-graph-edge import, see deviations).
- **Validation evidence:**
  - `npm test` → 6 suites, **72/72 passed** (15 new: 8 handler + 7 client; determinism checks compare worker-path output element-wise against direct `generateFields`/`generateElevation` calls at 128/96; 512² smoke: 262144-element buffers).
  - `npm run build` → exit 0 (strict `tsc --noEmit` + vite), re-run after clean `dist/`.
  - Worker chunk emitted as separate asset: `dist/assets/map.worker-C2FsIWiX.js` (3.50 kB), deterministic across rebuilds. First line: `(function(){function e(e){let t=e>>>0;return()=>{` — pure IIFE; grep finds **zero** ESM import/export statements (`worker.format: 'iife'` respected, D5). Emitted wiring: `onmessage=e=>{g(e.data,(e,t)=>{_.postMessage(e,t)})}})()`.
  - Client tests prove: happy paths resolve; supersede rejects `SupersededError` and drops the stale response silently; late responses to settled requests are no-ops; error responses reject with the worker's message; `byteLength`-0 buffer rejects `DetachedBufferError`; worker error event and `terminate()` reject all pending with `WorkerCrashError`; recompute supersede leaves a concurrent generate pending (channel independence).
  - Perf re-check after the `generateFields` refactor: 512² wall time 54.5 ms (T3 baseline 59.8 ms) — two-loop split cost nothing.
- **Deviations (all minor):**
  1. `src/main.ts` received one side-effect import (`import './worker/index.ts'`) — outside the "only ADD files" letter, but required by the mandated build validation: with the instantiation site deferred to T6/T10, nothing reachable from the entry graph referenced the worker, so `vite build` would emit no worker chunk. The import adds no runtime behavior, no instantiation, no DOM wiring (T6/T10 own that); the emitted URL export is `mapWorkerUrl` via `?worker&url` in `src/worker/index.ts`.
  2. `protocol.ts` imports only `FieldParams` — the sketched `Fields` import would be unused (fails `noUnusedLocals`); response shapes are unchanged.
  3. Third typed error `DetachedBufferError` added alongside `SupersededError`/`WorkerCrashError` (the detach guard requires a typed rejection distinct from crash/supersede).
  4. Dispatch-throw test triggers via `resolution: 2²⁷` (pixel count 2⁵⁴ > 2⁵³−1 typed-array cap ⇒ immediate RangeError inside the pipeline) rather than a synthetic throw; `params: null` (spec's case) is caught by validation and also yields an error response — both paths covered. (An earlier draft used resolution 300000; on Node the 9e10-element allocation overcommits instead of throwing and the test hangs — replaced.)
  5. 15 new tests vs "~11" estimated — richer validation matrix, same scope.
- **Next unblocked task:** T7 (renderer) — T6 (controller) needs T5 + T7 and stays blocked until T7 lands; T7 is the critical-path next step.
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — 72/72 tests re-run independently; clean-dist rebuild emitted the same iife chunk `map.worker-C2FsIWiX.js` with zero ESM import/export tokens (classic `self.onmessage` wiring); independent probe (generate, seed 7, res 64) matched `generateFields` element-wise (4096/4096) with a 2-buffer transfer list; D5 invariants (fresh per-request allocation, no-touch-after-post, `byteLength === res²×4` detach guard) and latest-wins (SupersededError + silent stale drop, both halves tested) traced in code; post-refactor fields measurements reproduce T3's records exactly ([0.0402, 0.7927] / [0.1237, 0.8873] / r = −0.1047); `main.ts` side-effect import assessed outcome-preserving (build-graph edge only — no runtime behavior, no instantiation, violates no committed decision).

---

## T7 — Canvas renderer · Frontend

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** worker subagent (T7 + D2 architecture as specified in the task brief).
- **Outcome:** Render layer split per D2 into a pure, node-testable composition module and a thin DOM blitter. `src/render/compose.ts`: `packRGBA` (one-time endian probe — Uint32 1 read as byte — packs r,g,b,a=255 into the platform's ImageData word order, unsigned); `buildBiomeLut(biases, steps=64)` — 64×64 quantized elevation×moisture → packed biome color, each cell = `classify(binCenter, binCenter, biases)`, hex parsed once per biome via cache (rebuild + recompose IS the moisture slider's instant path); `buildMoistureRamp()` — 256-entry linear ramp #d8c790 → #20658c; `composeChunk(mode, fields, out, fromRow, toRow, lut?)` — row-clamped chunk composer, 'elevation' = round(e·255) in r=g=b, 'moisture' = ramp[round(m·255)], 'biomes' = LUT lookup; returns rows written. `src/render/renderer.ts`: `MapRenderer` — square-canvas guard (D2 512²) + null-context throw; `setFields` allocates per-mode offscreen canvas + Uint32 buffer + ImageData at the *fields'* resolution (256² preview fields blit-upscale with `imageSmoothingEnabled = false`); `composeMode` loops `composeChunk` in 64-row slices → bulk `ImageData.data.set(byteView)` → `putImageData` offscreen; `draw`/`crossfade` are `drawImage`-only on the visible canvas (`globalAlpha` crossfade, alpha reset to 1). `rebuildBiomes(biases)` = LUT rebuild + biome-mode-only recompose. **`getImageData` appears nowhere in src/render** (D2 no-readback). `src/render/index.ts` re-exports the public API.
- **Files created:** `src/render/compose.ts`, `src/render/renderer.ts`, `src/render/index.ts`, `src/render/compose.test.ts`. No existing files modified (`main.ts`/app UI/noise/generation/worker untouched; `#map-canvas` keeps T1's `image-rendering: pixelated`).
- **Validation evidence:**
  - `npm test` → 7 suites, **85/85 passed** (13 new; node-env, pure layer per the pure/DOM split).
  - `npm run build` → exit 0 (strict `tsc --noEmit` validates `renderer.ts` against the DOM lib; vite emits the unchanged worker chunk `map.worker-C2FsIWiX.js`, 3.50 kB — worker untouched). Render modules are not yet in the entry bundle (nothing imports them until T6/T8/T10 wire them — by design; no build-graph edge needed since no separate chunk must be emitted).
  - **Perf (D2 budget <8ms/chunk, <40ms/frame):** 512² biomes compose, 8 chunks of 64 rows, 5 runs: avg chunk **0.091–0.450 ms**, max 0.145–1.279 ms, full frame **0.74–3.61 ms** — 18–90× headroom per chunk; not flaky. Log line: `[perf] 512² biomes compose: 8 chunks of 64 rows — avg 0.405ms, max 0.453ms, total 3.247ms`.
  - Correctness: packRGBA byte-view test ([r,g,b,255] in memory on any endianness) + platform-conditional word-formula check; biome LUT exhaustive vs `classify` at all 4096 bin centers (0 mismatches) and 200-cell biased sample vs `classify` with `moistureBias +0.3` (0 mismatches, shifts demonstrably present); ramp endpoints exact + midpoint within 2/channel; composeChunk exhaustive @64² seed 7: elevation/moisture byte-exact, biomes byte-exact vs LUT lookup; chunked [0,32)+[32,64) ≡ [0,64) element-wise in all three modes; row clamping + rows-written semantics.
- **Deviations:**
  1. **LUT-vs-classify per-pixel agreement threshold recalibrated 99.5% → 95%** (measured **96.95%** at 64², seed 7). 64×64 quantization puts a 1/64 ≈ 1.6%-wide fringe around every band boundary where the LUT picks the bin-center biome; the brief's ≥99.5% was a planning estimate that under-counted boundary density. Replaced with a stronger exact-correctness assertion: **every disagreeing pixel lies within one 1/64 bin of a D3 band boundary** (boundaries derived from `ELEVATION_BANDS`/`MOISTURE_BANDS`, not duplicated) — 0 unexplained disagreements, proving the LUT is classify-on-the-quantized-grid exactly as committed. Visual impact: none beyond the committed LUT design (boundary-fringe pixels get a neighboring biome's committed color).
  2. `rebuildBiomes` recomposes only when the biome surface already holds composed content; otherwise the LUT swap suffices (next `draw` composes lazily with the new LUT) — avoids wasted work when biomes aren't displayed.
  3. Renderer sets `imageSmoothingEnabled = false` so 256² D2 preview fields upscale nearest-neighbor, matching the CSS-pixelated final frame look.
  4. The `crisp-edges` CSS fallback line was **not** added: T1's `image-rendering: pixelated` is present on `#map-canvas`, but `style.css` is app-UI surface that this task was told not to touch — the one-line fallback belongs to T9 (canvas presentation). Flag for T9.
- **Next unblocked task:** T6 (controller — needs T5 + T7, both done) is unblocked; T9 (polish) also unblocked (dep T7).
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — 85/85 re-run (13 render tests; perf re-measured in verifier's run: avg 0.372ms/chunk, max 0.443ms, 2.982ms full 512² frame vs <8ms/<40ms budgets), build exit 0 with unchanged worker chunk, independent node probe exact-matched biomes pixels (0,0)/(31,31) and 5 grayscale samples to the LUT/formula; deviations 1–4 all assessed outcome-preserving (Dev 1: 96.95% agreement reproduced, 0 unexplained — every disagreement within one 1/64 bin of boundaries derived from `ELEVATION_BANDS`/`MOISTURE_BANDS` constants, quantization fringe is the committed LUT's expected behavior; Dev 2 lazy recompose unobservable — next draw uses the new LUT; Dev 3 strengthens D2's pixelated look; Dev 4 crisp-edges→T9 flagged).

---

## T6 — Update controller · Frontend

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** worker subagent (T6 brief: hybrid semantics + D2 preview path + town-hall cancel rules).
- **Outcome:** `src/controller/map-controller.ts` — `MapController` behind narrow structural ports (`WorkerPort` = generate/recomputeElevation, `RendererPort` = setFields/rebuildBiomes/draw), fully Node-testable. Routes the three committed semantics: (1) **moisture → instant local reclassify** — `setMoisture` clamps [0,1], derives `moistureBias = (v − 0.5) × 0.8` (±0.4, seaLevelBias 0), calls `renderer.rebuildBiomes + draw('biomes')` and NEVER the worker; (2) **elevation → debounced recompute with D2 preview** — slider maps `elevationFreq = 2.5 + 2v` (center 0.5 → 3.5 = default) and `redistribution = 1.2 + 0.6v` (center → 1.5) over `DEFAULT_FIELD_PARAMS`; drag = trailing `setTimeout` debounce (~30ms, stored handle → fake-timer friendly) → preview recompute at 256²; `commitElevation()` / `setElevation(v, {commit:true})` clears the timer → immediate 512² recompute. Preview moisture@256 cached per seed (`Map<seed, Float32Array>`; v1 moisture params are seed-independent → cache key = seed): cache miss → ONE `generate@256` whose elevation serves that preview directly (no extra recompute) and whose moisture half is cached; cache hit → `recomputeElevation@256` composed locally with the cached moisture into `{elevation, moisture, resolution: 256}` (no new views needed). (3) **seed / Generate-class → full 512² regen** — `regenerate(seed?)` / `setSeed` fire the registered animation-cancel hook FIRST (fire-and-forget, default no-op; never queue, never block), then `generate@512`; on arrival: fields cached as `currentFields`, `setFields + rebuildBiomes + draw('biomes')`, `{type:'fields', final:true}` emitted. `subscribe(listener)` returns an unsubscriber; events: `fields` / `preview` / `error` (`SupersededError` filtered out, nothing rethrown) / `state` (after every control change, before the worker round-trip). Stale-guard: `genEpoch` bumps on every Generate-class start and is compared at resolve time (a 512² elevation answer arriving after a newer regenerate is dropped); plus `issueEpoch` bumping on EVERY worker request issuance (deviation 1). `src/controller/create.ts` — `createMapController(canvas, workerFactory?)` wiring the real `MapWorkerClient` via the D4 inline pattern `new Worker(new URL('../worker/map.worker.ts', import.meta.url), { type: 'module' })` + `MapRenderer`; `src/controller/index.ts` re-exports the public API.
- **Files created:** `src/controller/map-controller.ts`, `src/controller/create.ts`, `src/controller/index.ts`, `src/controller/map-controller.test.ts`. No existing files modified (main.ts untouched — wiring is T10).
- **Validation evidence:**
  - `npm test` → 8 suites, **95/95 passed** (10 new; node env; `vi.useFakeTimers()` + manually-resolved deferreds; FakeWorkerPort call log + FakeRendererPort call log).
  - `npm run build` → exit 0 (strict `tsc --noEmit` validates `create.ts`'s inline `new Worker(new URL(...))` pattern; vite emits the unchanged worker chunk `map.worker-C2FsIWiX.js`, 3.50 kB — controller not yet in the entry bundle by design, T10 wires the graph edge, same precedent as T7's render modules).
  - Test matrix (spec areas 1–8): moisture sets worker log EMPTY, renderer log exactly [rebuildBiomes, draw('biomes')], bias 0.32, state event, unsubscribe honored; 6-call burst at 5ms spacing → exactly ONE `recomputeElevation@256` after 30ms carrying the LAST drag value's params (freq 3.2 @0.35), preview fields + `preview` event on resolve; commit clears the debounce (advance 1000ms → still 1 call), `recomputeElevation` resolution arg 512, final frame `[setFields, rebuildBiomes, draw('biomes')]`, cached 512² moisture reused by reference, `{type:'fields', final:true}`, `currentFields` identity; seed 7 cache behavior — first drag ONE `generate@256`, second drag `recomputeElevation@256` with zero generates and preview moisture === cached array by reference; stale 512² elevation response after a newer regenerate → zero renderer effect (last setFields provably seed-2's by marker); cancel hook order exactly `['cancel','request','cancel','request']` across two regenerates; `SupersededError` → no error event, `Error('boom')` → exactly one `{type:'error'}` with the same Error reference; clamps 5→1 and −1→0.
- **Deviations (minor):**
  1. **Added `issueEpoch` (latest-issuance-wins) on top of the specified `genEpoch`.** Generate and elevation are independent worker channels (T5), so worker latest-wins only guards superseding WITHIN a channel — the spec's two-epoch scheme leaves a cross-channel hole: e.g. a slow cache-miss preview `generate@256` resolving after a commit's 512² frame would clobber the newer render. Every worker request captures both epochs; a response publishes only if it is the latest issuance AND no newer Generate-class started. The mandated genEpoch comparison at resolve time is preserved exactly (test 5 proves it).
  2. `commitElevation()` before any generate has resolved falls back to a full `generate@512` (no cached 512² moisture to recombine); settles through the identical finalize path.
  3. Initial state seed 0, sliders centered 0.5/0.5 (= `DEFAULT_FIELD_PARAMS` / zero biases); the brief fixed the mappings but not the seed default — T11's first-load flow owns the real initial seed.
  4. `setMoisture` unconditionally calls `renderer.draw('biomes')` per the brief; the real renderer throws if no fields were ever delivered, so T10/T11 must deliver fields before user input — guaranteed by the first-load auto-generate (fake-port tests exercise the pre-fields call per spec).
  5. Test 2 warms the per-seed preview cache first: the first-ever drag is a cache-miss `generate@256` by the spec's own preview-assembly rule, so a burst test asserting `recomputeElevation@256` needs one warmed cycle; the 6-call burst itself then asserts exactly one recompute.
- **Next unblocked task:** T8 (staged animation — needs T6 + T7, both done; registers `setAnimationCancel` and consumes the `fields`/`preview` lifecycle events) and T10 (controls & error surface — needs T6; calls `commitElevation()` on pointer-up/change and renders worker failures from `error` events).
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — 95/95 tests + build exit 0 (worker chunk unchanged) re-run independently; fake-timer probe reproduced 5× setElevation(0.6) burst → exactly one `recomputeElevation@256` (elevationFreq 3.7, redistribution 1.56) and commit → 512² final (generate fallback, then `recomputeElevation@512` on second commit); genEpoch + issueEpoch both traced — issueEpoch outcome-preserving (worker latest-wins is per-channel, so the cross-channel slow-preview-clobbers-final-frame hole is real and required by latest-wins intent); deviations 2–5 plus Float32 5-decimal marker precision all assessed outcome-preserving (fallback identical by T5's generateFields→generateElevation delegation; seed 0 owned by T11; pre-fields draw ordering owned by T10/T11; cache warm-up is harness sequencing only).

---

## T8 — Staged animation controller · Frontend

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** worker subagent (T8 brief: staged Elevation→Moisture→Biomes reveal per D2 frame strategy).
- **Outcome:** `src/render/animation.ts` — `StagedAnimation` behind the narrow structural port `AnimationRendererPort` (`draw`/`crossfade`; `MapRenderer` satisfies it, tests fake it), fully node-testable via an injectable environment (`AnimationEnv`: `requestFrame`/`cancelFrame`/`now`/`prefersReducedMotion`; defaults = guarded global `requestAnimationFrame` → `setTimeout(cb,16)` fallback, `performance.now` → `Date.now` fallback). Choreography per D2: stage frames are the renderer's pre-composed mode surfaces, transitions are `crossfade` GPU blits — zero per-frame pixel work, no per-frame putImageData. All timing taste in ONE config (`AnimationConfig`: `stageMs` 1000 / `transitionMs` 400 / `easing` = `easeInOutQuad`; `DEFAULT_ANIMATION_CONFIG` total = 2×(stageMs+transitionMs) = **2800 ms** ∈ committed 2.5–4 s). Sequence: announce+`draw('elevation')` synchronously → hold → eased crossfade elevation→moisture (announce at transition start) → hold → eased crossfade moisture→biomes → **one opaque `draw('biomes')` after the last crossfade frame** (t=1) so no half-blended residue → announce `'done'`, resolve. Fade-1 window is end-inclusive so the moisture hold opens on a fully-opaque frame. `skip()` = the mid-animation input-cancel path (T10 registers it via `MapController.setAnimationCancel`): synchronous cancel + one `draw('biomes')`, announces only `'biomes'`-if-not-yet + `'done'` (skipped stage names never replayed), resolves the pending promise, no-op when idle; never queues, never blocks. `play()` while playing skips the old run first. Reduced motion: one `draw('biomes')`, announcements `['biomes','done']`, resolve immediately — zero frames scheduled. `onStageChange(cb)` returns an unsubscriber (aria-live feed for T10; each stage fires at most once per run, `'done'` always last). `dispose()` = hard stop without drawing, listeners dropped, pending promise still resolved `'done'` (documented on the method — avoids dangling awaits). Stale-frame safety: ticks compare run identity, so scheduler callbacks surviving a cancel are inert. `src/render/index.ts` re-exports the public API.
- **Files created:** `src/render/animation.ts`, `src/render/animation.test.ts`. Modified: `src/render/index.ts` (+animation re-exports). Renderer/controller/main untouched.
- **Validation evidence:**
  - `npm test` → 9 suites, **107/107 passed** (12 new; node env; manual frame-stepping harness — `requestFrame` pushes callbacks into a queue the test steps, `now()` reads a mutable fake clock, renderer fake records every call).
  - Test 1 measured **total elapsed = 2800 ms exactly** by final-frame fake-clock (asserted 2700–2900); fade ramps: 9 samples each at 50 ms steps, t=0 first / t=1 last, monotonic; exactly two draws per run (elevation, biomes); announcements `['elevation','moisture','biomes','done']`.
  - Skip mid-transition: exactly one new synchronous call (`draw('biomes')`), zero frames fire afterwards, `done` exactly once; skip when idle: pure no-op; skip during elevation hold: announcements `['elevation','biomes','done']` — no replayed stage names. Reduced motion: single `draw('biomes')`, no frames, resolves. Play-while-playing: old run skipped (draw biomes) then restart from elevation; both promises resolve `'done'`. Config isolation: 500/100 → 1200 ms total. Easing: endpoints/midpoint exact, f(0.25)+f(0.75)=1.
  - `npm run build` → exit 0 (strict `tsc --noEmit` + vite; worker chunk unchanged `map.worker-C2FsIWiX.js`, 3.50 kB).
- **Deviations (minor):**
  1. 12 new tests vs "~7–9" estimated — the 7 spec-pinned scenarios plus direct tests for dispose, unsubscribe, skip-during-hold announce semantics, DEFAULT constants, and split easing checks (T5 precedent: richer matrix, same scope).
  2. `AnimationEnv` exported as a named interface (the brief sketched the env inline but mandates injectable `now`) — T10 needs the type for its DOM wiring.
  3. Fade-1 end boundary made inclusive (t=1 frame drawn at the transition's last frame) — without it the moisture hold displayed at eased t=0.96875; the brief mandates the opaque-ensure only for the final biomes frame, but the same residue logic applies at the fade-1 boundary.
- **Next unblocked task:** T9 (polish — needs T7, already done) and T10 (controls — needs T6, already done); T11 still blocked on T13; T16 unblocked once verified (needs T6+T8).
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — 107/107 tests + build exit 0 re-run independently (12/12 animation; worker chunk unchanged); independent probe (fake renderer + manual frame queue + fake clock) reproduced moisture hold opening at `crossfade('elevation','moisture',1)` (fade-1 t-sequence 0…0.96875→1), synchronous skip → single `draw('biomes')` with an empty scheduler queue, stale pre-skip callbacks + 2 further steps → zero additional renderer calls, stages `['elevation','moisture','biomes','done']`; deviations 1–3 all outcome-preserving (12 tests = richer matrix, same scope; `AnimationEnv` export required by T10's wiring + injectable `now`; end-inclusive fade-1 eliminates the eased-t=0.96875 hold residue — same opaque-ensure rule the brief mandates for the final biomes frame, cost = one boundary blit).

---

## T10 — Controls & error surface · UI

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** worker subagent (T10 brief: controls + error/retry surface per town-hall + D5).
- **Outcome:** `src/ui/control-panel.ts` — `buildControlPanel(container, ports)` (DI-friendly, zero controller/animation imports; pure DOM builder, createElement only, no innerHTML): elevation + moisture `input[type=range]` (0–1 step 0.01, `<label for>`, live "0.50" readouts), seed row (text input `inputmode=numeric` + prominent 🎲 with `aria-label="Randomize seed"`), action row (Generate, Skip), preset row (Archipelago/Continent/Highlands — **disabled placeholders**, `data-preset`, T12 enables), utility row (Export/Share — **disabled placeholders**, `data-action`, T14 enables), stage label (`aria-live="polite"`, default ''), error region (`role="alert"`, `hidden`, message + Retry). Wiring per T6/T8 contract: elevation `input`→`setElevation(v)` / `change`→`commitElevation()`; moisture `input`→`setMoisture`; Generate→`regenerate()`; 🎲→`setSeed(rng())` (injectable, default `Math.floor(Math.random()*1_000_000)`); seed text parsed as int on change/blur — invalid ignored + display reverted to last valid, same-seed re-entry no-ops. Stage label maps elevation/moisture/biomes→Elevation/Moisture/Biomes, 'done'→'Biomes' (final stage name, no timers). Skip button enabled only while `animation.playing` (refreshed on stage events, incl. the synchronous 'elevation' announcement every play() makes); canvas-click-to-skip via `bindCanvasSkip(canvas)` guarded by playing. `state` events sync sliders/readouts/seed (echo guard: programmatic writes to a slider suppressed between its pointerdown/pointerup; readouts always update; focused seed input never clobbered; malformed payloads dropped via an `isControlState` unknown-narrower). Error handling: `{type:'error'}`→ generic friendly message "Worker failed — retrying" in the alert region + panel fully disabled except Retry → `ports.onRetry()`; `{type:'fields'}`→ error hidden + re-enabled. `destroy()` removes every registered listener (controls + canvas + both subscriptions). `src/app.ts` — `startApp(canvas, container?)`: builds the whole stack (`createMapController` → `StagedAnimation(renderer, undefined, {prefersReducedMotion})` → `setAnimationCancel(()=>{animation.skip(); return true})` → panel + `bindCanvasSkip`); panel ports are stable forwarding shims over listener sets that survive rebuilds, so retry/rebuild swaps controller+animation without stale subscriptions. Retry (D5): capture current seed → dispose old controller+animation (debounce cleared, late worker resolutions settle silently, fresh worker via re-run of `createMapController`'s inline `new Worker(new URL(...))`) → `regenerate(lastSeed)`; fresh `fields` event re-enables the panel. `src/main.ts` replaced: queries `#map-canvas` + `#controls`, calls `startApp`, **no auto-generate** (T11 owns boot), dark-fill placeholder removed. `index.html`: `<aside id="controls">` added next to the canvas. `src/ui/index.ts` re-exports the panel API.
- **Files created:** `src/ui/control-panel.ts`, `src/ui/control-panel.test.ts`, `src/ui/index.ts`, `src/app.ts`. Modified: `src/controller/map-controller.ts` (+`dispose()`/disposed guards), `src/controller/create.ts` (returns `{controller, renderer}` as `MapControllerBundle`), `src/controller/index.ts` (+bundle type re-export), `src/main.ts` (rewritten), `index.html` (+aside), `package.json` (+`happy-dom@^20.11.10` devDep).
- **Validation evidence:**
  - `npm test` → 10 suites, **119/119 passed** (12 new; per-file `// @vitest-environment happy-dom` — no global vitest config change; node-env suites untouched). Matrix: elevation input/change semantics (change does NOT re-fire setElevation); moisture; 🎲 injected rng (424242) + Math.random default (0.999999→999999) + display update; seed valid/invalid/re-blur/same-seed cases; Generate; skip button idle/playing/click + canvas click idle-vs-playing; stage label full sequence + aria-live; error → alert visible/message/disabled-everything-but-Retry/retry-callback/fields-recovery; state sync + drag guard (mid-drag slider frozen at pointer value, readout tracks, other slider synced, pointerup releases) + malformed state ignored; presets/export/share placeholders + data attributes + label[for] associations + range semantics; destroy (no controller calls after, no state sync, no stage label change, canvas listener removed even when bound post-destroy).
  - `npm run build` → exit 0 after clean `dist/` (strict `tsc --noEmit` + vite). Worker chunk **unchanged**: `dist/assets/map.worker-C2FsIWiX.js` 3.50 kB, zero import/export tokens (pure iife, D5) — the graph edge moved from T5's temporary `main.ts` side-effect import to the real `main.ts → app.ts → create.ts` inline `new Worker(new URL(...))` instantiation.
  - Dev-server smoke: `npm run dev` → `curl http://localhost:5173/biome-generator/` → HTTP 200, HTML contains `id="controls"` and `id="map-canvas"`; server killed.
- **Deviations (all flagged in the brief or minor):**
  1. `happy-dom@20.11.10` devDependency (pre-approved production-level enabler for DOM-logic tests; per-file environment comment only — no global config).
  2. `createMapController` now returns `{controller, renderer}` (`MapControllerBundle`) — additive, app.ts needs the renderer to construct `StagedAnimation`; no existing caller broke (only index re-export referenced it).
  3. `MapController.dispose()` added (flagged): sets `disposed`, clears the debounce, drops listeners/cancel hook; every public mutator no-ops and every in-flight worker `.then`/`.catch` settles silently. Worker termination/recreation stays with the client (D5) — `createMapController` builds a fresh client+worker per call.
  4. `index.html` gained `<aside id="controls">` (flagged); aside is unstyled — presentation is T9's.
  5. Presets/Export/Share are present-but-disabled placeholders (per brief; T12/T14 enable) with `data-preset`/`data-action` hooks.
  6. Retry re-runs the last Generate-class intent as `regenerate(lastSeed)` on the fresh stack; elevation/moisture slider positions reset to center (restoring them pre-fields would `draw` against the field-less real renderer, which T6 deviation 5 forbids) — documented in app.ts.
  7. Removed T5's temporary `import './worker/index.ts'` build-graph edge from `main.ts` — superseded by the real instantiation edge; worker chunk hash byte-identical (evidence above).
  8. 12 new tests vs "~9–11" estimated — the spec's areas (a)–(i) plus default-rng and malformed-state coverage (T5/T8 precedent: richer matrix, same scope).
- **Next unblocked task:** T13 (URL-hash state — needs T10 types ✓ `PanelControllerEvent`/`ControlState` shape via controller events) and T12 (presets — T3+T10 ✓, enable the placeholder buttons); T11 still blocked on T13.
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — 119/119 tests + clean-dist build exit 0 (worker chunk `map.worker-C2FsIWiX.js` byte-identical pure iife — first line `(function(){…`, zero ESM tokens — with the real `new Worker(new URL(...))` edge now in the entry bundle) and dev-smoke 200 with `#controls`/`#map-canvas` all re-run independently; independent happy-dom probe re-drove input@0.8 → `setElevation(0.8, non-commit)` / change → `commitElevation` (no re-fire), error → alert visible + panel disabled + Retry live, retry → `onRetry`×1, fields → hidden + re-enabled, plus `MapController.dispose` guards (post-dispose mutators no-op — zero new worker requests/events/renders; late resolve AND reject settle silently — no zombie renders); onerror/onmessageerror → `rejectAllPending(WorkerCrashError)` → controller `error` event → panel traced in source and built bundle; deviations 1–8 all assessed outcome-preserving (happy-dom pre-approved; bundle-return additive; dispose flagged and probe-verified; unstyled aside = T9's; placeholders per T12/T14 dependency order; T5 temp import removal proven equivalent by the unchanged chunk hash; 12-vs-~9-11 tests = richer matrix); **UX follow-up (non-blocking):** retry resets sliders to center — outcome-preserving for the committed 'friendly error + retry, no white screen' criterion (restoring pre-fields would violate T6's renderer contract; fix later by re-applying slider positions after the fresh `fields` event, and call `client.terminate()` on the dropped worker during rebuild for strict rq5 terminate-before-recreate hygiene — correctness unaffected since the old worker is never reused and its late responses are guard-dropped).

---

## T13 — URL-hash state · Frontend

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** worker subagent (T13 brief: pure state module, no DOM wiring — T11 reads on boot, T14's share button writes).
- **Outcome:** `src/state/urlState.ts` — full-control-state hash codec + location ports. `SharedState` = `{version, seed, elevation, moisture, preset}` with `STATE_VERSION = 1` (forward compatibility: `parseHashBody` gates on `v` and defaults unknown formats, ready for future migration branches); `DEFAULT_SHARED_STATE` = v1 / seed 0 / sliders centered 0.5/0.5 / preset null (matches `MapController`'s initial state). `serializeState` → compact body `v=1&seed=42&el=0.5&mo=0.75&preset=continent` (no leading `#`; sliders 2-decimal with trailing zeros trimmed — "0.5"/"0.75"/"1"/"0"; seed floored ≥ 0; preset key omitted when null). `parseHashBody` — total sanitization, never throws: empty/whitespace/garbage/missing-or-wrong `v`/unparseable number → `DEFAULT_SHARED_STATE`; well-formed-but-out-of-range numbers sanitize per-field instead (sliders clamp to [0,1], seed floors to integer ≥ 0 with negative → default seed 0, preset must match `/^[a-z]{1,24}$/` else null); parse via `URLSearchParams` (never throws on malformed %-sequences, unlike raw `decodeURIComponent`); strict number parse guards `Number('') === 0` forgery and rejects non-finite. `LocationPort` (`{hash, replaceState}`) is the only effect boundary — zero DOM globals in module code (grep-verified; `globalThis.location` never referenced; wiring belongs to T11/T14). `readStateFromLocation`: `''`/`'#'` → null (caller applies defaults), `'#v=1&…'` → parsed body. `writeStateToLocation`: immediate `replaceState('#'+body)` — replaceState-only semantics, no history entries. `createUrlStateWriter(loc, {delayMs=200, schedule=setTimeout, cancel=clearTimeout})`: trailing-edge coalescing — first `push` in a batch arms ONE timer, pushes inside the window only replace the pending state, the fire writes the LAST state exactly once; `flush()` forces the pending write synchronously AND cancels the armed timer, no-op when nothing pending; injectable scheduler keeps it timer-free in tests. `src/state/index.ts` re-exports the public API (+`UrlStateWriter`/`UrlStateWriterOptions` named types).
- **Files created:** `src/state/urlState.ts`, `src/state/index.ts`, `src/state/urlState.test.ts`. No existing files modified.
- **Validation evidence:**
  - `npm test` → 11 suites, **128/128 passed** (9 new; node env; FakeLocation records replaceState urls and structurally exposes no pushState; FakeScheduler = cancellable timer queue fired by hand).
  - `npm run build` → exit 0 (strict `tsc --noEmit` + vite; worker chunk unchanged `dist/assets/map.worker-C2FsIWiX.js`, 3.50 kB — state layer not yet in the entry bundle by design; T11/T14 own the wiring graph edge, same precedent as T7/T6).
  - Test matrix per spec: 4 slider-exact round-trips (default; seed 999999 + 0/0 + null; seed 42 + 1/1 + 'continent'; seed 7 + 0.37/0.82 + 'archipelago') deep-equal through serialize→parse; exact canonical string `'v=1&seed=42&el=0.5&mo=0.75&preset=continent'`, null-preset key omission, no leading '#'; malformed `''`/`'   '`/`'garbage'`/`'v=2&seed=1'`/`'v=1&seed=abc'`/version-less → DEFAULT_SHARED_STATE with no exception; field-level sanitization `'v=1&el=9&mo=-4'` → 1/0 (clamped, not defaulted), `'v=1&seed=-5'` → seed 0, `'v=1&preset=EVIL!%20injection'` → null, `'v=1&seed=3.7'` → 3; readStateFromLocation `''`/`'#'` → null, `'#v=1&seed=5'` → seed 5; writeStateToLocation single replaceState `'#<body>'`; coalescing writer — 5 pushes → exactly ONE armed timer at the default 200 ms with zero writes, fire → exactly one replaceState carrying the LAST state, flush-when-idle → no write + no cancel, push+flush → immediate write + cancel called + late fire of the cancelled timer inert; DEFAULT_SHARED_STATE sanity.
- **Deviations (minor):**
  1. Unparseable numeric field anywhere (e.g. `seed=abc`) → whole body falls back to `DEFAULT_SHARED_STATE`, while out-of-range-but-parseable numbers sanitize per-field — per the brief's doc-comment clauses ("unparseable numbers" → defaults; clamping/flooring for the rest). The two readings coincide on every spec'd example; the implemented split is the stricter one for mixed bodies (`v=1&seed=5&el=abc` → defaults, not seed-5-with-default-slider).
  2. Slider serialization trims trailing zeros ("0.5", not "0.50") — required by the brief's own canonical example (`el=0.5`), so "2-decimal" reads as "rounded to 2 decimals, compact form".
  3. `serializeState` defensively sanitizes its input (floor/clamp/pattern-check preset before emitting) so a shared URL can never carry garbage even if a future caller passes an unclean state — round-trip exactness unaffected for valid states.
  4. `getUrlStateWriter` options/return types exported as named interfaces (`UrlStateWriterOptions`, `UrlStateWriter`) instead of inline literals — T11/T14 need the types for wiring; identical shape.
- **Next unblocked task:** T11 (first-load experience — needs T8 ✓ + T13 ✓, reads state on boot), T14 (share + export — needs T13 ✓ + T7 ✓, share button copies the serialized URL), and T15 (QA determinism suite — needs T2 ✓ + T3 ✓ + T13 ✓, URL round-trip leg now available).
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — 128/128 tests + build exit 0 (worker chunk `map.worker-C2FsIWiX.js` unchanged) re-run independently; independent 44-input hostile fuzz against the real module (embedded nulls, `&&&`, `seed=1e18`/`1e21`, `NaN`/`Infinity`/`1e309`, duplicate keys, script/unicode/25-char presets, 10k-char + 5k-key bodies, malformed %-sequences) — **0 throws, 0 invalid states** (`seed=1e18` stays: finite integer ≥ 0 per the floor rule; `mo=NaN`/`el=Infinity`/`seed=abc`/whitespace-only `el` → whole-body defaults; `-0.3`→0, `-5`→0, `3.7`→3; all preset abuse → null, 24-char slug kept); duplicate keys resolve **first-wins** (URLSearchParams `.get()` spec semantics — the brief's "last-wins" parenthetical was factually off, but either resolution is safe and never crashes); 5/5 verifier-chosen round-trip states deep-equal, canonical string + trailing-zero trim byte-exact, serialize idempotent; writer harness reproduced 5-push → ONE 200 ms timer → ONE write of the last state, flush = immediate write + cancel with late-fire inert, idle flush no-op; zero DOM globals / zero `pushState` in module code (grep; LocationPort only); deviations 1–4 all outcome-preserving (unparseable-vs-out-of-range split matches the brief's clauses and is stricter on mixed bodies; trim mandated by the canonical example; defensive serialize is idempotent for valid states and even a NaN-poisoned state emits a body that parses to defaults; named type exports additive); mtime evidence confirms only the 3 `src/state` files touched.

---

## T9 — Palette application & polish · UI/Visual

- **Status:** awaiting-approval · **Executed:** 2026-08-27 · **Delegation:** worker subagent (T9 brief: palette-luminance test suite + canvas presentation + responsive layout + beach knob).
- **Outcome:** (1) **Automated palette-luminance suite** `src/generation/biome-palette.test.ts` (node env): WCAG 2.2 luminance recomputed from hex (T4 precedent); the D3 adjacency set — **20 table-adjacent pairs** — is transcribed literally AND re-derived from `classify` over the 7-band × 4-moisture-column crosswalk grid (derivation ≡ transcription, count/dup/self-pair guards, so a transcription slip cannot hide); **ΔL ≥ 0.08 asserted for every pair on both the committed palette and the beach-alternate palette**; grayscale band-mean ordering chain deepOcean < ocean < lowland mean < highland mean < mountain-band mean (incl. tundra, per brief) < snow, plus beach-brighter-than-ocean in both variants; `BEACH_ALTERNATE_HEX = '#d8c790'` exported from `biomes.ts` as a tunable constant (default beach untouched, guarded by test; measured L = 0.5746, D3 said ≈ 0.57). (2) **CVD check — implemented the real Machado, Oliveira & Fernandes (2009) deuteranopia simulation at severity 1.0**: the 3×3 matrix was verified against the authors' published simulation-matrices table (inf.ufrgs.br) at full precision before transcription (not from memory — the brief's uncertainty was about guessed values); applied to LINEAR RGB per the paper, luminance recomputed directly off the simulated linear channels, then the same 20-pair ΔL ≥ 0.08 suite on both palettes; row-sums-to-1 + white-invariance guards protect the transcription. The structural checks were ALSO implemented (not either/or): no red-dominant color (red channel share < 0.55 of channel sum for all 12 biomes) and the five green-family biomes pinned to the 45°–200° green→cyan hue arc. (3) **Presentation polish**: `src/style.css` fully rewritten + `index.html` gained the `#app-header` (h1 "Procedural Biome Generator" + tagline) — palette-derived dark theme (bg #111827, text #e5e7eb, ocean #20658c primary fills, lighter ocean #3d8ab8 focus rings); canvas gets the **`crisp-edges` fallback line BEFORE `pixelated`** (T7's deferred flag), `aspect-ratio: 1`, `min(90vmin, 640px)` desktop / `width: 100%` at ≤ 640px; flex-wrap row (canvas + fixed 280px controls aside) stacking below 640px, usable at 375px (`max-width: 100%`, 2.5rem touch-height controls, no horizontal scroll); controls per spec (Generate = primary filled, Skip = subtle, 🎲 = square icon button, presets = 3 equal, Export/Share = quiet with clear disabled states, stage label = status line with reserved `min-height` + "Stage: " prefix when non-empty, error region = `#fca5a5` on `#7f1d1d` with an `[hidden]` display guard so `display:flex` cannot defeat the hidden attribute); `:focus-visible` outlines on all interactive elements; `prefers-reduced-motion` CSS guard; chrome-only transitions — nothing fights the T8 canvas animation. (4) **Layout smoke** `src/ui/layout.test.ts` (happy-dom): parses the real `index.html` via DOMParser (canvas 512² attrs + aria-label, canvas-before-controls source order in `#app`, header/viewport/title/lang, module script → main.ts) and mounts the REAL `buildControlPanel` into a fresh aside (2 labeled ranges, seed input, 9 buttons incl. disabled T12/T14 placeholders, `aria-live` stage line, `role=alert` hidden error region); asserts `style.css` content (crisp-edges-before-pixelated order, 640px media query, `max-width: 100%`, `aspect-ratio`, reduced-motion) and computes WCAG contrast of the `:root` tokens **parsed from the CSS itself** (focus ring ≥ 3:1 on bg AND panel; body text ≥ 4.5:1 on both surfaces; error text ≥ 4.5:1 on error bg).
- **Files created:** `src/generation/biome-palette.test.ts`, `src/ui/layout.test.ts`. Modified: `src/generation/biomes.ts` (+`BEACH_ALTERNATE_HEX` export only — no default hex changed), `src/style.css` (rewritten), `index.html` (+header; canvas/aside/script structure unchanged).
- **Validation evidence:**
  - `npm test` → 13 suites, **147/147 passed** (19 new: 12 palette + 7 layout; the prior 128 untouched).
  - `npm run build` → exit 0 after clean `dist/` (strict tsc + vite); worker chunk **unchanged** `map.worker-C2FsIWiX.js` (3.50 kB, D5 iife intact); emitted CSS asset 3.72 kB contains crisp-edges→pixelated in the correct order + `#app-header` rules; `dist/index.html` carries the header markup.
  - Dev-server smoke: `npm run dev` → `curl -L http://localhost:5173/biome-generator/` → **HTTP 200**, served HTML contains `#map-canvas` / `#app-header` / `#controls`; server killed.
  - Palette facts re-measured by the suite + independent probe: normal-vision min adjacency ΔL = **0.1052** (deepOcean–ocean), 2nd 0.1171 (grassland–mountain); deuteranopia-simulated min = **0.0961** (deepOcean–ocean), 2nd 0.1105 (savanna–grassland) — identical minima under the beach-alternate palette (beach is never the binding pair).
- **Deviations:**
  1. **CVD check chosen:** real Machado 2009 severity-1.0 deuteranopia simulation (matrix verified against the published source pre-transcription) PLUS both structural checks. The brief's literal "±40° hue window among the five greens" was factually unsatisfiable — measured spread ≈ 137° (savanna 52° … taiga 189°) — and "pairwise ΔL ≥ 0.08 among all five greens" is not a D3 claim (rainforest–forest ΔL = 0.027, but they are never adjacent: beach/grassland/taiga columns separate them). Honest formalization: adjacency-scoped luminance separation (main + simulation suites) + no-reds + hue-arc containment, all documented in-test.
  2. 19 new tests vs "~8–12" estimated — the brief sketched more cases than its own estimate (T5/T8/T10 precedent: richer matrix, same scope).
  3. "Stylesheet link present" interpreted as: **no `<link>` added to index.html** — this Vite app loads `style.css` via `main.ts`'s import (single CSS path; a second link would double-serve the same file in dev and build). The layout test asserts the module-script entry (the stylesheet carrier) and the stylesheet file content directly.
  4. Mountain-band grayscale mean = mean(Mountain, Tundra) per the T9 brief's "incl. tundra"; the research doc's "mountain 0.451" used Mountain alone — the ordering chain holds either way (0.326 < 0.523 < 0.916).
  5. 375px visual confirmation (rendered no-horizontal-scroll / one-screen feel) is structural for now (max-width + media query + touch heights, asserted on the CSS text) — actual browser rendering check deferred to the coordinator's browser pass before T17, alongside the D3 "Coblis/Sim Daltonism screenshot" acceptance item which the automated Machado simulation now covers analytically.
- **Next unblocked task:** T17 (human comprehension check — deps T8 ✓ + T9 ✓ now satisfied; note T17 is a human-procedure halt task). T12/T11/T14 remain pending on their own deps, unaffected.
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — 147/147 tests + clean-dist build exit 0 (worker chunk unchanged) + dev-smoke 200 all re-run independently; Machado matrix internally consistent (row sums ≈ 1, white → white) and hand-checked: forest–taiga sim ΔL = 0.112 (probe 0.1117) and min pair deepOcean–ocean 0.0961 / normal 0.1052 reproduced exactly; deviations 1–5 all outcome-preserving (adjacency-scoped ΔL + no-reds + hue-arc is at least as strong as the unsatisfiable ±40° window for D3's committed adjacency-legibility outcome — green-family hue spread ≈136° confirmed, rainforest–forest ΔL 0.0278 non-adjacency matches D3's own reasoning); rendered-browser 375px + Coblis visual confirmation remains an explicit follow-up for the coordinator's pre-T17 browser pass (deviation 5).

---

## T12 — Presets · Product/UX

- **Status:** executed 2026-08-27 — pending verifier (ultron-supreme) · **Delegation:** worker subagent (T12 brief: 3 fixed presets as parameter snapshots + slider positions, click = batched animated regen, no user-defined presets).
- **Outcome:** `src/presets/` — new pure registry module. `PresetDefinition` = `{id, name, elevation, moisture, overrides}` where `overrides: Partial<Pick<FieldParams, 'falloffStrength'|'elevationOctaves'|'moistureFreq'|'moistureOctaves'>>` (the allowlist deliberately excludes the slider-derived keys `elevationFreq`/`redistribution` and all seed-stability keys; exported as `PresetOverrideKeys`). `PRESETS` = exactly the three committed snapshots — **archipelago** (el 0.85 → freq 4.2/redist 1.71, mo 0.55, `{falloffStrength: 0.35, elevationOctaves: 6}` — strong rim falloff + extra octave → scattered islands), **continent** (el 0.40 → 3.3/1.44, mo 0.50, `{falloffStrength: 0.08}` — mild falloff → one big landmass), **highlands** (el 0.95 → 4.4/1.77, mo 0.35, `{falloffStrength: 0.0, moistureOctaves: 4}` — no islanding, tall rugged drier terrain); taste-tunable constants. `findPreset(id)` → registry object or null (ids are lowercase slugs matching T13's `/^[a-z]{1,24}$/` wire pattern). `MapController.applyPreset({elevation, moisture, overrides})` (additive, flagged): clamps both sliders, installs (or clears on `null`) a copied override set, cancels any pending elevation-drag debounce, emits ONE batch state event, then fires ONE Generate-class `regenerate()` (animation-cancel hook first — T8 skip path). Overrides live in a private `paramOverrides` merged LAST in the `fieldParams` getter (override keys win over defaults and slider-derived values for the keys they carry; slider-derived `elevationFreq`/`redistribution` never collide since the allowlist excludes them). **Override-clearing semantics (decided, documented in the docstring + pinned by tests):** manual elevation input (`setElevation`, drag or commit form) clears overrides → custom terrain, fully slider-derived from the next worker request; `setMoisture` does NOT clear them (moisture is classification-only by committed scope — a climate nudge must never change terrain character); `regenerate`/`setSeed` keep them (Generate re-runs current params). Panel (T10 surface): the 3 preset buttons now build straight from `PRESETS` (registry/UI can't drift), start ENABLED with `aria-pressed="false"`; click → `ports.controller.applyPreset(findPreset(id))` + pressed-marking; `state` events re-evaluate pressed state via 2-decimal slider matching (matching event re-marks — idempotent with the applyPreset echo; any manual deviation clears all pressed = custom). `ControlPanelPorts.controller` gained `applyPreset(preset: PresetDefinition)` (typed structural port; fakes satisfy it structurally). `app.ts` (additive, flagged): forwards the port to `controller.applyPreset`, tracks `activePresetId` via a stable rebuild-surviving event listener using the same 2-dec matching rule, and returns `getActivePreset(): string | null` from `startApp` for T13 read / T14 share state assembly. Removed the now-unused `capitalize` helper from control-panel.ts (preset labels come from the registry).
- **Files created:** `src/presets/presets.ts`, `src/presets/index.ts`, `src/presets/presets.test.ts`. Modified: `src/controller/map-controller.ts` (applyPreset + paramOverrides + fieldParams merge + setElevation override-clear + docstrings), `src/ui/control-panel.ts` (registry-built enabled preset buttons + applyPreset port + aria-pressed sync), `src/ui/control-panel.test.ts` (FakeController.applyPreset + updated placeholder assertion + 4 new tests), `src/ui/layout.test.ts` (makePorts + applyPreset noop; presets-enabled assertion), `src/app.ts` (applyPreset wiring + getActivePreset).
- **Validation evidence:**
  - `npm test` → 14 suites, **163/163 passed** (16 new; the prior 147 untouched). New matrix: registry (6, node) — exactly 3 in order with unique ids `archipelago|continent|highlands`, names, every id matches `/^[a-z]{1,24}$/`, sliders finite in [0,1], overrides touch only allowed keys (never elevationFreq/redistribution; octaves integers ≥1; falloff in [0,1]), all three characters pairwise distinct (sliders+overrides signature), `findPreset` known ids → the registry objects (`toBe`), `'nope'`/`''`/`'Continent'`/`null` → null. Controller (6, node, fake ports) — applyPreset issues exactly ONE generate@512 + ZERO recomputes carrying the overrides (falloff 0.35, octaves 6) with slider-derived freq 4.2/redist 1.71 intact, cancel hook ordered BEFORE the request (`['cancel','request']`), ONE batch state event `{seed:0, elevation:0.85, moisture:0.55}`; pending drag debounce cancelled (advance 1000ms → no stray preview, only the 512² generate); `fieldParams` layers overrides over defaults with slider-derived keys still derived and untouched keys default; manual `setElevation(0.5)` returns fieldParams to exactly `DEFAULT_FIELD_PARAMS` while a preceding `setMoisture(0.9)` did NOT clear the override; `applyPreset(…, null)` clears previously installed overrides; `regenerate(42)` re-sends the override. Panel (4, happy-dom) + 2 updated assertions — preset buttons enabled at build with aria-pressed false (control-panel.test.ts placeholder test updated per brief; layout.test.ts real-mount updated likewise); click archipelago → applyPreset with the exact registry payload (then highlands → second call); pressed toggling true-on-clicked/false-on-others and moves across clicks; manual state event with non-matching sliders clears all pressed; state events matching preset sliders (incl. 0.854 → 2-dec 0.85) re-mark pressed idempotently.
  - `npm run build` → exit 0 after clean `dist/` (strict `tsc --noEmit` + vite). Worker chunk **unchanged**: `dist/assets/map.worker-C2FsIWiX.js` 3.50 kB (D5 iife intact — presets/controller/panel changes never enter the worker graph).
  - Numeric distinctness probe (seed 0, 256², sea level 0.40): archipelago land 5.9% / meanE 0.155, continent 30.2% / 0.330, highlands 25.1% / 0.302 — three measurably distinct terrain characters to anchor the visual review.
- **Deviations:**
  1. Controller additive method + `paramOverrides` field (flagged in brief). The DECIDE item resolved as: manual elevation input clears overrides; **moisture input does not** (classification-only scope invariant — a climate nudge changing falloff/octaves would mutate terrain character without a terrain regen); regenerate/setSeed keep them. Documented in the `applyPreset` docstring.
  2. `ControlPanelPorts.controller.applyPreset` typed as `PresetDefinition` (the brief's alternative to an inline `overrides: unknown` literal) — still structural; panel test fakes satisfy it without imports beyond the type.
  3. `app.getActivePreset()` returns the active preset **id** (`string | null`), matching T13's `SharedState.preset` shape directly (brief left id-vs-definition open).
  4. Preset buttons are now generated from `PRESETS` instead of the hardcoded id list (guarantees registry/UI consistency; made `capitalize` unused → removed). Existing assertions expecting disabled presets updated in `control-panel.test.ts` + `layout.test.ts` (sanctioned by the brief).
  5. Known limitation (logged for T13/T14): the wire format carries no override set — after a manual elevation drag away-and-back to a preset's exact slider values, pressed-state/`getActivePreset` re-mark the preset while controller overrides are cleared (a fresh load re-applies the full preset), and a moisture-only tweak after a preset shares a URL whose default-parameter regen differs from the on-screen map. Both are 2-dec-matching consequences of the committed `SharedState` shape; revisit only if share fidelity demands it.
  6. 16 new tests vs "~10–14" estimated (T5/T8/T9/T10 precedent: richer matrix, same scope).
  7. Visual terrain-character sign-off (three screenshots + user review per acceptance) deferred to the coordinator's browser pass — numeric distinctness evidence above (5.9/30.2/25.1% land) stands in analytically; falloff 0.35 giving only 5.9% land at seed 0 is deliberately sparse but is exactly the "scattered islands" intent; tune the constants if the browser pass disagrees.
- **Next unblocked task:** T11 (first-load experience — needs T8 ✓ + T13 ✓, both already complete, so T11 is the next critical-path task; it can also consume `getActivePreset()` when assembling boot state). T14 (share+export — T13 ✓ + T7 ✓) and T15 (QA suite — T2/T3/T13 ✓) were already unblocked and unaffected.
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — 163/163 tests + clean-dist build exit 0 (worker chunk `map.worker-C2FsIWiX.js` unchanged, pure iife) re-run independently; independent generateFields probe (256², seed 0, sea level 0.40, controller slider mapping replicated over the registry snapshots) reproduced land fractions **5.9% / 30.2% / 25.1%** (meanE 0.155/0.330/0.302) — three clearly distinct terrain characters; visual sign-off still owed to the coordinator's browser pass (deviation 7). Deviations 1–6 assessed outcome-preserving: the decided override semantics (manual `setElevation` clears → custom slider-derived terrain; `setMoisture` keeps → climate nudge never mutates terrain character; `regenerate`/`setSeed` keep → Generate re-runs current params) are documented in the `applyPreset` docstring and pinned by tests; typed structural port; id-returning `getActivePreset`; registry-built buttons; the 2 updated assertions are the brief-sanctioned disabled→enabled flip and still verify registry ids/labels/aria-pressed AND that export/share remain disabled T14 placeholders — not rubber-stamps. **KNOWN v1 wire-fidelity corner routed to T11/T14:** `SharedState` carries the preset id but not the override state, so once T11's boot re-applies preset overrides whenever the id is present, (a) preset → elevation drag → drag back to the same 2-dec slider values re-marks the id and shares a URL whose boot re-applies overrides the sharer's session had cleared, and (b) preset → moisture-only tweak keeps overrides live but drops the id (sliders no longer match) so the shared URL regenerates slider-params-only — in both journeys the restored map is NOT byte-identical to the sharer's screen; T11's verifier must handle or document.

---

## T11 — First-load experience · Frontend

- **Status:** executed 2026-08-27 — pending verifier (ultron-supreme) · **Delegation:** worker subagent (T11 brief: boot/restore + animation routing + URL write-back + the two T12 verifier-flagged fidelity corners).
- **Outcome:** The first-load experience is now real. `src/app.ts` reworked around an injectable `startApp(canvas, container?, opts?)`: **boot** (once, at construction) reads the location port — a stateful hash restores seed/sliders/preset through the NEW `controller.applySharedState` batch (one state event, ONE `generate@512`, preset overrides re-installed iff the id resolves via `findPreset` → restored map identical to the sharer's); a bare URL boots the canonical **continent** preset with the deterministic default seed 0 (`DEFAULT_BOOT_PRESET` export) and writes the resulting state to the hash. **Animation routing:** the app-level controller listener plays the staged animation on every `fields` event with `cause: 'generate'` (boot/restore, Generate, 🎲, preset click) and does nothing on `cause: 'elevation-commit'` (controller already drew instantly); `play()` is still called under reduced motion so aria-live announcements fire (the real `StagedAnimation` handles the immediate draw, T8-tested); the returned promise is caught-and-logged so no rejection ever dangles. **URL write-back:** `createUrlStateWriter` keeps the hash current — one `push({version, seed, elevation, moisture, preset: activePresetId})` per `state` event plus the boot pin (seed/🎲/preset changes all arrive as state events, so one subscription covers everything). **Sticky preset id (resolves T12's flagged corners (a)+(b)):** `activePresetId` is set on preset click and on restored boot, and cleared ONLY when a state event finds `controller.hasOverrides === false` (i.e. an elevation drag/commit cleared the terrain overrides) — the old 2-decimal slider re-derivation is deleted. Corner (a) preset→elevation-drag→drag-back: overrides gone → id stays null → shared URL = pure slider params, faithful. Corner (b) preset→moisture-tweak: overrides live → id stays on → shared URL re-applies the preset's overrides, faithful. **Retry/rebuild** now re-applies the LAST FULL shared state via `applySharedState` (replacing the old capture-seed-only `regenerate(lastSeed)` — sliders and preset survive a worker failure; safe pre-fields because `applySharedState` never draws), which also resolves T10's logged follow-up. `MapController` additive changes (flagged): `fields` events carry `cause: 'generate' | 'elevation-commit'` (regenerate/applyPreset/applySharedState paths → 'generate'; commit paths incl. the generate fallback → 'elevation-commit'; previews unchanged), `get hasOverrides` exposes override liveness, `applySharedState` batch-restores; `FieldsCause` re-exported from the controller index. `main.ts` verified unchanged — `startApp(canvas)` now boots automatically.
- **Files created:** `src/app.test.ts`. Modified: `src/app.ts` (rework — T11's main surface), `src/controller/map-controller.ts` (additive: cause field, `hasOverrides`, `applySharedState`, `findPreset` import), `src/controller/index.ts` (+`FieldsCause`), `src/controller/map-controller.test.ts` (+6 tests). Panel/render/state/presets modules untouched (imports only).
- **Validation evidence:**
  - `npm test` → 15 suites, **180/180 passed** (17 new: 6 controller + 11 app; the prior 163 untouched). App matrix (happy-dom, REAL panel + injected fakes): bare boot — exactly ONE `applySharedState({seed:0, el:0.5, mo:0.5, presetId:'continent'})`, only other controller call is the stack's cancel-hook registration, one replaceState `#v=1&seed=0&el=0.5&mo=0.5&preset=continent`; stateful boot — `{seed:42, el:0.8, mo:0.3, presetId:'archipelago'}` from hash, id sticky, URL round-trips byte-exact; pattern-valid-but-UNREGISTERED preset id (T13's slug pattern rejects hyphens — `unregistered` probes the findPreset-miss corner) → no overrides, id null, URL drops the preset key; animation — generate-cause plays, elevation-commit and preview never, second generate plays again; reduced motion — env forwarded AND `play()` still called once; sticky — continent click → id set, moisture tweak keeps it (`el=0.4&mo=0.7&preset=continent` in the hash), elevation drag (even back to the exact preset value 0.4) nulls it and the hash loses `preset=`; seed change flows through the same push; retry — error → Retry click → `applySharedState({seed:42, el:0.7, mo:0.3, presetId:null})` (full state, not just seed) + fresh animation stack; preset-active retry restores the id (`el=0.85&mo=0.6&preset=archipelago`); dispose flushes the pending write, tears the animation down, drained timer inert; panel ports still forward after the rework (elevation input → setElevation, Generate → regenerate). Controller additions: cause correctness on regenerate vs commit (incl. the generate-fallback commit); `applySharedState` single-churn (one generate@512, zero recomputes, ONE state event, archipelago overrides riding with slider-derived freq intact); null/unknown presetId → pure `DEFAULT_FIELD_PARAMS`; clamp/floor sanitization; `hasOverrides` transitions (applyPreset→true, setMoisture→stays, setElevation→false and stays false).
  - `npm run build` → exit 0 after clean `dist/` (strict `tsc --noEmit` + vite; entry bundle now 24.92 kB carrying the boot wiring). Worker chunk **unchanged**: `dist/assets/map.worker-C2FsIWiX.js` 3.50 kB (D5 iife intact).
  - Dev smoke: `npm run dev` → `curl -L http://localhost:5173/biome-generator/` → HTTP 200 with `#map-canvas`/`#controls`; server killed.
  - Built-bundle smoke: `npx vite preview` → entry 200, worker chunk 200, and the served entry JS contains the continent boot wiring; server killed. **Real-browser boot verification (worker + canvas + matchMedia) is deferred to the coordinator's upcoming browser pass** — node-side cannot execute the worker+canvas stack (follow-up logged).
- **Deviations:**
  1. Controller additive changes per brief (cause field, `hasOverrides`, `applySharedState`); `map-controller.ts` now imports `findPreset` from the pure presets registry (no cycle: presets → generation only).
  2. Default boot = continent preset with deterministic seed 0, not a random seed (per brief: first-visit determinism + the bare URL round-trips identically).
  3. Sticky-off preset semantics replace the old 2-dec slider matching in app.ts (the brief's mandated fix for T12's corners (a)+(b)). Residual, cosmetic-only: the PANEL's `aria-pressed` sync still uses its own 2-dec re-marking (panel module off-limits this task), so after preset→drag→drag-back the button can LOOK pressed while the URL correctly carries no preset id — flagged for a future panel touch-up; URL/share fidelity (T14's surface) is unaffected.
  4. Retry restores the full shared state via `applySharedState` (brief-sanctioned improvement over `regenerate(lastSeed)`); also resolves T10's "retry resets sliders" follow-up — sliders/preset are restored WITH the regenerate, never drawn against the empty pre-fields renderer.
  5. `StartAppOptions` injection shape: the bundle is injected as a FACTORY (`createBundle`, called per build/rebuild — static objects cannot survive D5 dispose/recreate semantics) plus a `createAnimation` factory (fake-animation recording), `animationEnv` merge, `loc`, and `writerOptions` (T13's `UrlStateWriterOptions` — covers the now/schedule injection). Exported port types `AppControllerPort`/`AppAnimationPort`/`AppBundle`/`StartAppOptions`; `AppHandle` gains an additive `dispose()` (flush + teardown). Real-wiring `LocationPort` is an adapter over `globalThis.location.hash` + `history.replaceState` (the port bundles both; replaceState-only semantics preserved).
  6. 17 new tests vs "~10–14" estimated (T5/T8/T10/T12 precedent: richer matrix — both retry variants, dispose, port-forwarding regression, and the unregistered-id corner — same scope).
  7. Real-browser boot check deferred to the coordinator's browser pass (see validation evidence); `vite preview` 200 + boot-wiring-in-bundle stands in analytically.
- **Next unblocked task:** T14 (share + export — T13 ✓ + T7 ✓; share can read `getActivePreset()` and the writer's canonical hash) and T15 (QA determinism suite — T2/T3/T13 ✓, now also the boot/restore path). **M3 ("the show": T8 + T9 + T11) is complete.**
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — 180/180 tests + clean-dist build exit 0 (worker chunk `map.worker-C2FsIWiX.js` unchanged, entry 24.92 kB) + dev-smoke 200 re-run independently; independent happy-dom probe with the REAL `MapController` (fake worker port/animation/loc only) 6/6 — bare boot = exactly ONE generate@512 seed 0 with continent falloff 0.08 over center-derived freq/redist 3.5/1.5 + URL `#v=1&seed=0&el=0.5&mo=0.5&preset=continent`; hash 42/0.8/0.3/archipelago restored byte-exact with overrides riding (falloff 0.35, octaves 6, freq 4.1 slider-derived); generate-arrival plays ×1, elevation-commit (cached-recompute AND generate-fallback branches) and cache-miss preview never play, second generate replays with no double-play; sticky id survives moisture tweak, clears on elevation input even AT the exact preset slider value, URL drops `preset=`; retry-rebuild restores full SharedState (sliders + preset overrides) and the rebuilt stack animates its arrival. T12 flag closure: corners (a)+(b) both fixed by the sticky-off rule (grep: `activePresetId` set only on click/boot, nulled only in the hasOverrides===false state branch; 2-dec re-derivation absent from app.ts); the panel's aria-pressed 2-dec re-marking confirmed cosmetic-only — it reads state events and writes the aria attribute, never feeds controller/URL (shared state driven solely by the sticky id; the two can diverge only visually). No Math.random anywhere in the boot path (only the 🎲 click default). Real-browser boot confirmation remains the coordinator's pass (worker-logged follow-up); T10's secondary `client.terminate()`-on-retry hygiene clause still open, non-blocking (old worker never reused, late responses guard-dropped).

---

## T14 — Share + Export · Frontend

- **Status:** executed 2026-08-27 — pending verifier (ultron-supreme) · **Delegation:** worker subagent (T14 brief: share copies the CURRENT URL with clipboard fallback chain; export downloads the canvas as PNG named by seed).
- **Outcome:** Share and Export are live. **Panel** (`src/ui/control-panel.ts`): the two buttons are ENABLED (disabled-placeholder lines removed; `data-action="export|share"` kept) and route clicks to the two new required `ControlPanelPorts` members `onShare()` / `onExport()` — the panel owns NO copy/download logic (the app holds the writer + location). Outcome rendering is on the handle: `flashShareSuccess()` swaps the Share label to `Copied!`, announces via a new visually-hidden `#share-status` `aria-live="polite"` span (inline sr-only styles — no style.css change needed), hides any shown fallback, and reverts after `SHARE_FLASH_MS = 1500` via an injectable `PanelTimer` port (`ports.flashTimer`, default lazily-resolved `setTimeout`/`clearTimeout` so `vi.useFakeTimers` intercepts); a second flash resets the timer; `destroy()` clears a pending flash. `showShareFallbackUrl(url)` reveals `#share-fallback` — a labeled (`Share this link`) read-only text input carrying the URL, pre-`select()`-ed so ⌘C works immediately — as the last-resort manual-copy affordance; `elements.shareFallback` is the new elements-surface member. **App** (`src/app.ts`): `onShare` flushes the coalescing URL writer FIRST (a just-changed slider is up to 200 ms stale in the hash otherwise), builds the absolute URL, then copies with a three-step degrade: `navigator.clipboard.writeText` (feature-detected, failures resolve `false`, never rejects) → temporary off-screen read-only textarea + `document.execCommand('copy')` (feature-detected; happy-dom has no `execCommand`) → `panel.showShareFallbackUrl(url)`; either programmatic success → `panel.flashShareSuccess()`. The default `getShareUrl` = adapter share-base + `#` + `serializeState(lastShared)` — the CANONICAL current state, not a re-read of the hash (injected fake locs don't mirror `replaceState` back into `.hash`, and the state-event push keeps `lastShared` fresh, so the copied URL can never be stale). The app's DEFAULT location adapter (`AppLocationAdapter`, the only extension point — `LocationPort` in urlState.ts is untouched) gains `href` = `origin + pathname + live hash`; `readShareBase` prefers the adapter's `href` minus hash, else `globalThis.location` origin+pathname. `onExport`: `toBlob(canvas)` → null = silent no-op → `createObjectURL` → `<a download="biome-generator-{seed}.png" href>` appended, clicked, removed → `revokeObjectURL` (revocation strictly after the click); seed read from `controller.state.seed` at click time; the async chain is caught-and-logged so no rejection dangles. `StartAppOptions` gains six optional injectables with real defaults — `clipboard`, `execCommand`, `toBlob`, `createObjectURL`, `revokeObjectURL`, `getShareUrl` — with their types exported (`ClipboardWrite` resolves boolean success, `ExecCommandFn`, `CanvasToBlob`, `CreateObjectURLFn`, `RevokeObjectURLFn`, `GetShareUrl`). `src/ui/index.ts` re-exports `PanelTimer` + the flash constants.
- **Files created:** none. Modified: `src/ui/control-panel.ts` (enabled buttons, onShare/onExport/flashTimer ports, flash + fallback surfaces, destroy cleanup), `src/app.ts` (share/export implementations + injectables + href-extended default adapter + docstring), `src/ui/index.ts` (+PanelTimer/SHARE_FLASH_* exports), `src/ui/control-panel.test.ts` (+T14 block, harness share/export counters, placeholder test flipped), `src/app.test.ts` (+T14 blocks, boot() gains an options-merge param + canvas exposure, flushMicrotasks helper, afterEach restores mocks), `src/ui/layout.test.ts` (consequential: makePorts noops for the two new required ports; disabled-placeholder assertions flipped to enabled + fallback-hidden).
- **Validation evidence:**
  - `npm test` → 15 suites, **191/191 passed** (11 new: 5 panel + 6 app; the prior 180 untouched). Panel matrix (happy-dom): click routing (2× share, 1× export → ports called; buttons enabled, data-action kept); flash sets `Copied!` on label AND `#share-status` announce, holds at 1499 ms, reverts at 1500, announce cleared; second flash at t=1000 resets the window (no early revert at t=2000, revert at t=1500-of-flash-2); fallback region hidden at build → shown with labeled read-only input carrying the URL → hidden again by flashShareSuccess; destroy clears the flash timer (label still `Copied!` after 5000 ms). App matrix (happy-dom, REAL panel + injected fakes): share with a pending coalesced write (seed 7, timer NOT fired, `loc.urls` empty) → clipboard fake recorded `urlsAtCall = 1`, i.e. replaceState landed BEFORE the clipboard call, and the copied text is `http://localhost:3000/#v=1&seed=7&el=0.5&mo=0.5&preset=continent` (fresh seed through the default getShareUrl), then `Copied!` + fallback stays hidden; clipboard false + execCommand true → `['copy']` called once, flash shown, fallback hidden; both false → fallback input shows `http://localhost:3000/#v=1&seed=0&el=0.5&mo=0.5&preset=continent` (happy-dom default location base), label stays `Share`; injected `getShareUrl` used verbatim. Export: seed 42 → `toBlob` received the live canvas, order exactly `['toBlob','createObjectURL','click','revoke']`, anchor `download="biome-generator-42.png"` + `href="blob:fake-url"` (anchor-click spy on the prototype, no real navigation); `toBlob → null` → order `['toBlob']`, zero clicks, no `a[download]` in the DOM, no crash.
  - `npm run build` → exit 0 after clean `dist/` (strict `tsc --noEmit` + vite; entry 27.43 kB carrying the share/export wiring). Worker chunk **unchanged**: `dist/assets/map.worker-C2FsIWiX.js` 3.50 kB (D5 iife intact — share/export never enter the worker graph).
  - Manual round-trip acceptance (copied URL reproduces identical map / PNG matches canvas) requires a real browser with a real clipboard — deferred to the coordinator's browser pass, same as T11's logged follow-up; the URL-freshness half of the criterion is covered analytically by the flush-before-copy test and T11's byte-exact restore tests.
- **Deviations:**
  1. Default `getShareUrl` serializes the canonical `lastShared` state instead of re-reading `loc.hash` after the flush: injected fake locations record `replaceState` without updating their own `.hash`, and re-reading would make the copied URL depend on that mirror. Same wire bytes as the flushed hash (both come from `serializeState`), strictly fresher by construction.
  2. `clipboard` injectable is typed `(text: string) => Promise<boolean>` (true = copied) rather than mirroring `navigator.clipboard.writeText`'s `Promise<void>` — lets the default feature-detect + rejection-path resolve `false` instead of throwing into the chain, and keeps the degrade logic branch-free on exceptions.
  3. Export of a null blob is a SILENT no-op (brief allowed "log deviation note"): there is no user-actionable recovery from a failed `toBlob`, an error region would overstate it, and the async chain is still catch-guarded with a `console.error` for real failures.
  4. `layout.test.ts` touched beyond the named file list — consequential only: its `makePorts()` implements the now-two-members-larger `ControlPanelPorts` (noops added) and its placeholder assertions flipped disabled→enabled exactly as the panel test's did. No production layout/style changes; `style.css` untouched (the sr-only announce span and fallback input carry their own inline styles).
  5. `aria-live` announce for the flash lives in a dedicated visually-hidden `#share-status` span rather than reusing the stage label — the stage label is T8's surface and must not be clobbered by a copy confirmation.
  6. 11 new tests vs "~10–14" estimated — on-target.
- **Next unblocked task:** **T15** (QA determinism & regression suite — T2+T3+T13 ✓; T14's flush-before-copy test is a ready-made seed for the URL round-trip section). **T16** (perf & jank — T6+T8 ✓) remains unblocked and independent. Lane QA (T15/T16/T17) is now the frontier; T18 (deploy) unblocked since T1.
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — verified in the post-T3b tree (the dispatch-cap miss from the previous run). Full record:
  - **Scope reconciliation (read first):** the dispatch brief labeled this task "T14: regression suite consolidation"; the authoritative plan/log define **T14 = Share + Export** (the end-of-run task whose 191/191 report was never verified), while regression-suite consolidation is **T15's outcome (status: pending — NOT executed)**. Per protocol the docs' T14 entry is the spec; verified against it. The consolidation-shaped audit the brief asked for was additionally performed against the suite as it stands post-T3b and is recorded below — it does NOT close T15.
  - **Gates re-run:** `npm test` → **197/197** (15 suites; = T14's 191 + T3b's 6, confirming the count arithmetic); `npm run typecheck` → exit 0; `npm run build` after clean `dist/` → exit 0 (worker chunk `map.worker-DbpHG_aw.js` 3.96 kB, first line `(function(){`, zero import/export tokens — D5 iife intact).
  - **T14 spec verification:** source read of `src/app.ts` (share: flush → `getShareUrl` (canonical `lastShared` serialization, never a stale hash re-read) → clipboard → execCommand textarea → `showShareFallbackUrl` selectable fallback → `flashShareSuccess`; export: `toBlob` → object URL → `<a download="biome-generator-{seed}.png">` click → `revokeObjectURL` strictly after the click; null blob = silent no-op) and `src/ui/control-panel.ts` (enabled buttons, `data-action` routing, `#share-status` aria-live flash, labeled read-only fallback input, destroy clears the flash timer) — matches the outcome ("share button copies URL (clipboard fallback: select URL); export downloads PNG via `canvas.toBlob` + download attribute") with the logged deviations 1–6 all previously assessed and none weakened by T3b.
  - **Suite audit (file → coverage):** simplex.test.ts (8: D1 constants/perm/determinism/bounds/continuity/16 golden @1e-10) · fbm.test.ts (9: D1 defaults/memoization/determinism/decorrelation/bounds/zero-mean @512²/octaves/continuity) · biomes.test.ts (31: D3 transcribed boundary matrix, 12-biome grid reachability, bias shifts, WCAG luminance constants) · fields.test.ts (15: determinism 128² + T3b 256², normalization bounds, dual resolution, ocean band, falloff/redistribution monotonicity, decorrelation, perf smoke, T3b pipeline reachability at defaults + presets through the controller merge) · biome-palette.test.ts (12: D3 20-pair ΔL ≥ 0.08 normal + beach-alternate, Machado 2009 deuteranopia with row-sum/white guards, structural CVD, grayscale ordering, beach knob) · handler.test.ts (8: D5 transfer lists = the response buffers, values = direct pipeline calls, 512² smoke, invalid/dispatch-throw error containment) · client.test.ts (7: D5 supersede + stale drop, channel independence, detached-buffer, crash/terminate) · compose.test.ts (13: D2 packRGBA endianness, LUT exhaustive vs classify + boundary-fringe rule, ramp, pixel correctness all 3 modes, chunking equivalence, clamping, <8ms/<40ms budgets) · animation.test.ts (12: T8 full sequence 2800 ms, skip ×3 modes, reduced-motion, play-while-playing, config isolation, unsubscribe, dispose) · map-controller.test.ts (22: T6 hybrid semantics, preview cache, stale drop, cancel hook, presets/overrides, T11 cause + applySharedState + hasOverrides) · urlState.test.ts (9: T13 round-trip, canonical string, malformed→defaults, sanitization, replaceState-only, writer coalescing + flush) · presets.test.ts (6: T12 registry shape/allowlist incl. T3b shoulders/distinctness/findPreset) · control-panel.test.ts (21: T10/T12/T14 wiring, error/retry, drag guard, flash/fallback/destroy) · layout.test.ts (7: index.html skeleton, mounted panel inventory incl. enabled T14 buttons, stylesheet guards + WCAG contrast) · app.test.ts (17: T11 boot/restore/sticky/retry/dispose, T14 share flush-before-copy + fallback chain + getShareUrl, export order/filename/revoke/null-blob). **No `.skip`/`.todo`/`xit`/`.only` anywhere** (the only `.skip(` hits are the animation skip-API under test); no can't-fail assertions; apparent duplication (WCAG helpers re-implemented ×3, reachability at classify- vs pipeline-level, determinism at 4 layers) is deliberate layer-separation, each guarding a distinct seam. T3b's 6 tests live as three clearly-labeled describe blocks inside `fields.test.ts` (the natural home, controller merge mirrored in-test) — not a bolt-on file. Thin DOM seams (`renderer.ts`, `create.ts`, `main.ts`) have no direct unit tests by the committed pure/DOM split (strict tsc + build + app-level fakes + coordinator browser pass stand in).
  - **Mutation probes (3, all bite; restored byte-identical via `cmp`, 197/197 re-confirmed after restore):** (1) worker-client latest-wins supersede disabled in `client.ts` → **2 client tests fail**; (2) `formatSlider` made lossy (1 decimal) in `urlState.ts` → **round-trip + canonical-string tests fail**; (3) `writer.flush()` removed from `onShare` in `app.ts` → **the T14 flush-before-copy test fails**. T3b's reachability tests were NOT re-mutated: the T3b verifier's record (5/6 fail with normalization disabled, byte-identical restore, independent calibration) was read, is detailed and consistent with the assertions in the tree, and is trusted per protocol.
  - **Follow-ups (non-blocking):** (1) **T15 remains pending** — its own deliverables (field-hash determinism test, formal one-octave mutation check, normalization-on 512² hash case) are the next QA task; the suite verified here is consolidated-by-construction (colocated per-module, no orphaned duplicates) but T15 is not thereby executed. (2) Real-browser manual round-trip for share (clipboard copy → fresh-load identical map) and export (PNG matches canvas) still owed to the coordinator's browser pass — T14's own logged deferral, standing alongside T11's. (3) `fields.test.ts` perf smoke asserts <2 s (loose by design, anti-hang; logged ~56 ms vs D2's 300 ms budget) — hard perf evidence stays T16's DevTools recordings. (4) Cosmetic: `presets.ts` header still says "recalibrated 0.35 → 0.65" (code + tests say 0.7) — carried from T3b's verifier. (5) T10's `client.terminate()`-on-retry hygiene clause still open (pre-existing, non-blocking).

---

## T3b — Elevation range normalization + preset recalibration · Frontend

- **Status:** completed — verified + auto-approved (ultron-supreme) 2026-08-27 · **Executed:** 2026-08-27 · **Delegation:** none (production worker in the Ultron-Supreme run, coordinator-briefed).
- **What/why:** The coordinator's browser check measured mountain and snow at 0.00% of pixels across all 3 presets × 8 seeds at 512² (~39M samples/row); tundra/taiga also never appear (archipelago loses forest too). Root cause, reproduced independently here: the elevation pipeline ends at the redistribution pow, and the fBm `(n+1)/2` mapping practically tops out ~0.83 (the theoretical amplitude range is never reached), which any pow ≥ 1.2 compresses further — the max elevation ever produced was **0.859, below the committed snow threshold 0.88**, with the mountain band (0.80–0.88) catching only a rounding-error sliver. T3's original spec said "normalize [0,1]"; the implementation only clamped. Secondary defect: the Highlands preset read as scattered islands (land ~26%, no mountains). The product brief promises mountains on the maps — outcome-affecting.
- **Approach chosen:** Percentile range-normalization of the elevation field **post-shaping** (after falloff + pow), per the preferred option: one histogram pass (4096 bins over the in-memory Float32Array) → `[pLow, pHigh]` = `[Q(0.02), Q(0.9995)]` → in-place remap to [0,1], clamped at the shoulders. Exposed as two new `FieldParams` knobs with defaults (`elevationNormLow: 0.02`, `elevationNormHigh: 0.9995`) so presets can calibrate; `normalizeElevationRange` is exported pure. Why this over alternatives: (a) it makes reachability structural (the top of the field *always* spans into the mountain/snow bands regardless of noise luck) rather than re-tuning magic constants per preset; (b) the very high top shoulder (99.95th) keeps snow *rare by design* — only the extreme tail reaches 0.88+; (c) determinism is untouched — the bounds are a pure function of the generated field (no RNG, no cross-call state; memoized noise factories unchanged); (d) cost is one extra array pass + a 16 KB histogram (~1–2 ms at 512²). The redistribution slider mapping (1.2+0.6v) was **not** retuned — with normalization in place it now shapes band *occupancy* rather than max height, and the calibration targets were met without touching T6's committed mapping.
- **Preset recalibration (necessary, not optional):** normalization changed what the old falloff constants mean. Archipelago falloff **0.35 → 0.7**: at 0.35 the island plateaus stretched up across sea level (~21% land — no longer island-scattered); 0.7 restores ~10.6% land. Highlands gains an `elevationNormHigh: 0.99` override (vs 0.9995 default): its pow-1.77 top tail is thin, so the default shoulder starved mountain/snow; the wider shoulder yields ~2.9% mountain / ~3.8% snow / ~46.5% land. Continent unchanged (falloff 0.08 → one coherent landmass, ocean 58.4%). The preset override allowlist (`PresetOverrideKeys`) was extended minimally with the two normalization shoulders (still excludes the slider-derived `elevationFreq`/`redistribution` and all seed-stability keys).
- **Hard constraints honored:** biome table untouched (sea level 0.40; bands 0.28/0.40/0.46/0.66/0.80/0.88); fBm defaults untouched (5 octaves / gain 0.5 / lacunarity 2.0 / default freq 3.5; moisture 3 octaves); `generateFields(seed, params, resolution)` signature unchanged; no new dependencies; no git commands run.
- **Measurement table** (512², seeds 0–7, controller merge mirrored exactly — `elevationFreq 2.5+2v`, `redistribution 1.2+0.6v`, preset overrides, `moistureBias (m−0.5)·0.8`; per-biome numbers are mean % across seeds):

  | Config | land % (mean) | ocean % (mean) | max elev (mean of per-seed max) | mean elev | mountain % | snow % | tundra % | taiga % |
  |---|---|---|---|---|---|---|---|---|
  | defaults before | 34.28 | 65.72 | 0.7933 | 0.346 | 0.00 | 0.00 | 0.09 | 0.05 |
  | defaults after | 41.13 | 58.87 | **1.0000** | 0.362 | **0.93** | **0.75** | 1.23 | 0.23 |
  | archipelago before | 4.87 | 95.13 | 0.6833 | 0.156 | 0.00 | 0.00 | 0.00 | 0.00 |
  | archipelago after | **10.63** | 89.37 | **1.0000** | 0.131 | **0.15** | **0.31** | 0.26 | 0.05 |
  | continent before | 30.85 | 69.15 | 0.7682 | 0.332 | 0.00 | 0.00 | 0.07 | 0.05 |
  | continent after | 41.59 | 58.41 | **1.0000** | 0.365 | **0.93** | **0.82** | 1.20 | 0.28 |
  | highlands before | 25.85 | 74.15 | 0.7972 | 0.308 | 0.00 | 0.00 | 0.12 | 0.00 |
  | highlands after | **46.50** | 53.50 | **1.0000** | 0.402 | **2.92** | **3.80** | **2.37** | 0.00 |

  Target check: defaults ocean 58.9% ∈ [45,75] (pre-fix 60–70 — close continuity); all 12 biomes ≥ 0.1% sweep-mean at defaults AND continent (smallest: taiga 0.23% / 0.28%; desert 0.96% / 0.99%; rainforest 0.67% / 0.69%); mountain 0.93% > 0.05%; snow 0.75% ∈ (0, 2%). Archipelago land 10.63% (per-seed 9.23–13.67) ∈ [3,15], mountain 0.15% mean ≫ 0.01% cumulative. Highlands land 46.5% ≥ 35, mountain 2.92% (min seed 2.44%) ≥ 2, snow 3.80% ≥ 0.5, tundra 2.37% ≥ 0.5 (taiga is 0 by design — the preset's dry moisture slider −0.12 bias empties the m ≥ 0.75 column; "taiga or tundra" satisfied by tundra). Full per-biome before/after rows preserved in the task report; harness mirrored `map-controller.ts`'s `fieldParams` getter verbatim.
- **Preview-vs-final subtlety (documented behavior):** percentile bounds are data-dependent per resolution, so a 256² drag preview can differ slightly from its 512² commit frame. Measured: land-fraction delta ≤ **0.11 percentage points** at defaults across seeds 0–2 — visually indistinguishable, and the preview was already approximate (256² nearest-neighbor upscale). No contortions added; determinism per (seed, params, resolution) is test-pinned.
- **Files changed:** `src/generation/fields.ts` (+`elevationNormLow`/`elevationNormHigh` FieldParams knobs with defaults, +exported `normalizeElevationRange` with the NORMALIZATION NOTE doc, wired at the end of `generateElevation`), `src/presets/presets.ts` (allowlist + archipelago 0.7 falloff + highlands `elevationNormHigh: 0.99` + intent doc), `src/presets/presets.test.ts` (allowlist keys + percentile sanity ranges), `src/controller/map-controller.test.ts` (1 assertion: registry archipelago falloff 0.35 → 0.7), `src/generation/fields.test.ts` (+6 T3b regression tests). Docs: `docs/ultron/plan.md` (T3b task + index row 4b).
- **Validation evidence:**
  - `npm test` → 15 suites, **197/197 passed** (was 191; +6 new T3b tests: defaults reachability — every biome > 0 in each of 4 seeds (101/202/303/404) + every biome ≥ 0.1% sweep-mean + ocean band + mountain > 0.05% + snow ∈ (0,2%) + max elevation > 0.88 through the pipeline; highlands preset merge — land ≥ 35%, mountain ≥ 2%, snow ≥ 0.5%, tundra ≥ 0.5% per seed; archipelago preset merge — land ∈ [3,15]% per seed + mountains present; normalization determinism at 256² (preview resolution, byte-identical). The pre-existing same-seed determinism test continues to cover 128².
  - `npm run typecheck` → exit 0. `npm run build` → exit 0; worker chunk `map.worker-DbpHG_aw.js` 3.96 kB (was 3.50 kB — the normalization enters the worker graph by design, `generateElevation` lives there), first line `(function(){...`, **zero ESM import/export tokens** (D5 iife invariant intact).
  - Perf: suite smoke `generateFields(512², defaults)` = **58.5 ms** (T3 baseline 59.8 ms, pre-change same-session 66.5 ms; D2 budget < 300 ms) — the normalization's two passes + 16 KB histogram are inside run-to-run noise. 256² preview path 22.2 ms avg (budget < 100 ms).
  - Behavior preservation through normalization: the existing falloff (mean 0.3576 → 0.0967 at faloff 1.0) and redistribution (0.4083 → 0.2986 at 2.0) monotonicity tests still pass; ocean-fraction sanity test now logs 0.5741 at seed 1.
  - Calibration harnesses were throwaway scripts in `/tmp` (never in the repo; deleted after use). Reachability/behavioral coverage that has ongoing value was promoted into `src/generation/fields.test.ts` instead.
- **Deviations:** (1) `FieldParams` gained two knobs (sanctioned by the brief: "FieldParams may gain fields (with defaults)"); they ride the worker protocol as data — stale messages lacking them degrade gracefully to skip-normalization via the NaN guard. (2) Preset override allowlist extended with the normalization shoulders (brief sanctioned "extend the preset override allowlist minimally"); the committed exclusion of slider-derived keys is unchanged and still test-pinned.
- **Risks / follow-ups for the verifier:** (a) the "≥ 0.1% across the sweep" reading — means pass with ≥ 2× margin, but individual seeds can dip below 0.1% for the rarest biome (taiga min 0.04% at defaults); per-seed *presence* (> 0) is asserted for every biome and seed. (b) Highlands vs continent land fractions are ~5pp apart (46.5 vs 41.6) — distinctness rests on composition (2.9% mountain + 3.8% snow + 2.4% tundra + dry climate vs green forested continent) and terrain frequency; visual sign-off belongs to the coordinator's browser pass. (c) T15's field-hash determinism suite should include a normalization-on case (currently covered by the T3b 256² determinism test at 256²; 512² covered by the reachability seeds being stable). (d) Old shared URLs restore the same maps as before only up to the recalibration — a preset-id URL shared pre-T3b now renders the recalibrated preset (intended: the old renders were the defect).
- **Approval:** ✅ verified + auto-approved (ultron-supreme) 2026-08-27 — full independent re-validation, all green:
  - **Gates re-run:** `npm test` → **197/197** (15 suites); `npm run typecheck` → exit 0; `npm run build` after clean `dist/` → exit 0, worker chunk re-emitted `map.worker-DbpHG_aw.js` **3.96 kB**, first line `(function(){`, **zero import/export tokens** (single-file IIFE, D5 intact).
  - **Independent calibration** (verifier's own standalone Node-24 harness in `/tmp`, controller merge + slider mapping `2.5+2v` / `1.2+0.6v` / `moistureBias (m−0.5)·0.8` mirrored verbatim, 512² × seeds 0–7, harness deleted after): matches the worker's table to the decimal — defaults land **41.13** / ocean 58.87 / mountain **0.93** / snow **0.75**; continent 41.59 / 58.41 / 0.93 / 0.82; archipelago land **10.63** (per-seed 9.23–13.67) / mountain 0.15; highlands land **46.50** / mountain **2.92** / snow **3.80** / tundra **2.37**; max elevation 1.0000 everywhere. All 13 acceptance checks PASS (incl. all 12 biomes present in EVERY seed at defaults and continent; archipelago land 3–15% per-seed; highlands land ≥35 / mountain ≥2 / snow ≥0.5 / tundra ≥0.5).
  - **Determinism:** sha256 + raw `Buffer.compare` of elevation AND moisture for 3 configs (defaults / highlands / archipelago) at 512² × two runs each → byte-identical; interleaved foreign generations (different seeds/params/resolutions) between runs → identical; `normalizeElevationRange` purity probe with an interleaved degenerate-shoulder call → identical; zero `Math.random` under `src/generation`, `src/noise`, `src/worker`; the histogram is a fresh per-call allocation (no module state).
  - **Test quality (mutation test):** with the `normalizeElevationRange` call disabled in `generateElevation`, **5 of the 6 new T3b tests fail** (defaults per-seed presence, defaults sweep-mean, max-elevation > 0.88, highlands preset, archipelago preset) — exactly the original defect signature; the determinism test correctly unaffected. `fields.ts` restored byte-identical (`cmp`) and 197/197 re-confirmed after restore.
  - **Perf:** 512² `generateFields` median 72.8 ms in the standalone harness / 55.9 ms in-suite (worker claim 58.5 ms — same ballpark, ≪ 300 ms D2 budget, no regression).
  - **Constraint audit:** `biomes.ts` sea level 0.40 + bands 0.28/0.40/0.46/0.66/0.80/0.88 unchanged; `fbm.ts` DEFAULTS 5 octaves / lacunarity 2.0 / gain 0.5 unchanged; `DEFAULT_FIELD_PARAMS` octaves 5/3, gains 0.5, freqs 3.5/2.5 unchanged (sole addition: the two normalization shoulders); `generateFields(seed, params, resolution)` signature unchanged; `package.json` dependency set unchanged (happy-dom/typescript/vite/vitest).
  - **Risk probes:** (a) taiga per-seed minimum at defaults measured **0.041%** (never zero across seeds 0–7; sweep-mean 0.235% ≥ 0.1% with >2× margin; per-seed presence > 0 is a real, mutation-tested assertion) — acceptable. (b) Highlands-vs-continent distinctness rests on composition, measured: mountain 2.92 vs 0.93, snow 3.80 vs 0.82, desert 4.00 vs 0.99, forest 1.89 vs 3.47, rainforest 0 vs 0.69, plus freq 4.4 vs 3.3 and falloff 0 vs 0.08 — a dry/rocky/snowy vs green/forested character gap that is analytically convincing; final visual confirmation stays with the coordinator's browser pass as planned. (c) Old share URLs rendering recalibrated presets confirmed an intended trade-off, not a fidelity break: same-URL → same-map holds for the CURRENT code (deterministic boot restore through the registry), cross-version stability was never promised, and the pre-T3b renders were the defect itself.
  - **Preview-vs-final:** 256²-vs-512² land delta re-measured 0.018 / 0.110 / 0.057 pp (seeds 0–2, defaults) — matches the ≤ 0.11 pp claim.
  - **Verdict follow-ups (non-blocking):** (1) cosmetic doc typo in `presets.ts` header — "Falloff recalibrated 0.35 → **0.65**" should read 0.35 → **0.7** (code and this log both say 0.7); (2) T15's determinism suite should pin a normalization-on 512² hash case (worker's own flag); (3) coordinator's browser pass to visually sign off highlands-vs-continent character.

## T3c — Snow/mountain ratio + land coherence calibration · Frontend

- **Status:** needs-fix — verified 2026-08-27 (ultron-supreme): every claimed measurement independently reproduced (8-seed sweep, spot values, outside-sweep probes, determinism, byte-identical worker chunk, mutation probes — all match), but 2 defects block auto-approval: a stale inline doc line and a bare-boot product gap; see Verification below · **Executed:** 2026-08-27 · **Delegation:** none (production worker in the Ultron-Supreme run, coordinator-briefed).
- **What/why:** The coordinator's post-T3b visual sign-off confirmed mountains+snow render everywhere, but quantified two taste defects at 512² (flood-fill): (1) snow/mountain ratio INVERTED — highlands snow 3.6–4.1% vs mountain 1.1–1.9% at all seeds; continent seed 0 mountain 0.33% vs snow 0.50%; archipelago mountain 0.00–0.34% vs snow 0.24–0.38%. Snow should CAP mountains, not exceed them. (2) Land coherence too weak — largest 4-connected land component as share of total land: continent 45.2/68.4/58.5/59.3% at seeds 0/42/7/12345 (seed 0 read as "archipelago-ish"), highlands 41.9–66.9%.
- **Mechanism verified (why snow > mountain):** two compounding parts, both measured. (a) The mountain *elevation band* [0.80, 0.88) is NOT itself inverted vs the snow region [0.88, 1.0] at defaults/continent (e.g. defaults seed 0: 0.98% vs 0.41% of pixels) — but the mountain **biome** is only the DRY half of that band (`MOUNTAIN_MOISTURE_SPLIT` 0.5), and the conditional dry share within the band is **0.38–0.44** (high-elevation pixels are conditionally wetter; global P(m<0.5) ≈ 0.49), so mountain_biome ≈ band × 0.40 while snow takes the ENTIRE ≥ 0.88 region regardless of moisture. With band/snow ≈ 2.4, mountain biome ≈ 0.96 × snow — inverted. (b) The 0.9995 top shoulder both clamps a rank slice to exactly 1.0 and leaves the wide [0.88, 1] region (0.12 of span vs the band's 0.08) holding too much of the slowly-decaying normalized tail; at highlands the 0.99 shoulder put a full 1% point mass at 1.0 (27% of its snow).
- **Approach chosen (parameters only, no algorithm changes):** (1) Default `elevationNormHigh` 0.9995 → **0.99999** (near-max stretch parks the extreme tail at the very top of [0,1]; snow collapses while the band keeps its mass). Alone this plateaus at ratio 0.85–0.88 — mountain shrinks as fast as snow. (2) Default `falloffStrength` 0.03 → **0.0**: the falloff's d-gradient spreads the top region's values apart (fattening the normalized snow tail); with the near-max shoulder, falloff 0 lifts defaults/continent to ratio ≥ 1. The old 0.03 was calibrated pre-normalization and is visually near-invisible; island character was already preset-carried. (3) Preset recalibration with the brief-sanctioned `elevationFreq` allowlist extension (feature scale is terrain character; the T6 slider mapping formula is untouched for manual use — overrides apply only while a preset is installed, and `redistribution` stays excluded + test-pinned):
  - **continent**: slider 0.4 → **0.3** (redistribution 1.38 grows the landmass into its saddles), `elevationFreq: 2.72` (bigger features than the slider-derived 3.1 — the coherence lever; at 2.72 the seed-404 twin landmasses merge into one 97%-of-land component), `falloffStrength: 0.08` (drowns rim islands), `elevationNormLow: 0`, `elevationNormHigh: 0.99999`.
  - **highlands**: slider 0.95 → **0.75**, `elevationFreq: 1.5` (the old small-scale 4.4 fragmented land into mesas — coherence 26–67%; at 1.5 the land is one elevated mass, coherence 56.3–99.4%), `falloffStrength: 0.06` (center-pull), `elevationNormHigh: 0.997` (balances mountain ≥ 2% + snow ≥ 0.5% against snow ≤ mountain), `elevationNormLow: 0`, moisture slider 0.35 → **0.15** (deepens the committed "drier climate" so the dry share of the mountain band feeds the mountain biome; with 0.35 snow out-sized mountain at every seed), `moistureOctaves: 4` kept.
  - **archipelago**: `elevationNormHigh: 0.9998` pinned (inheriting 0.99999 starved the islets' rare mountain pixels to exactly 0 at one measured seed; 0.9998 keeps a trace of rock while holding snow ≤ 0.5% and land 3–15% with margin).
- **Hard constraints honored:** biome table + sea level 0.40 + band thresholds untouched; fBm defaults untouched (octaves/gain/lacunarity; elevationFreq 3.5 / moistureFreq 2.5 / redistribution 1.5 defaults unchanged); T6 slider mapping `2.5+2v` / `1.2+0.6v` untouched (presets deviate ONLY through the override mechanism); `generateFields(seed, params, resolution)` signature unchanged; zero extra array passes (values-only change — worker chunk re-emitted byte-identical at 3.96 kB); no new dependencies; no palette changes.
- **Before/after tables** (512², 8 seeds: 0, 42, 7, 12345, 101, 202, 303, 404; controller merge mirrored — `elevationFreq 2.5+2v`, `redistribution 1.2+0.6v`, preset overrides, `moistureBias (m−0.5)·0.8`; per-seed mountain%/snow% and coherence% = largest land component share):

  | Config | seeds failing snow≤mtn | ratio at worst seed (mtn/snow) | coherence min→max | land % range | notes |
  |---|---|---|---|---|---|
  | defaults before | 6/8 | 0.37/0.41 (s0) | 23.3→54.4 (no floor) | 35.2–45.0 | ocean mean 58.6 |
  | defaults after | **0/8** | 0.69/0.62 (s404, 1.11) | 21.2→54.4 (no floor) | 28.6–41.3 | ocean mean 62.4; mtn min 0.16% > 0.05; snow mean 0.34 ∈ (0,2); 12 biomes ≥ 0.23% mean, presence > 0 all seeds (taiga min 10 px @ s42) |
  | continent before | 5/8 | 0.33/0.50 (s0) | 37.4→85.5 | 34.7–49.1 | ocean mean 57.4 |
  | continent after | **0/8** | 0.42/0.41 (s0, 1.02) | **55.8→99.8** | 43.7–63.3 | ocean mean 46.1 ≥ 45 floor; 12 biomes ≥ 0.44% mean, presence all seeds |
  | highlands before | 8/8 | 2.44/3.73 (s0) | 26.3→66.9 | 43.9–48.1 | mtn 2.0–2.9, snow 3.4–4.1 |
  | highlands after | **0/8** | 2.09/1.91 (s7, 1.09) | **56.3→99.4** | 38.7–57.9 | mtn min 2.09 ≥ 2; snow min 1.54 ≥ 0.5; tundra min 3.98 ≥ 0.5; land min 38.7 ≥ 35 |
  | archipelago before | 7/8 (no ratio req) | — | 45.9→91.3 (no floor) | 9.4–14.6 | snow max 0.45 (≤ 0.5, thin) |
  | archipelago after | n/a | snow max 0.24 ≤ 0.5 all seeds + extras (s5 0.23, s6 0.14, s8 0.40) | 47.9→81.7 (no floor) | 8.1–13.6 | land ∈ [3,15] every seed; max land 13.6 < 20 |

  Extra-seed robustness (not required by the sweep): continent s5 coherence 41.5% (below 55 — one landmass + one big satellite at that draw), s6 73.5, s8 93.5; highlands s5–s123456 all pass (mtn ≥ 1.91 @ s123456, slightly under the 2% bar used for the sweep seeds; s5/s6/s8 mountain 2.24–4.43, coherence 61.9–97.0). Defaults extra seeds s5/s6/s8: ratio passes at all.
- **Coherence metric resolution-stability (test precondition):** continent/highlands at seeds 0/42/7/12345 measured at 128²/256²/512² — verdicts identical at all three resolutions, deltas ≤ 0.4pp (continent min 56.2/56.0/55.8; highlands 76.2/75.9/75.7). The regression test therefore floods at 256².
- **Visual sanity (before/after PNGs from the real pipeline, classified with the committed palette, vision-analyzed):** defaults after s0 — "white snow areas read as small caps sitting on gray mountain cores … no obviously inverted proportion". Highlands before s303 — "white snow appears as large blobs wider than or comparable to their gray cores — inverted"; after s303 — "one dominant landmass … snow caps … arid but mountainous highlands". Continent after s101 (coh 99.8) — "one clearly dominant landmass with coastal water around it … gray cores with small white snow caps". Continent after s0 (the 55.8% floor seed) — caps fixed; dominance improved (4–6 connected masses vs 8–10 before) but still the weakest visual read; ASCII land-mask ground truth confirms the masses ARE 4-connected through isthmuses (the flood-fill metric is honest, the "read" is a bay-cut continent). Archipelago after — "clearly reads as a scattered archipelago" with 2 residual islets showing snow wider than a nearly-absent gray core (measured snow ≤ 0.24% — within the brief's "mountains may be tiny" allowance for scattered islets).
- **Files changed:** `src/generation/fields.ts` (DEFAULT_FIELD_PARAMS: falloffStrength 0.03 → 0.0, elevationNormHigh 0.9995 → 0.99999; CALIBRATION/NORMALIZATION notes updated — no code-path changes), `src/presets/presets.ts` (allowlist + `elevationFreq`; all three presets recalibrated as above; intent doc rewritten), `src/presets/presets.test.ts` (allowlist keys + elevationFreq sanity + redistribution-only exclusion), `src/controller/map-controller.test.ts` (+1 assertion: archipelago `elevationNormHigh` 0.9998 rides `applySharedState`), `src/app.test.ts` (sticky-preset URL/drag-back follows continent's new 0.3 slider), `src/generation/fields.test.ts` (+5 T3c regression tests). Docs: `docs/ultron/plan.md` (T3c task + index row 4c).
- **Validation evidence:** `npm test` → 15 suites, **202/202** (was 197; +5 T3c tests: per-seed snow ≤ mountain for defaults (4 seeds) + continent & highlands registry merges (4 seeds each) + archipelago snow ≤ 0.5% (4 seeds); per-seed coherence ≥ 55% at 256² for continent + highlands (4 seeds each, resolution-stability-documented)). `npm run typecheck` → exit 0. `npm run build` → exit 0; worker chunk `map.worker-DbpHG_aw.js` 3.96 kB **byte-identical name/hash to T3b** (values-only change never enters the worker graph). Determinism re-probed: sha256 of elevation+moisture for defaults@512/continent@512/highlands@256 identical across two runs with interleaved foreign generations (different seeds/params/resolutions). Suite duration 3.86s (was 2.95s; the 5 pipeline tests add ~0.9s). Calibration harnesses were throwaway scripts in `/tmp` (deleted after use).
- **Deviations:** (1) `elevationFreq` joined the preset override allowlist — explicitly sanctioned by the brief ("allowlist extension has T3b precedent — extending it further is allowed if needed"); `redistribution` remains excluded and test-pinned. (2) Preset slider positions moved (continent 0.4→0.3, highlands 0.95→0.75, highlands moisture 0.35→0.15) — the brief lists "elevation slider position" as a lever and preset data is T12/T3c-owned; the highlands moisture move is the same "drier climate" lever deepened to fix the ratio (mountain biome = dry half of the band). (3) Default falloffStrength 0.03 → 0.0 — a parameter change on the tunable the CALIBRATION NOTE explicitly reserves for tuning; pre-normalization rationale was obsolete.
- **Risks / follow-ups for the verifier:** (a) Margins are honest but thin at three pinned points: continent seed 0 ratio 1.02 (0.42/0.41) and coherence 55.8 (floor 55); defaults seed 404 ratio 1.11; highlands seed 7 mountain 2.09 (floor 2.0). Any future param nudge re-rolls these dice — the regression tests sit exactly on the tightest seeds by design. (b) Continent's coherence fix is a specific freq-lottery pocket (2.72; 2.71–2.74 all pass at falloff 0.08, robust ±0.02, but 2.70 and below re-splits seed 404) — the value is deterministic and pinned by data + tests. (c) Outside the sweep, continent seed 5 measures 41.5% coherence (fails 55) and highlands seed 123456 mountain 1.91% (under the 2% bar) — the brief's requirement is the ≥8-seed sweep (0,42,7,12345 + 4 more: 101,202,303,404), which passes everywhere; noted so nobody mistakes the sweep for a universal quantifier. (d) Highlands' visual character shifted: bigger features + drier climate (desert 14.9% mean) + tundra 6.3% — vision reads it as arid-but-mountainous with a single dominant mass; a wetter look would re-open the ratio/mountain-floor squeeze. (e) The conditional band dry-share (0.38–0.44 vs global ~0.49) is measured structure between the two independently seeded fBm fields in the upper tail — global |Pearson| stays < 0.3 (test unchanged, passing); root cause is documented as measured, not derived. (f) Archipelago islets can still show cap-without-core (snow present where the mountain band is 0–1 px) — explicitly allowed by the brief.
- **Verification (ultron-supreme, 2026-08-27) — VERDICT: NEEDS-FIX (no auto-approve).** All claimed measurements were independently reproduced with the verifier's own Node-24 harness (throwaway scripts in `/tmp`, deleted after); the sweep, determinism, chunk and mutation claims are all TRUE. Two defects block approval — a one-line stale doc comment and a boot-path product gap discovered by adversarial probing (both listed at the end, with a ready fix path).
  - **Gates re-run:** `npm test` → **202/202** (15 suites); `npm run typecheck` → exit 0; `npm run build` after clean `dist/` → exit 0.
  - **Independent 8-seed sweep** (512², seeds 0/42/7/12345/101/202/303/404; controller merge mirrored verbatim from `map-controller.ts` — `elevationFreq 2.5+2v`, `redistribution 1.2+0.6v`, overrides layered last, `moistureBias (m−0.5)·0.8`; verifier's own flood fill). Matches the worker's table to the decimal:

    | config | ratio fails | tightest mtn/snow | coh min→max % | land % range | floors |
    |---|---|---|---|---|---|
    | defaults | 0/8 | 0.69/0.62 @ s404 (1.11 — exact) | 21.2→54.4 | 28.6–41.3 | ocean mean 62.4 ∈ [45,75]; mtn min 0.16 > 0.05; snow mean 0.34 ∈ (0,2); 12 biomes > 0 in EVERY seed |
    | continent | 0/8 | s0 0.42/0.41 (ratio 1.03; claim "1.02" = rounding of the same data) | **55.8**→99.8 (min exactly @ s0) | 43.7–63.3 | ocean mean 46.1 ≥ 45; 12 biomes in every seed |
    | highlands | 0/8 | s7 2.09/1.91 (1.10) | **56.3**→99.4 | 38.7–57.9 | land min 38.7 ≥ 35; mtn min 2.09 ≥ 2; snow min 1.54 ≥ 0.5; tundra min 3.98 ≥ 0.5 |
    | archipelago | n/a (snow-cap rule) | snow max **0.25** ≤ 0.5 (claim 0.24 — display rounding of s303; cap holds 2×) | 47.9→81.7 | 8.1–13.6 ∈ [3,15] | mountains present in the sweep (s0 exactly 0, 7/8 other seeds > 0 — T3b's "present somewhere" criterion) |
  - **Outside-sweep honesty probes:** continent s5 coherence **41.5** (claim 41.5), s6 **73.5**, s8 **93.5** — claims exact; failure mode is one-landmass-plus-big-satellite coherence, NOT ratio inversion (s5 mtn 0.58 vs snow 0.16, ratio intact) → judged documented calibration variance, acceptable under the sweep-scoped acceptance. Highlands s123456 mtn **1.91** (claim 1.91; snow 1.23, ratio intact; land 41.8 ≥ 35). Archipelago s5/s6/s8 snow **0.23/0.14/0.40** (claims exact). Defaults s5/s6/s8 ratio passes. No swept or probed config regresses to ratio inversion.
  - **Coherence resolution-stability (test precondition):** continent/highlands × seeds 0/42/7/12345 at 128²/256²/512² → identical pass verdicts at all three; mins continent **56.2/56.0/55.8**, highlands **76.2/75.9/75.7** — exactly the claimed values (adjacent-resolution deltas ≤ 0.3pp; full min spread 0.32/0.49pp).
  - **Determinism:** sha256 of elevation AND moisture for defaults@512 s0 / continent@512 s42 / highlands@256 s7, two runs with interleaved foreign generations (different seeds/params/resolutions) → **byte-identical**. Zero `Math.random` under `src/generation`, `src/noise`, `src/worker`.
  - **Worker chunk:** re-emitted `map.worker-DbpHG_aw.js` **3.96 kB, same content hash as T3b — the "byte-identical" claim is TRUE and mechanically explained**: `DEFAULT_FIELD_PARAMS` is tree-shaken out of the worker graph (the changed literals `0.99999`/`0.0` appear nowhere in the chunk; only the `params.falloffStrength` property read rides it). First line `(function(){`, **zero import/export tokens** (D5 IIFE invariant intact).
  - **Mutation probes (both bite; restored byte-identical via `cmp` + sha256; 202/202 re-confirmed after restore):** (a) default `elevationNormHigh` reverted to 0.9995 → **"defaults: snow ≤ mountain at every seed" FAILS** (continent/highlands correctly unaffected — they pin explicit overrides). (b) continent `elevationFreq: 2.72` override removed (fallback to slider-derived 3.1) → suite FAILS via the **ratio** test — but NOT via the coherence test: at freq 3.1 the test's seeds (0/42/7/12345) still measure 63.7–94.8; the only coherence breach is **s404 = 47.8%**, which is not in the coherence test's seed list → follow-up F1.
  - **Constraint audit:** biome table + sea level 0.40 + bands 0.28/0.40/0.46/0.66/0.80/0.88 + `MOUNTAIN_MOISTURE_SPLIT` 0.5 untouched; fBm DEFAULTS (5 oct / lacunarity 2.0 / gain 0.5) untouched; `DEFAULT_FIELD_PARAMS` octaves 5/3, gains 0.5, freqs 3.5/2.5, redistribution 1.5, seedOffset 7919 unchanged (sole deltas: falloffStrength 0.03→0.0, elevationNormHigh 0.9995→0.99999 — exactly as claimed); T6 slider formulas `2.5+2v` / `1.2+0.6v` verbatim in the default path; `generateFields(seed, params, resolution)` signature unchanged; devDependencies unchanged (happy-dom/typescript/vite/vitest); palette hexes untouched; `presets.ts` intent doc matches code and measurements (and T14-era "0.65" typo is gone); the CALIBRATION NOTE block is correctly rewritten for 0.0.
  - **DEFECTS (needs-fix list):**
    1. **Stale inline doc** — `src/generation/fields.ts:16` still reads `falloffStrength: number; // default 0.03` while the actual default is **0.0** (line 42). The CALIBRATION NOTE block below it IS correctly updated; this one-line field comment was missed and directly contradicts it. One-line fix. (Pre-declared defect pattern in the verification brief: "a stale note claiming 0.03 is current = defect".)
    2. **Bare-boot hybrid config still shows the commissioned defect at seed 0** — T11's bare-URL boot applies `DEFAULT_SHARED_STATE` sliders (0.5/0.5) + the continent preset id, so the app's FIRST-LOAD map runs redistribution **1.5** (slider-derived) instead of the calibrated **1.38** (preset slider 0.3; `redistribution` is deliberately excluded from the override allowlist). Measured at 512² across the 8 sweep seeds: **seed 0 — the fixed boot seed — snow 0.35% > mountain 0.32%** (marginal inversion, ~800 px; the inversion class that triggered T3c, though at near-tie magnitude rather than the original 4–10×), and s404 coherence 52.6 < 55 (all other seeds pass both checks; boot seed 0 coherence 56.3 passes). T3c's swept configs are honest and fully fixed, but the product's most-viewed map is a fifth, uncalibrated configuration. Fix options (coordinator's call): (i) boot the preset's own slider positions — derive the bare-URL boot state from `findPreset(DEFAULT_BOOT_PRESET)` sliders (small `app.ts` change, T11-owner review) so first load ≡ preset click; or (ii) explicitly accept the hybrid and pin the boot config's actual numbers (s0 mtn 0.32/snow 0.35, coh 56.3, land 39.4) as documented behavior with a regression test on the boot config.
  - **Follow-ups (non-blocking):** F1 — add seed 404 to the T3c coherence test's seed list (`fields.test.ts`): the `elevationFreq: 2.72` override's coherence payoff lives at s404 (47.8% without it) and is currently unguarded by its named test (the mutation is only caught incidentally by the ratio test). F2 — T15 determinism suite should pin a normalization-on 512² hash case (carried from T3b). F3 — coordinator real-browser boot/visual check still open (carried from T11/T14); that pass should include the bare-URL first-load map given defect 2.

## T3c-fix — Bare-boot calibration + stale comment + coherence-seed guard · Frontend

- **Status:** completed — re-verified + auto-approved (ultron-supreme) 2026-08-27 (addresses the T3c verification's 2 defects + follow-up F1; see re-Verification below) · **Executed:** 2026-08-27 · **Delegation:** none.
- **What/why:** The T3c verifier reproduced every calibration claim but found the app's most-viewed map — the bare-URL first load — was still an uncalibrated FIFTH config: T11's boot applied `DEFAULT_SHARED_STATE`'s centered 0.5/0.5 sliders and only layered the continent preset's field-param overrides on top, so the boot map ran slider-derived redistribution **1.5** instead of the preset's calibrated **1.38** (slider 0.3; `redistribution` is deliberately excluded from the override allowlist). At the fixed boot seed 0 that hybrid measured snow 0.35% > mountain 0.32% — exactly the marginal-inversion defect class T3c was commissioned to kill — and at s404 its coherence dipped to 52.6% < 55%. Second defect: `fields.ts:16`'s inline field comment still said `// default 0.03` for `falloffStrength` after T3c moved the default to 0.0 (the CALIBRATION NOTE block was updated; this one line was missed). Follow-up F1: the coherence regression test's seed list lacked seed 404 — the only seed where removing the continent `elevationFreq: 2.72` override breaches the 55% floor (47.8% without it), so that override was unguarded by its named test.
- **Boot-path change (the fix, option (i) of the verifier's list):** new exported `bareBootState()` in `src/app.ts` — a bare URL now boots the boot preset's OWN snapshot: `findPreset(DEFAULT_BOOT_PRESET)` supplies the slider positions (continent 0.3/0.5) and the id, seed stays the deterministic 0. `startApp`'s boot line becomes `readStateFromLocation(loc) ?? bareBootState()`. A bare first load is now bit-for-bit the state a Continent preset click produces (one `applySharedState`, one generate), and the URL write-back pins it as `#v=1&seed=0&el=0.3&mo=0.5&preset=continent`. Semantics preserved: stateful URLs still win verbatim (explicit `el`/`mo`/`seed`/`preset` are applied as the sharer left them — the restore path is untouched); same-URL → same-map fidelity holds (the written hash parses back to the boot state); the sticky preset id survives boot because `applySharedState` INSTALLS the preset's overrides (booting at the preset's own positions is not a manual terrain input — only an elevation drag/commit clears overrides, `hasOverrides === false` remains the sole off-switch); the reduced-motion path is untouched (animation routing keyed on fields-cause, unchanged). Graceful fallback: if the boot preset id ever left the registry, `bareBootState()` returns the centered defaults with a null preset (unreachable today — `DEFAULT_BOOT_PRESET` is a const and the registry is static).
- **Defect 1:** `src/generation/fields.ts:16` comment corrected to `// default 0.0 (see CALIBRATION note below)` — comment only, the CALIBRATION NOTE and the actual default were already correct.
- **F1:** coherence regression test seeds `[0, 42, 7, 12345]` → `[0, 42, 7, 12345, 404]` (keeps 0 and 404; all five kept — the flood-fill runs at 256², the added seed costs ~2×25 ms per preset). Verified the guard bites via a throwaway probe: continent s404 coherence **47.8%** with the slider-derived freq 3.1 (override removed) vs **97.2%** with the 2.72 override — the seed list now fails loudly on exactly that mutation.
- **Measured evidence (throwaway vitest probe at the controller merge, deleted after use):** NEW boot config (continent @ s0, 512²): snow **0.406%** ≤ mountain **0.420%** (ratio 0.97), coherence **55.8%** ≥ 55 — both acceptance invariants hold, and match the T3c sweep's continent s0 row (mtn/snow 1.03, coh 55.8) since boot IS that config now. OLD hybrid boot (continent overrides over el=0.5) @ s0 for contrast: snow 0.351% > mountain 0.322% (the verifier's inversion, reproduced to the decimal) — fixed by construction. Full new coherence seed table at 256²: continent 56.0/80.2/94.3/69.4/**97.2** (s0/s42/s7/s12345/s404), highlands 83.2/89.0/75.9/99.5/**56.4**.
- **Files changed:** `src/app.ts` (+`bareBootState()` export, boot line + file-header/`DEFAULT_BOOT_PRESET` docs updated — the only behavior change in the run), `src/generation/fields.ts` (one comment character: 0.03 → 0.0), `src/app.test.ts` (boot test (a) now asserts the preset's own positions + the `el=0.3` URL; NEW (a2): `bareBootState()` = continent snapshot, merged boot FieldParams = the calibrated continent merge (redistribution 1.38 / falloff 0.08 / freq 2.72), written hash round-trips to the boot state, sticky id survives boot with `hasOverrides` true; the 5 URL-expectation sites that carried the old 0.5 boot elevation updated), `src/generation/fields.test.ts` (F1 seed 404 + NEW "T3c-fix boot config" describe: boot map at 512² satisfies snow ≤ mountain and coherence ≥ 55%, reusing the sweep helpers — one extra 512² generate, no sweep duplication). Docs: this entry + `docs/ultron/plan.md` (T3c status + index row 4c).
- **Validation evidence:** `npm test` → 15 suites, **204/204** (was 202; +2: app boot (a2), boot-config invariants in fields.test.ts; suite 5.7→~7–10 s, dominated by the pre-existing 512² sweeps). `npm run typecheck` → exit 0. `npm run build` → exit 0. All calibrated T3c parameter values untouched (fields.ts defaults + presets.ts values byte-identical); determinism paths untouched (boot change is pure state selection on the main thread; the worker graph is unchanged — `DEFAULT_FIELD_PARAMS` still never enters the worker chunk).
- **Risks / notes for the verifier:** (a) First-load URLs now serialize `el=0.3` instead of `el=0.5` — any external bookmark/fixture expecting the old bare-boot hash `#v=1&seed=0&el=0.5&mo=0.5&preset=continent` will still load fine (explicit URL values win) but is no longer what a bare boot emits. (b) The sticky-id drag-back corner is unchanged and still test-pinned: after booting at the preset's positions, a manual elevation drag clears overrides and dragging BACK to 0.3 does not re-mark the id. (c) The boot map's margins are the continent sweep's margins — tightest at its own floor seed (coherence 55.8 vs floor 55, ratio 0.97); any future param nudge re-rolls both, and the new boot-config test sits exactly on them by design. (d) F2 (T15 determinism 512² hash case) and F3 (coordinator real-browser boot/visual pass, now expecting the calibrated first-load map) remain open and unaffected.
- **Re-Verification (ultron-supreme, 2026-08-27) — VERDICT: PASS → T3c auto-approved.** Both needs-fix defects and follow-up F1 independently confirmed resolved in the tree; the T3c calibration itself is bit-for-bit untouched. Evidence (verifier's own Node-24 harness in `/tmp`, deleted after; sources restored/pristine via sha256 after mutation probes):
  - **Defect 1 (stale comment) resolved:** `src/generation/fields.ts:16` now reads `// default 0.0 (see CALIBRATION note below)`. Proof of "comment-only": reconstructing the pre-fix file from the current one by reverting exactly line 16 yields sha256 `6d9120f7…` — the verifier's recorded pre-fix hash — so line 16 is the sole delta. `src/presets/presets.ts` is **byte-identical** to the pre-fix hash `0dfd52c3…` (calibration values untouched); defaults at lines 42/49 still `falloffStrength: 0.0` / `elevationNormHigh: 0.99999`.
  - **Defect 2 (bare-boot hybrid) resolved:** `bareBootState()` in `src/app.ts` returns the boot preset's OWN snapshot (`{v:1, seed:0, elevation:0.3, moisture:0.5, preset:'continent'}` via `findPreset(DEFAULT_BOOT_PRESET)`), and the boot line is `readStateFromLocation(loc) ?? bareBootState()` — stateful URLs still win verbatim. **Verifier's own re-measurement of the NEW boot config** (bareBootState merged exactly as `MapController.fieldParams` does → freq 2.72 / redist 1.38 / falloff 0.08): 512² seed 0 → **mountain 0.420% ≥ snow 0.406%**, coherence **55.8%** (fix worker's numbers exact, and identical to the T3c sweep's continent-s0 row — boot IS that config now); s404 coherence 97.2%. The hybrid (redistribution 1.5) no longer exists on any default path: bare boot = preset click; the retry fallback (`lastShared ?? {...DEFAULT_SHARED_STATE}`) carries preset null → pure defaults; manual elevation input clears overrides. Residual, documented behavior (not a defect): a stateful URL explicitly carrying `el=0.5&…&preset=continent` (minted by pre-fix bare boots) still restores the hybrid faithfully — committed same-URL→same-map fidelity, the same cross-version class T3b's verifier already ruled intended.
  - **Preserved semantics (code + tests read, suite green):** app.test.ts (a)/(a2)/(b)/(b2) pin — bare boot emits exactly one `applySharedState` with the preset's own sliders + `#v=1&seed=0&el=0.3&mo=0.5&preset=continent`; the written hash round-trips (`parseHashBody` ≡ `bareBootState()` → same-URL→same-map); the sticky id survives boot (`hasOverrides` true; only an elevation drag/commit clears); stateful restore verbatim; unregistered preset id → null id, no overrides. No stale `el=0.5&mo=0.5&preset=continent` expectations remain.
  - **F1 resolved and re-probed:** coherence test seeds now `[0, 42, 7, 12345, 404]` (256²). Mutation re-probe (continent `elevationFreq: 2.72` override removed → slider-derived 3.1): the **coherence test now FAILS directly** (s404 collapses to 47.8%), alongside the ratio test and the new boot-config test — 3 tests bite; restored byte-identical (`cmp` + sha256), suite re-confirmed green. Spot-checks of the fix worker's new-seed table: continent s404 @256 = **97.2**, highlands s404 @256 = **56.4** — both exact.
  - **Gates re-run:** `npm test` → **204/204** (15 suites; +2 = app (a2) + boot-config invariants); `npm run typecheck` → exit 0; `npm run build` after clean `dist/` → exit 0, worker chunk still `map.worker-DbpHG_aw.js` 3.96 kB **byte-identical** (boot change is main-thread state selection; the worker graph is untouched).
  - **52.6/56.3 nuance adjudicated (no disagreement):** the fix worker's hybrid figures — s0 mtn 0.322 / snow 0.351 / coherence 56.3 — reproduce the verifier's original s0 row exactly (0.32/0.35/56.3); the verifier's 52.6% was explicitly the **s404** hybrid coherence, re-measured now at 52.6 again. Their "could not reproduce" was a seed mix-up on their side (they measured s0 and read 52.6 as an s0 claim); both harnesses agree per-seed to the decimal. The hybrid is gone either way.
  - **Follow-ups remaining (non-blocking, carried):** F2 — T15 determinism suite to pin a normalization-on 512² hash case; F3 — coordinator real-browser boot/visual pass, now expecting the calibrated first-load map (`el=0.3` hash). Old external bookmarks of the pre-fix boot hash still load (explicit URL values win) — documented in the T3c-fix entry's risks.

## T15 — Determinism & regression suite consolidation · QA

- **Status:** completed — executed 2026-08-27 · **Delegation:** none (production worker in the Ultron-Supreme run, coordinator-briefed).
- **What/why:** Plan lane E names five suite legs — noise golden values + statistics, field-hash determinism, URL round-trip, classifier boundary matrix, palette ΔL adjacency — under acceptance "suite green; deliberate mutation (one octave changed) fails the hash test." Audit of the tree at T15 start: four legs already existed and were verified in place (simplex.test.ts: 16 golden values @1e-10 + perm-table/determinism/bounds/continuity; fbm.test.ts: 512² zero-mean statistics + bounds/determinism/octave-composition/continuity; biomes.test.ts: D3 transcribed elevation × moisture boundary matrix + bias shifts; biome-palette.test.ts: 20-pair ΔL ≥ 0.08 normal + deuteranopia; urlState.test.ts: T13 round-trip/canonical/malformed/sanitize). The **field-hash leg was the real gap**: the pre-T15 determinism coverage was same-inputs-twice *property* tests at 128² and 256² only (T3-era + T3b's), pinning no values at all — a silent value drift (an octave changed, a reordered arithmetic op, an engine-sensitive rewrite) could pass every test in the file as long as run-to-run reproducibility held and the behavioral bands stayed wide. This is also exactly follow-up **F2**, carried by the T3b and T3c verifiers ("pin a normalization-on 512² hash case") and re-flagged by T14's.
- **Deliverable (all in `src/generation/fields.test.ts`, the suite's pipeline home):** a `T15 field-hash determinism (seed × params × resolution grid)` describe with a byte-level digest helper — two independent 32-bit lanes (FNV-1a + murmur3-style mixer) folded over the raw Float32 bytes of each field, elevation and moisture hashed separately (`e:… m:…`) so a failure names the drifted field — and a pinned grid of four cases (the deliberate budget, each earning its 512²/256² generate):
  1. **defaults @ 512², seed 404** — the normalization-on final-resolution case (F2); 404 is the defaults sweep's ratio-tightest seed. Digest `e:0b4517dcec50057f m:c9071afb9ed63a16`.
  2. **highlands preset merge @ 512², seed 7** — a preset config through the controller merge (freq 1.5 / falloff 0.06 / normHigh 0.997 / moistureOctaves 4); 7 is highlands' ratio-tightest seed. `e:783fc69e15645ad6 m:66e1b07db81a9ccf`.
  3. **bare-boot continent merge @ 512², seed 0** — the first-load map's exact bytes (the config T3c-fix's boot test pins behaviorally). `e:0e27b278c5e9c812 m:f70138fe372f43c4`.
  4. **defaults @ 256², seed 9** — the preview resolution; *the identical configuration* the removed T3b 256² property test used. `e:d2e64633e148ac3a m:6a17f983e96c9842`.
  Portability note (in-code): the pipeline's arithmetic is IEEE-754-exact only (`Math.imul`/`floor`/`sqrt`, no transcendentals), so the byte digests are engine-stable; little-endian assumption matches every engine the suite targets.
- **Interleaved-generation isolation** (second new test): config A (defaults, 256², seed 9) generated, then two maximally foreign generations between the runs (bare-boot continent @ 512² seed 0 AND highlands merge @ 512² seed 7 — different seed, params, and resolution), then A again → element-wise identical AND equal to the pinned digest. This guards the property the verifiers probed manually in T3b/T3c ("interleaved foreign generations → byte-identical"): no shared mutable state may leak across generations (noise factories, permutation tables, normalization histogram are all per-call; `fbm2D`'s memo cache is keyed by the full options set and unused by `generateFields`, which calls `createFbm2D` directly).
- **Consolidation — every test added/removed/moved (file: fields.test.ts; suite total unchanged at 204):**
  - ADDED `reproduces the pinned byte-level digests (elevation AND moisture) for every grid case` (the 4-case table above; failure message names the case).
  - ADDED `same inputs reproduce element-wise, with foreign generations interleaved between the runs`.
  - REMOVED `same seed twice produces element-wise identical elevation and moisture` (T3-era, 128²) — superseded: run-to-run equality is now asserted element-wise AND against recorded constants at 256²/512² (interleaved test); 128² same-config equality remains covered transitively by handler.test.ts's worker-path-vs-direct comparison at 128².
  - REMOVED `normalization is a pure function of the field: same inputs → identical arrays at 256²` (T3b) — superseded by pin #4, which pins the *same configuration* (defaults, seed 9, 256²) to a recorded digest: strictly stronger (reproducibility now also fails on cross-version value drift, not just intra-run nondeterminism). A tombstone comment at the old site points to the T15 block.
  - MOVED `different seed changes both fields (at least 10% of elements differ)` from the deleted T3-era `generateFields determinism` describe into the T15 describe — unchanged, one word of scope widened (asserts divergence for both fields, so the hash grid cannot silently degenerate to seed-independence).
  - NOT touched: every behavioral test (T3b reachability, T3c ratio/coherence, T3c-fix boot) — all keep their exact seeds and thresholds, including the load-bearing seed 404 in both T3c sweeps (F1's guard, verified present at `fields.test.ts` coherence/ratio SEEDS lists).
  - NOT deduped across files (deliberate): the T14 verifier's audit stands — repeated WCAG helpers, classify-level vs pipeline-level reachability, and the 4-layer determinism ladder (noise → fbm → fields → worker) are intentional layer separation, each guarding a distinct seam; the plan's T15 scope names no cross-file consolidation.
- **Mutation check (the acceptance validation; performed, not codified — a mutation test needs the mutation applied by hand):** `DEFAULT_FIELD_PARAMS.elevationOctaves` 5 → 4 in `fields.ts`, full `npm test` → **5 failures**: the pinned-digest test fails first on byte drift (`defaults @ 512², seed 404 digest changed: expected 'e:81c675e38d01a2d3 m:c9071afb9ed63a16' to be 'e:0b4517dcec50057f m:c9071afb9ed63a16'` — note the moisture digest unchanged, correctly isolating the drift to the elevation field), plus the interleaved test, T3c ratio (defaults + continent/highlands), and the T3c-fix boot test. Restored by exact inverse edit; restoration proven byte-identical at the artifact level — the post-restore `npm run build` re-emits `index-B0NE8tHL.js` / `map.worker-DbpHG_aw.js` with the **same content hashes as the pre-T15 baseline build**; 204/204 re-confirmed.
- **Gates:** `npm test` → 15 suites, **204/204 before and after** (2 removed, 2 added, 1 moved — net 0); duration same ballpark: baseline 5.9 s, post-T15 runs 7.5–9.7 s over three runs on a machine with visible load variance (the pin test itself logs ~380 ms; interleaving adds two 512² + two 256² generates ≈ 0.5 s of test time) — single-digit seconds, within the T3c-fix entry's documented 7–10 s band. `npm run typecheck` → exit 0 (after fixing the one error this task introduced and caught via its own gate: the new `FieldParams` type import was initially missing — vitest doesn't typecheck, `tsc` did). `npm run build` → exit 0, worker chunk 3.96 kB iife, D5 invariant intact.
- **Constraint compliance:** tests/docs only — zero production-code changes (the `fields.ts` octave edit was the mutation check, restored byte-identically; build hashes prove it); no calibration value touched (T3b/T3c values byte-preserved, verified by the pinned digests themselves); no assertion weakened (both removals replaced by strictly stronger coverage); no new dependencies; no git commands.
- **Spec-vs-delivered reconciliation:** every plan-T15 item delivered. Scope checks from the dispatch brief: the T10 worker-client `terminate()`-on-rebuild hygiene note is **not** in the plan's T15 scope (which names only the five suite legs + mutation check) → left logged, untouched (no scope-creep); F2 closed (pin #1/#3 are normalization-on 512² cases); F1 verified intact (seed 404 present in both T3c seed lists, untouched).
- **Risks / notes for the verifier:** (a) The four digests are pinned constants — any *intended* future recalibration (T3b/T3c-class work) must re-record them; the failure message prints the received digest so re-pinning is a paste, and the in-code comment says the same. (b) The digest helper's portability rests on the no-transcendentals audit (grep-verified: `Math.imul`/`floor`/`sqrt` only under `src/noise` + `src/generation`) and little-endian targets — a big-endian host or a transcendentals-adding change would false-positive, loudly. (c) Runtime: the suite's wall clock moved from ~5.9 s to ~7.5–9.7 s observed; the budget guard remains the perf smoke (<2 s per 512² generate, anti-hang by design), with hard perf evidence still T16's DevTools mandate. (d) The mutation check was run against the full suite (5 tests fail) rather than only the hash test — the hash test is the *first* failure and the only one that fails on byte drift with behavior still in-band, which is precisely the net the old suite lacked.
- **Verification (ultron-supreme, 2026-08-27) — VERDICT: PASS → T15 auto-approved.** Full independent re-validation with the verifier's own Node-24 harness (throwaway script in `/tmp`, deleted after); every substantive claim reproduced:
  - **Gates re-run:** `npm test` → **204/204** (15 suites; fields.test.ts 21 tests); `npm run typecheck` → exit 0; `npm run build` after clean `dist/` → exit 0 — worker chunk `map.worker-DbpHG_aw.js` 3.96 kB, first line `(function(){`, **zero ESM tokens** (D5 intact); entry `index-B0NE8tHL.js` 27.75 kB, exactly the claimed pre/post-T15 hash.
  - **Pin integrity (the decisive check — pins pin REALITY):** the verifier recomputed **all four** digests with its OWN harness — own controller-merge mirror read from `map-controller.ts` (`{...DEFAULT_FIELD_PARAMS, elevationFreq 2.5+2v, redistribution 1.2+0.6v, overrides last}`), own two-lane digest implementation over the raw Float32 bytes of the real `generateFields` output, plus interleaved foreign generations between double-runs. All four match the pinned constants exactly: defaults@512² s404 `e:0b4517dcec50057f m:c9071afb9ed63a16`; highlands@512² s7 `e:783fc69e15645ad6 m:66e1b07db81a9ccf`; boot-continent@512² s0 `e:0e27b278c5e9c812 m:f70138fe372f43c4`; defaults@256² s9 `e:d2e64633e148ac3a m:6a17f983e96c9842` — run-to-run identical everywhere. Independent sha256 cross-checks recorded for future verifiers (defaults@512² s404: sha-e `8f699e34ac746bc8`, sha-m `837855d233b27a7e`; boot@512² s0: `a2633410b89fe035` / `531c6cf68ff090e9`).
  - **Helper sanity:** two independent 32-bit lanes (FNV-1a + murmur3-style) over raw bytes = 64 combined bits, no truncation; elevation/moisture hashed in separate lanes — both mutation probes confirmed the moisture digest byte-identical while the elevation lane moved (structural: neither mutated knob touches moisture). Pinned constants are string literals, so the test cannot vacuously derive its own expectation.
  - **Mutation probes re-run (both bite; restored byte-identical via sha256 `1178324055…`, suite 204/204 + build hashes re-confirmed green after restore):** (a) `elevationOctaves` 5→4 → **exactly 5 failures**, digest message reproduced verbatim — `defaults @ 512², seed 404 digest changed: expected 'e:81c675e38d01a2d3 m:c9071afb9ed63a16' to be 'e:0b4517dcec50057f m:c9071afb9ed63a16'` (names the case; moisture lane unchanged isolates the drift to elevation) — plus interleaved, T3c ratio ×2, T3c-fix boot. (b) subtle normalization drift `elevationNormHigh` 0.99999→0.9995 (the exact T3b defect class) → digest failure (`e:c7626717ceae43ba` vs pin, moisture lane again unchanged) + interleaved + defaults ratio. **Nuance on the entry's "fails first" phrasing:** the digest test is the MOST specific failure and the only byte-drift-diagnosing one, but in execution order it is failure [4/5] (the ratio/boot tests are declared earlier in `fields.test.ts` and genuinely fail too); wording imprecision only, substance verified.
  - **Zero production changes:** `presets.ts` full sha256 = `0dfd52c3d0dafa09a56ccc91d47f98b13349ac9e704e65f38b907055128bcad1` — exact match to the T3c-fix record (untouched). `fields.ts`: whitespace-clean, defaults `elevationOctaves: 5` / `falloffStrength: 0.0` / `elevationNormHigh: 0.99999` confirmed via the mutation round-trip, all T3c/T3c-fix knife-edge behavioral margins still passing (continent s0 boot coherence 55.8 vs floor 55, highlands s7 mtn 2.09 vs floor 2.0 — value drift could not survive these), and today's bytes are now pinned by the independently reproduced digests. Observation (non-blocking): the T3c-fix entry's truncated pre-fix hash `6d9120f7…` is NOT reproducible from the current `fields.ts` under any plausible old line-16 text (6-variant sweep) — most plausibly a recording artifact in that log entry, since the full `presets.ts` hash recorded alongside it matches exactly; process note: record FULL hashes, not prefixes.
  - **No weakening:** `handler.test.ts` still runs the generate@128 worker-path-vs-direct element-wise comparison (transitively covers the removed 128² same-inputs-twice test); pin #4 supersedes the removed 256² property test on the SAME configuration (defaults, seed 9, 256²) with a strictly stronger recorded-constant assertion; the moved seed-divergence test now asserts BOTH fields (widened); T3c seed lists keep the load-bearing 404 (ratio `[0,7,12345,404]`, coherence `[0,42,7,12345,404]` = the T3c-fix F1 record); no threshold, seed, or assertion loosened anywhere.
  - **Five legs live in the tree:** noise golden+stats → `src/noise/simplex.test.ts` (16 golden values @1e-10, determinism/bounds/continuity) + `src/noise/fbm.test.ts` (512² zero-mean, octave composition); field-hash determinism → `src/generation/fields.test.ts` T15 block (4 pins + interleaved + seed divergence); URL round-trip → `src/state/urlState.test.ts` (round-trips, canonical string, malformed→defaults, sanitize); classifier boundary matrix → `src/generation/biomes.test.ts` (D3 band boundaries transcribed); palette ΔL → `src/generation/biome-palette.test.ts` (20-pair ΔL ≥ 0.08 + Machado deuteranopia).
  - **Scope rulings confirmed:** F2 closed (pins #1/#3 are normalization-on 512² cases); F1 intact; the T10 `terminate()`-on-rebuild hygiene follow-up correctly left untouched (outside the plan's T15 scope — still open, non-blocking); no cross-file dedupe, per the T14 verifier's layer-separation ruling.
  - **Follow-ups (non-blocking):** any future recalibration must re-record the four digests (the failure message prints the received digest — paste-and-go; the in-code comment says the same); log full sha256 hashes rather than prefixes; carried: coordinator real-browser pass (T11/T14/T3c-F3) and T16 DevTools perf evidence.

## T16 — Performance & jank validation · QA

- **Status:** completed — executed 2026-08-27 · **Delegation:** none (production worker in the Ultron-Supreme run, coordinator-briefed).
- **What/why:** Plan T16 outcome = hard evidence for success measure #1 against the D2 budgets (`research/rq2-resolution-performance.md`): (B1) worker regen <100 ms @256² / <300 ms @512² mid-hardware; (B2) no main-thread task >50 ms during slider drag + animation; (B3) per-frame main JS <10 ms during crossfades; plus D2's compose budgets (<8 ms/chunk, <40 ms/frame) and the preview path's ~100 ms end-to-end target. Acceptance: "DevTools performance recordings archived; recordings in production log." The vitest perf smokes (fields <2 s anti-hang; compose chunk budgets in Node) explicitly deferred hard perf evidence to T16 — this entry is that evidence.
- **Tooling decision (recorded per dispatch brief):** the coordinator's in-app browser automation is broken this session, and vitest/happy-dom cannot measure main-thread jank, rAF cadence, long tasks, or real-worker timing (no real compositor/scheduler/engine). Added `playwright` as a devDependency (bundled Chromium 1234 + WebKit 26.5 — the Safari engine family), ran the evidence matrix, then **removed it again** (transient dependency per constraints): `package.json` and `package-lock.json` verified to contain zero playwright entries and `node_modules/playwright` gone; downloaded browser binaries remain only in `~/Library/Caches/ms-playwright` (machine-level cache, outside the repo). The harness page and driver are archived as `.txt` copies under `docs/ultron/evidence/t16/` so a verifier can re-run the whole matrix.
- **Method (two surfaces × five environments):**
  1. **Harness page** (Vite dev server, importing the REAL production modules — real `new Worker(new URL(...))` worker, `MapWorkerClient`, `MapRenderer`, `StagedAnimation`, `MapController`, `startApp`): scenarios `worker` (main-thread round-trip timings per protocol op), `compose` (LUT build, `setFields`, per-mode first compose, crossfade blit, moisture reclassify path, per-64-row-chunk costs), `animation` (every rAF callback timed; crossfade windows fade-1 [950–1450 ms] / fade-2 [2350–2850 ms] of the 2.8 s reveal), `drag` (25 elevation inputs ~60 ms apart through the real 30 ms debounce → 256² preview path, then commit; 25 moisture inputs; worker postMessage counting), `memory` (30-regen churn + 8 real `startApp().rebuild()` cycles, heap sampled after explicit GC `--js-flags=--expose-gc`; live worker targets counted via CDP `Target.getTargets`).
  2. **Production app** (`vite preview` of the real `dist/` build, driven through real DOM events): phase-marked longtask (PerformanceObserver) + rAF instrumentation across boot animation → elevation drag (30 inputs + change-commit) → moisture drag → dice regen → Export → Share.
  Environments: `chromium-desktop` (1280×800, native), `chromium-desktop-4x` (4× CPU throttle via CDP `Emulation.setCPUThrottlingRate` — the accepted mid-hardware proxy), `chromium-mobile-4x` (Pixel 7 emulation + 4× throttle — mid-Android proxy), `webkit-desktop` (WebKit 26.5/JSC, native), `webkit-ios` (iPhone 13 emulation, WebKit/JSC). Host: macOS 26.6.2, Apple silicon, Node 24.8. Additionally: **two DevTools-format performance traces** (Chromium native + Chromium 4×; loadable directly in Chrome DevTools → Performance panel) covering boot animation + both drags.
- **Budget verdicts (budget → measured → verdict):**

  **B1 — worker regen <300 ms @512², <100 ms @256² (median/max, ms):**

  | op | V8 desktop | V8 4× | V8 Pixel7-4× | JSC desktop | JSC iPhone | budget | verdict |
  |---|---|---|---|---|---|---|---|
  | generate @512² (full) | 129/165 | 108/138 | 104/161 | 99/180 | 105/201 | <300 | **PASS** (worst observed 201 ms = JSC cold start; ≥1.5× margin everywhere) |
  | recompute-elev @512² | 90/96 | 71/73 | 67/68 | 68/70 | 68/69 | <300 | **PASS** |
  | generate @256² (preview cache-miss) | 32/40 | 26/29 | 26/28 | 26/27 | 26/27 | <100 | **PASS** |
  | recompute-elev @256² (preview hit) | 23/25 | 18/20 | 18/19 | 18/19 | 18/18 | <100 | **PASS** (≥4× margin) |

  Trace cross-check (worker-thread `onmessage` handler durations, isolated from the traces): boot generate@512² = 140.6 ms native / 109.2 ms "4×"; elevation commit @512² = 90.9 ms native / 69.9 ms "4×"; drag preview handlers 16–37 ms — consistent with the round-trip table. **Mid-hardware honesty note:** CDP CPU throttling applied to the page target **does not throttle dedicated-worker threads** (measured directly: worker handler 140.6 ms native vs 109.2 ms at "4×"; preview handlers statistically identical at 1× and 4×) — so the worker-side numbers are native-speed on both engines. They are nevertheless mid-band evidence, not high-end-outlier evidence: this host performs the 512² generate at 92–129 ms in-browser (56 ms in Node) vs D2's high-end-desktop reference 36 ms and its mid-laptop estimate band 75–150 ms — i.e. the measured host sits ~1.6–2.6× slower than D2's Ryzen 5950X baseline, inside the mid band the budget was set for, and the JSC numbers double as the D5 Safari-engine-family check (worst 201 ms cold < 300 ms). Arithmetic stress: devices ~2.3× slower than this host remain inside the 300 ms budget; ~4× slower would exceed it (129×4 ≈ 516) — real-device Safari / mid-Android spot-checks are therefore routed to T17's human checklist (below), which the plan's own wording anticipated ("tested explicitly" — automation cannot reach real devices here).

  **B2 — no main-thread task >50 ms during slider drag + animation:** PerformanceObserver longtask count during the elevation-drag, moisture-drag, boot-animation and dice-regen phases = **0 in all five environments** (150+ slider inputs + 3 full animations per env). Whole-page totals: 0 (native, both engines) and exactly **1 at 4×** in each throttled Chromium env, located before the first phase mark (initial bundle module evaluation under 4× throttle — outside the budget's "during slider drag + animation" scope; the module is 27.75 kB). Both DevTools traces confirm: the ONLY >50 ms durations anywhere are the worker isolate's `onmessage` compute (140.6/109.2/90.9/69.9 ms) — main-thread worst trace task = 4.7 ms native / 9.3 ms at 4×. **PASS.**

  **B3 — per-frame main JS <10 ms during crossfades:** harness-instrumented rAF worst frame inside fade windows: 2.4 ms (V8 native) / 4.6–4.7 ms (V8 4×, Pixel-4×) / 3 ms (JSC both); whole-animation worst frame ≤ 4.7 ms; animation frame gaps p95 ≤ 18.7 ms (clean 60 fps), gaps >33 ms = 0 (one exception: a single 33–50 ms boot-phase gap at desktop-4×, no longtask attached — one dropped boot frame under throttle, noted, not a budget item). On the real app the single worst main-thread frame anywhere (boot fields-arrival: `setFields` + LUT + biomes compose + draw, at 4×) = **9.1 ms (Pixel-4×) / 7.9 ms (desktop-4×) / 8 ms (JSC) — PASS but the tightest margin in the matrix** (a ~6× device would exceed 10 ms on that one frame; recorded as residual risk). WebKit `play()` synchronous elevation compose 7–8 ms < 10 ms. **PASS.**

  **B4 — D2 compose budgets (<8 ms/chunk, <40 ms/frame):** per-chunk `composeChunk` worst 0.6 ms (V8) / 3 ms (JSC); full-frame first compose medians 1.5–2.2 ms native / 3.8–5.6 ms at 4× (synthetic-loop maxes 6.6–13.3 ms — GC-amplified outliers, see B6 note); crossfade blit (two `drawImage` + `globalAlpha`) median 0.0 ms, max 0.7 ms — the GPU-composited path behaves exactly as D2 predicted. Moisture reclassify (LUT rebuild + recompose + blit) median 1.0–4.3 ms; **real per-input handler max 2.2 ms (V8 native) / 4.7 ms (4×) / 2–3 ms (JSC), 0 worker posts in every env** — the moisture path measurably never touches the worker (D2's palette-LUT design confirmed by instrumentation, not just code reading). **PASS.**

  **B5 — preview path end-to-end <~100 ms (D2: 30 ms debounce + 256² recompute):** measured input→preview-render median 49.5–53.4 ms, max 56.2–63.8 ms across all five envs (30 ms debounce + 16–25 ms recompute@256 + ~1 ms compose); 25/25 previews delivered during the simulated drag (latest-wins never starved the pipeline at 60 ms input cadence); elevation commit → final 512² frame 62–95 ms. **PASS** with ~1.6× headroom even at the throttled-mobile proxy.

  **B6 — memory / buffer churn (typical acceptance shape (f)):** JS heap after GC: **0.0 MiB delta over 30 regenerates** (all three Chromium envs; per-env baselines 24.8/40.1/48.1 MiB, flat) and flat across 8 `startApp` rebuild cycles; zero worker/protocol errors during churn (buffer detach guards never tripped). Worker-thread count after all scenarios: **11 alive = every worker ever constructed minus the one explicitly `terminate()`d** — i.e. the D5 retry path (`rebuild()`) leaves one live worker thread per retry (the long-open T10 "client.terminate() on rebuild" hygiene clause, now quantified: no JS-heap growth, bounded by user-triggered retries, fully reclaimed at page close). **Not a budget failure → no production fix under T16's mandate; routed as a T10 follow-up with numbers.** Note on synthetic-loop outliers: the harness's back-to-back `reclassify` loop showed occasional 28.9 ms (native) / ~70 ms (4×) iterations — GC pauses amplified by allocation churn with zero breathing room; the REAL interaction path (25–30 inputs spaced 40–55 ms apart, all five envs) never produced one: per-input max 4.7 ms, 0 longtasks. Real-path numbers are the authoritative ones; the synthetic outlier is recorded for honesty about harness limits.
- **T14 deferrals folded in (per state.md routing "fold into T16's browser validation"):**
  - **Share (clipboard round-trip):** Chromium (all 3 envs, granted clipboard permissions): click → 'Copied!' → `navigator.clipboard.readText()` returns exactly the live hash URL (`#v=1&seed=…&el=…&mo=…`); loading that URL in a fresh page reproduces a **byte-identical map** (canvas `toDataURL` equality — `mapIdentical: true` in all three). WebKit: 'Copied!' success indication fires (the clipboard write path succeeds); read-back is not automatable in WebKit, so determinism was verified instead via hash-URL fresh-load canvas identity on JSC (`webkit-hash-roundtrip.json`: identical). **Deferral CLOSED.**
  - **Export PNG:** all 5 envs: Playwright download event fires, suggested filename `biome-generator-{seed}.png`, saved file is a valid PNG (magic bytes) with IHDR **512×512**, ~49–52 kB (five files archived). **Deferral CLOSED.**
- **Observed non-perf defect (routed, NOT fixed — outside T16 scope, no budget impact):** the **Skip button stays enabled after the animation completes** — `refreshSkipButton()` is last driven by the `'done'` stage announcement, which `StagedAnimation.tick` fires *before* `settle()` flips `playing` to false, so the button never re-disables (dead button; clicking no-ops through the `playing` guard). Cosmetic/UX only; belongs to T10's panel wiring. Found because the first driver draft waited for skip-disabled as the settle signal and timed out.
- **Spec-vs-delivered reconciliation:** every plan-T16 outcome item has measured evidence (B1/B2/B3 above; "Safari + mid Android tested explicitly" delivered as WebKit-engine measurement + Chromium mobile-emulation + 4× throttle, with the real-device residue honestly routed to T17 — Playwright WebKit is the Safari engine family, not shipping Safari, and no automation can reach real phones in this environment). Acceptance "DevTools performance recordings archived" → two loadable `.trace` files + full per-env JSON dumps under `docs/ultron/evidence/t16/`; "recordings in production log" → the tables above. No production code was changed (defect-fix authority not exercised — no budget failed); calibration values untouched; test suite untouched.
- **Gates after cleanup:** `npm test` → **204/204** (15 suites); `npm run typecheck` → exit 0; `npm run build` (clean `dist/`) → exit 0 with **byte-identical assets** (`index-B0NE8tHL.js` 27.75 kB, `map.worker-DbpHG_aw.js` 3.96 kB — the same content hashes as the T15 record, proving zero production drift). Temporary harness files (`perf-harness.html`, `src/perf-harness.ts`) deleted; `playwright` devDependency removed.
- **T17 user-assisted checklist (folded per brief — real-browser items automation cannot reach):** (1) open the deployed URL on real Safari (iOS or macOS): confirm the staged animation is smooth and both sliders respond without visible stutter; (2) same on a mid-range Android device (Pixel a-class or similar): if a Generate/dice press feels slow, time it (budget <300 ms); (3) tap Share on a real device and paste the link into a fresh tab — map must be identical (clipboard read-back was automatable only on Chromium); (4) optional: DevTools mobile CPU throttle 6× spot-check of first load (the 9.1/10 ms worst-frame margin at 4× is the tightest number this task produced).
- **Risks / notes for the verifier:** (a) worker-timing run-to-run variance on this host is real (gen512@512² median observed 92.5–129 ms across sessions with dev/preview servers running; every run <300 ms) — re-run via the archived driver if in doubt; (b) the worker threads being unthrottleable via page-target CDP is a methodology limitation, disclosed and worked around with the band argument + JSC numbers; (c) the B3 4× worst-frame margin (9.1–9.3 of 10 ms) is the one place a slower device class would trip a budget — it is a single frame at fields arrival, not the crossfades themselves (crossfade worst 4.7 ms at 4×); (d) the memory evidence's worker-count check is Chromium-only (CDP); WebKit ran the same cycles with zero errors but no heap/worker-count APIs.
- **Evidence:** `docs/ultron/evidence/t16/` — `chromium-desktop.json`, `chromium-desktop-4x.json`, `chromium-mobile-4x.json`, `webkit-desktop.json`, `webkit-ios.json` (harness + app numbers), `trace-chromium-desktop.trace`, `trace-chromium-desktop-4x.trace` (DevTools-loadable recordings), `export-*.png` ×5 (T14 verification), `webkit-hash-roundtrip.json`, `perf-harness.ts.txt` / `perf-harness.html.txt` / `driver.mjs.txt` (re-runnable methodology).

## T16 Verification — independent audit · Ultron-Supreme verifier

- **Verdict: PASS — auto-approved (ultron-supreme), 2026-08-27.** All gates re-run by the verifier, all 16 evidence artifacts audited against the entry's tables (not spot — full cross-check), one budget independently re-measured with a verifier-built harness, both routed findings adjudicated REAL in source. Zero defects; three prose nits (below), none affecting any verdict.
- **Gates (verifier-run):** `npm test` → **204/204** (15 suites; the perf smoke printed `generateFields(512², defaults) wall time: 76.2 ms` in-suite, corroborating the perf claims); `npm run typecheck` → exit 0; `npm run build` → exit 0 emitting `index-B0NE8tHL.js` 27.75 kB + `map.worker-DbpHG_aw.js` 3.96 kB — **exactly the T15 record** (production-log lines 426/432): zero drift confirmed. Transient-tooling removal confirmed: `package.json` devDependencies = happy-dom/typescript/vite/vitest only; the only "playwright" strings in `package-lock.json` are vitest's own optional-peer `@vitest/browser-playwright` metadata (not an install); no playwright package in `node_modules`. Harness residue (`perf-harness.html`, `src/perf-harness.ts`) gone from the source tree; only `.txt` archives under `evidence/t16/` remain.
- **Evidence audit (full table cross-check, all 5 env JSONs):** **B1** — all 20 median/max cells match the entry's table exactly, including the worst cases (JSC iPhone gen512 105/201, Pixel7-4× 104/160.97→161; every env <300/<100 with the claimed margins). **B2** — per-phase PerformanceObserver longtask arrays are empty for all 6 phases × 5 envs; whole-page totals 1/1/1/0/0 with the 4× extras outside phase marks, as stated. **B3** — fade-window worst frames 2.4/4.6/4.7/3/3 ms and whole-animation worsts ≤4.7 ms match; the app worst main-thread frames are **9.1 ms (Pixel-4×) / 7.9 ms (desktop-4×) / 8 ms (JSC)** at boot fields-arrival (~1.2 s into boot), exactly as claimed. **B4** — `postsDuringMoisture` = **0 in all five envs**; real per-input handler maxes 2.2/4.6–4.7/3/2 ms match. **B5** — preview medians 49.5–53.4 ms exact; 25/25 previews delivered everywhere; commit 66–75 ms (app side reaches the claimed 62–95 band). **B6** — `churnDeltaMiB` 0 in all three Chromium envs (baselines 24.8/40.1/48.1, all 6 rebuild heap samples flat), `workersAliveAfterMemory` = 11 ×3, WebKit heap honestly `heapSupported: false` with the cycles still run.
- **Trace audit (independent parse):** both `.trace` files are valid DevTools-format traces (`traceEvents` + CrRendererMain/worker metadata). The ONLY >50 ms events anywhere are worker-isolate `HandlePostMessage` at **140.6/90.9 ms native and 109.2/69.9 ms 4×** — the entry's cross-check numbers verbatim. Drag-window worker handler durations 18.7–37.0 ms native / 14.9–32.6 ms 4× — the "16–37 ms" claim. The "main-thread worst trace task 4.7/9.3 ms" numbers are the worst main-thread `FireAnimationFrame` slices (4.70 native / 9.27 4×, at ~+1.2 s = the fields-arrival frame). Nit (a): larger sub-50 ms main-thread slices exist in the 4× trace at page-load/early-boot (Layout 41.1 ms at +74 ms, HandlePostMessage 26.0 ms at +269 ms) — outside the B2 scope ("during slider drag + animation") and below the 50 ms longtask bar, so no budget impact, but "worst trace task" reads more absolute than the measurement was.
- **Independent re-measurement (verifier harness, raw Node 24.8, `generateFields` @512², seed 404, 5 repeats/config):** defaults **median 78.3 ms** (runs 66.9–78.8), highlands-merge **median 78.6 ms** (78.4–78.8) — ~3.8× inside the 300 ms budget and consistent with the entry's "56 ms in Node" within its own disclosed run-to-run variance (in-browser 92–129 ms; JSC browser worst 201 ms remains plausible as cold-start overhead over native). Order-of-magnitude validation: PASS.
- **Methodology honesty:** the CDP-throttling limitation is stated twice in the entry (B1 "Mid-hardware honesty note" + Risks (b)) with the measured proof (worker handler 140.6 ms native vs 109.2 ms at "4×"). The verifier's own trace parse independently confirms the disclosure: drag-window worker handlers at "4×" (14.9–32.6 ms) are statistically NOT slower than native (18.7–37.0 ms). The workaround stands: this host is mid-band per D2 (in-browser 92–129 ms vs the 75–150 ms mid-laptop band), JSC native worst 201 ms < 300 ms with ~2.9× median margin (nit (b): the single cold-start outlier is 300/201 = 1.49×, so "≥1.5× margin everywhere" rounds up on one sample of n=8), and the ~2.3×-slower-device headroom arithmetic checks out. The T17 routing is inside the entry as required — quoted: "real-device Safari / mid-Android spot-checks are therefore routed to T17's human checklist (below), which the plan's own wording anticipated ('tested explicitly' — automation cannot reach real devices here)", plus the 4-item T17 user-assisted checklist (real Safari smoothness, mid-Android generate timing, real-device Share paste-back, optional 6× throttle first-load check).
- **Share/Export closure:** Chromium ×3 envs: `clipboardMatchesHash: true` + fresh-load `mapIdentical: true` with full URLs recorded (`#v=1&seed=…&el=…&mo=…`) — round-trip + canvas byte-identity proven. WebKit ×2: `copiedIndicated: true` with `clipboardUrl: null` (read-back honestly marked non-automatable) and the separate `webkit-hash-roundtrip.json` (`identical: true` on JSC) covering fresh-load identity. Export ×5 envs: the verifier independently parsed the archived PNGs — valid magic bytes, **IHDR 512×512** each, and file byte counts (49802/51626/52061/49183/49137) match each env's `export.bytes` exactly, filenames `biome-generator-{seed}.png`. Deferral closures: accepted.
- **Routed findings adjudicated (for the fix dispatch):**
  - **F1 — Skip button stays enabled after animation end: CONFIRMED REAL, diagnosis exact.** `src/render/animation.ts` `tick()` completion branch fires `this.announce(run, 'done')` (line 216) BEFORE `this.settle(run)` (line 217) — and `skip()` has the same order (lines 175–176). `settle()` is what nulls `this.run`, so during the 'done' announcement `playing` still returns true. `src/ui/control-panel.ts` `onStage` → `refreshSkipButton()` (line 322: `skipButton.disabled = !(masterEnabled && ports.animation.playing)`) therefore takes its LAST refresh with playing=true, and nothing re-fires after settle. The click handlers (lines 503/531) guard `if (ports.animation.playing) … skip()` — dead enabled button, click no-ops. Fix directions for T10: settle-then-announce (order swap), or refresh the skip button off a playing-change signal rather than stage announcements.
  - **F2 — one live worker leaked per retry/rebuild: CONFIRMED REAL, diagnosis exact and structural.** `src/app.ts` `rebuild()` (line 474) = `disposeStack(); buildStack();`. `disposeStack()` (lines 400–405) calls `controller.dispose()`, and `MapController.dispose()` (`src/controller/map-controller.ts` lines 201–206) sets `disposed`, clears debounce/listeners — it never terminates the worker (its own comment, line 199, says termination belongs to the caller). But the caller CANNOT: `createMapController` (`src/controller/create.ts` line 33–37) returns only `{ controller, renderer }`, discarding the only `MapWorkerClient` reference that owns `terminate()`. So every rebuild strands the previous worker thread. Evidence arithmetic verified: harness `appWorkerConstructions` = 9 (1 boot + 8 rebuilds), `workersAliveAfterMemory` = 11, and the harness contains exactly one explicit `client.terminate()` (`scenarioWorker`, harness line 180) — "every worker ever constructed minus the one explicitly terminated" is literally true. Impact as stated: idle threads, flat JS heap, bounded by user-triggered retries, reclaimed at page close. Fix directions for T10: expose the client (or a `terminate()` hook) on the bundle and call it in `disposeStack()`.
  - Both findings are recorded in the T16 entry as routed, not silently dropped (F1 has its own "routed, NOT fixed" bullet; F2 is routed inside B6 "as a T10 follow-up with numbers").
- **Prose nits (no action required):** (a) "main-thread worst trace task = 4.7/9.3 ms" is specifically the worst rAF task slices — sub-50 ms boot-region Layout/HandlePostMessage slices up to 41 ms exist in the 4× trace outside the budget scope (wording only); (b) "≥1.5× margin everywhere" is 1.49× on the single JSC cold-start outlier (2.9× on medians); (c) B5 "max 56.2–63.8 ms" — the five evidence maxes span 59.3–63.8 ms (medians 49.5–53.4 exact); the stated floor is 3 ms low, in the conservative direction.
- **Follow-ups for the coordinator:** dispatch the T10 fix covering F1 (skip-button refresh timing) + F2 (worker terminate on rebuild) — both diagnoses verified trustworthy above; keep the T17 human checklist items (real Safari, mid-Android, real-device Share, optional 6× first-load) attached to T17's execution; the B3 9.1/10 ms worst-frame margin stays the tightest residual risk.

## T16-fix — adjudicated defect dispatch (F1 + F2) · Production

- **Status:** executed 2026-08-27 · **Delegation:** none (production worker in the Ultron-Supreme run, coordinator-briefed on the T16 Verification diagnoses).
- **What/why:** the two routed findings the T16 verifier adjudicated REAL, fixed exactly along the verifier's written directions (both diagnoses re-confirmed at source before editing; neither was wrong):
  - **F1 — Skip button stays enabled after the animation ends.** `StagedAnimation.tick()`'s completion branch and `skip()` both announced `'done'` BEFORE `settle()` nulled the run, so the control panel's `refreshSkipButton()` — driven by stage events and reading `animation.playing` — took its last refresh with `playing === true` and never re-fired; the enabled button's click guard then no-oped. **Fix (settle-then-announce, the verifier's first suggested option — no event-contract change, no new UI):** `src/render/animation.ts` `tick()` completion branch and `skip()` now call `settle(run)` BEFORE `announce(run, 'done')`; stage order ('…biomes', 'done') and announce-once semantics are unchanged, so the panel's last refresh observes `playing === false` and disables Skip. Reduced motion was already correct (play() never creates a run — `playing` is false during both announcements; now pinned by test).
  - **F2 — one leaked live worker per retry/rebuild.** `MapController.dispose()` never terminated the worker and the caller couldn't: `createMapController` returned only `{controller, renderer}`, discarding the `MapWorkerClient` that owns `terminate()`, so `app.ts rebuild()` stranded one worker thread per retry. **Fix (internal wiring, public API shape stable):** `MapController` gains an optional constructor `onDispose` hook; `dispose()` is now idempotent (first call wins) and fires the hook once, AFTER `disposed` is set; `createMapController` wires `onDispose: () => client.terminate()`. In-flight requests at dispose time are absorbed: `terminate()` rejects pending with `WorkerCrashError`, and the controller's pre-existing disposed-guards in every `.catch` swallow it silently (no error events, no unhandled rejections — pinned by test). `MapControllerBundle`'s shape is unchanged; the T10-era follow-up "call `client.terminate()` on the dropped worker during rebuild" is thereby resolved (D5 retry semantics now actually enforced: terminate + fresh client + fresh worker per rebuild).
- **Files changed:** `src/render/animation.ts` (order swap ×2 + doc comments; no timing/config/behavior change otherwise), `src/controller/map-controller.ts` (`onDispose` hook + idempotent dispose + docstring rewrite), `src/controller/create.ts` (client wired through the hook; unused `WorkerPort` annotation dropped), `src/app.ts` (disposeStack comment only), `src/controller/create.test.ts` (NEW suite, 3 tests). Tests extended: `src/render/animation.test.ts` (+3), `src/ui/control-panel.test.ts` (+1 integration), `src/controller/map-controller.test.ts` (+2), `src/app.test.ts` (retry test gained a dispose assertion). Untouched: calibration values, biome table, animation timings (1000/400 ms stages — `DEFAULT_ANIMATION_CONFIG` test still pins 2800 ms total), worker protocol message shapes, worker sources (worker chunk hash byte-identical, below).
- **Test additions (9 new, 213/213 total):**
  - `animation.test.ts` — (1) normal completion: a second stage listener records `anim.playing` per announcement; asserts `true` at elevation/moisture/biomes and **`false` at 'done'**, with 'done' still announced exactly once; (2) same for the user-skip path (skip mid-fade-1); (3) reduced motion: `playing === false` during BOTH announcements (Skip disabled from the start) + `['biomes','done']` exactly once each.
  - `control-panel.test.ts` — integration: the REAL `StagedAnimation` (manual frame queue + fake clock, no-op renderer) wired behind the REAL panel ports. Asserts Skip disabled at build, **enabled during playback** (after 'elevation' and mid-fade 'moisture'), **disabled after normal completion**, re-enabled on the next play, and **disabled after a user skip**. This is the end-to-end pin of the defect (the pre-fix animation reports playing=true at 'done'; the fake-based unit test alone cannot catch it).
  - `map-controller.test.ts` — (1) the hook fires exactly once (double dispose idempotent), never while live, and dispose without a hook stays safe; (2) dispose during a pending generate: hook fires synchronously, the deferred's later rejection surfaces no error event and causes no further worker churn.
  - `create.test.ts` (new, happy-dom; FakeWorker + counting factory per the client-test pattern; canvas 2d context mocked at the prototype — the dispose paths compose no pixels) — (a) `controller.dispose()` → the factory-created Worker's `terminate` was called; (b) **worker-count arithmetic: 5 construct→dispose cycles → exactly 5 workers created, all 5 terminated, live count = 5 − 5 = 0 (constant across cycles; pre-fix it grew by 1 per cycle)**; (c) dispose while a generate is pending → worker terminated, nothing further posted, zero events after dispose, no unhandled rejections (vitest fails the run on any).
  - `app.test.ts` — the T10-era retry test (error → Retry → rebuild re-applies the FULL SharedState, kept green) additionally asserts the OLD controller's `dispose` ran, which with the real bundle is now what terminates the old worker.
- **Worker-count arithmetic proof:** T16's evidence — 11 alive after 9 app constructions + harness's own explicit terminate (9 − 1 explicit + harness worker = 11 observed). Post-fix, every construction's worker is terminated by its controller's dispose: N constructions → N terminations → live count 0 once the app is torn down, and exactly 1 live (the active stack's) during steady state regardless of retry count. Pinned mechanically by test (b) above with a counting factory.
- **Gates (run after the fix):** `npm test` → **213/213 passed, 16 suites** (was 204/15); `npm run typecheck` → exit 0; `npm run build` (clean `dist/`) → exit 0, **new asset hashes: `index-BaLHBJm7.js` 27.86 kB (was `index-B0NE8tHL.js` 27.75 kB — main-thread code changed, hash change expected), `map.worker-DbpHG_aw.js` 3.96 kB BYTE-IDENTICAL to the T15/T16 record** (worker graph untouched — first line still `(function(){…`, zero ESM import/export tokens, `self.onmessage` wiring: pure IIFE per D5), CSS unchanged `index-BcTqoKYm.css` 3.72 kB.
- **Deviations:** none from the verifier's fix directions. Two scope notes: (1) `dispose()` gained an idempotency guard (first call wins) so the hook is exactly-once by construction — no existing caller double-disposes, behavior for single calls unchanged; (2) F1's residual corner NOT addressed (outside the adjudicated defect, unchanged behavior): if the app is rebuilt (retry path) while an animation is playing, `animation.dispose()` announces nothing, so the Skip button keeps its last state until the next stage event — in the actual retry flow this is unreachable (the error event disables the whole panel before Retry appears), and after rebuild the button reads the NEW stack's animation (`playing === false`). Recorded for honesty; no UI invented.
- **Risks for the verifier:** (a) the F1 fix intentionally leaves `'biomes'` announced before settle (playing=true at that announcement) — transient and unobservable: 'done' fires synchronously after in the same tick; (b) `onDispose` ordering (disposed flag first, hook last) is what makes terminate-rejections silent — it is pinned by the dispose-during-generate tests; (c) `create.test.ts` mocks `getContext` at the prototype level; if a future test in the same file needs real rasterization it must restore the spy (afterEach does); (d) plan.md T16 row and T10's terminate-hygiene clause updated to point here (see below).

## T16-fix Re-verification — independent audit · Ultron-Supreme verifier

- **Verdict: PASS — fix verified, follow-up list CLOSED (auto-approved (ultron-supreme), 2026-08-27).** Both defects behaviorally resolved exactly along the adjudicated directions; every claim in the T16-fix entry checked at source; both requested mutation probes performed and reverted byte-exactly (proven by hash reproduction); no new defects.
- **F1 resolution confirmed at source:** `src/render/animation.ts` `tick()` completion branch now runs `settle(run)` (line 224) BEFORE `announce(run, 'done')` (line 225), and `skip()` the same (settle 180 → 'done' 181). With the run settled, `this.run === null` when 'done' listeners fire, so the panel's `refreshSkipButton()` observes `playing === false` and disables Skip. **Announce-once semantics intact** (all three new animation tests assert `'done'` announced exactly once; reduced-motion asserts the full `['biomes', 'done']` sequence; the pre-existing T8 suite is green). The reduced-motion path was already correct — `play()` returns before any run is created — and is now pinned.
- **F2 resolution confirmed at source:** `MapController` takes an optional `onDispose` constructor hook (readonly, fire-once); `dispose()` is idempotent (`if (this.disposed) return`) and fires the hook AFTER setting `disposed`, clearing debounce/listeners/cancelHook; `createMapController` wires `onDispose: () => client.terminate()`; `MapControllerBundle` still returns `{controller, renderer}` (public shape unchanged). The silent-rejection design holds mechanically: all five worker call sites pair `.then/.catch`, every `.catch` leads with `if (this.disposed) return;`, and the disposed flag precedes the hook — so `terminate()`'s `WorkerCrashError` rejections on in-flight requests can only land in guards that swallow them. `app.ts`'s `rebuild() → disposeStack() → controller.dispose()` now releases the old worker; the app retry test additionally asserts the OLD controller's dispose ran.
- **Gates (verifier-run, post-mutation-restoration):** `npm test` → **213/213, 16 suites**; `npm run typecheck` → exit 0; `npm run build` → exit 0 with **`index-BaLHBJm7.js` 27.86 kB + `map.worker-DbpHG_aw.js` 3.96 kB** — the exact claimed picture. Worker chunk byte-identical to the T15/T16 record (first line `(function(){`, zero ESM tokens — verifier re-checked; the fix is main-thread-only, worker graph untouched, D5 IIFE invariant intact). CSS hash unchanged.
- **Mutation probes (both reverted byte-exactly — the post-restoration build reproduces `index-BaLHBJm7.js`, proving clean reversion):**
  - **Probe 0 (meta-claim):** a verifier-created temporary test with a deliberate unhandled rejection makes `vitest run` exit 1 ("Unhandled Rejection" error section) even though the test itself passes — validating that `create.test.ts`'s "no unhandled rejections" drain-and-rely-on-vitest pattern is a real oracle, and that 213/213 green is itself no-unhandled-rejection evidence. Probe file deleted after.
  - **Probe 1 (F1):** reverting `tick()` to announce-then-settle fails exactly the two T16-fix F1 tests — `animation.test.ts` "playing is already false when the done announcement fires (normal completion)" (`expected true to be false` at the playing-at-'done' assertion) and the `control-panel.test.ts` real-integration test. The fix is genuinely pinned, not vacuous. Restored.
  - **Probe 2 (F2):** removing the `onDispose: () => client.terminate()` wiring fails all three `create.test.ts` tests — terminate never called, and the counting-factory test reports **5 workers alive after 5 construct→dispose cycles (expected 0): the original leak reproduced exactly by the mutation**. Restored.
- **Regression checks:** the T10 retry re-applies-SharedState test is intact and extended (boot + rebuild SharedState payloads asserted; the preset-id variant still green); the aria-live announce-once and stage-order tests all green; the T8 animation timing suite green (`DEFAULT_ANIMATION_CONFIG` 2800 ms pin included). Worker protocol, calibration values, biome table untouched (worker chunk hash proves it).
- **Disclosed residual — verifier AGREES, accepted:** "rebuild during playback leaves Skip's last visual state until the next stage event." Verified unreachable through the UI: `rebuild()` has exactly one production caller (`onRetry`, app.ts line 527), Retry is only clickable after an error event, and the error flow disables the entire panel (skip disabled via the `masterEnabled &&` term — pinned by the control-panel error test) before Retry appears. Even if reached by a direct `rebuild()` call, it is harmless: the click guard reads the CURRENT stack's `playing` (false) so the click no-ops, and the new animation's first stage announcement self-heals the button state. No action required.
- **Docs accuracy:** the T16-fix entry's files-changed list, test inventory (+9 across 5 files), worker-count arithmetic, and scope notes all match what the verifier read; plan.md's T16 status/index row and T10 terminate-hygiene clause correctly point at the fix (T10's rq5 clause legitimately closed by it). No inaccuracies found.
- **Post-fix state:** T16 follow-up list empty. Remaining open threads for the run: T17 (human comprehension check + the four real-device items on its checklist) and T18 (deploy); the B3 9.1/10 ms worst-frame residual-risk note stands as recorded in the T16 Verification entry.

## T17-prep — human-test protocol package · Production

- **Status:** executed 2026-08-27 · **Delegation:** none (production worker in the Ultron-Supreme run, coordinator-briefed). T17 itself remains a mandated halt — executed by the USER with two real humans; this task only prepared the runbook + evidence templates.
- **What/why:** composed `docs/ultron/t17-human-test-protocol.md` (user-facing, zero agent jargon) implementing the plan's T17 spec — Section A: two humans, ONE generation each, unprompted stage description, pass = both participants cover all three concept families (elevation/height, moisture/water-ish, biomes/colors — the plan's exact acceptance wording), verbatim-answer recording, no-leading-words ground rules, read-aloud script, honest-scope note that the shipped UI itself names the concepts (header tagline + "Stage: Elevation/Moisture/Biomes" status line) with a described-vs-read-aloud capture field rather than hiding shipped UI. Section B folds the T16-routed 4-item real-device checklist verbatim (T16 entry, "T17 user-assisted checklist": real-Safari smoothness; mid-Android generate timing <300 ms; real-device Share paste-back; optional 6×-throttle first load), each with device, steps, pass look, and screenshot evidence. Section C: evidence conventions (this folder) + the fill-in template; Section D: hand-back → fix-cycle-before-T18 gating.
- **Files created:** `docs/ultron/t17-human-test-protocol.md`, `docs/ultron/evidence/t17/t17-results.md` (fill-in template). **Modified:** `docs/ultron/plan.md` (T17 status → `awaiting-user (protocol prepared)` + index row 17). **No production code touched**; `state.md`/`research/` untouched per brief.
- **Verification:** every referenced command exists in `package.json` (`dev`, `build`, `preview`, `typecheck`, `test`); UI labels verified against `src/ui/control-panel.ts`, `src/presets/presets.ts`, `index.html`, `src/style.css`: sliders "Elevation"/"Moisture", "Seed" + 🎲, "Generate"/"Skip", presets "Archipelago"/"Continent"/"Highlands", "Export"/"Share" ("Copied!" flash; "Share this link" fallback), stage line "Stage: Elevation/Moisture/Biomes" (CSS `::before`), tagline "Simplex noise, staged into biomes — elevation, moisture, color.". `vite.config.ts` sets no `server.host`/`preview.host` → `--host` genuinely required for LAN phone testing (stated as such); URLs carry the `/biome-generator/` base. Gates on the inspected tree: `npm test` **213/213** (16 suites) · `npm run typecheck` exit 0 · `npm run build` exit 0 with byte-identical assets (`index-BaLHBJm7.js` 27.86 kB, `map.worker-DbpHG_aw.js` 3.96 kB, `index-BcTqoKYm.css` 3.72 kB — the exact T16-fix Re-verification record; zero drift).
- **Discrepancies folded honestly (no plan change requested):** (1) T16's checklist item 1 says "open the deployed URL", but T18 (deploy) is still pending — the protocol uses the local production build (`npm run build` + `npm run preview -- --host`) and marks a post-T18 re-run against the public URL as a welcome bonus; (2) the plan's "unprompted" goal vs. the shipped UI's self-naming surfaces — resolved as a recording instruction (described-vs-read field), since hiding the tagline/stage line would test something other than the product.

## T18-prep — deploy packaging · Production

- **Status:** executed 2026-08-27 (prep only — T18 remains a wizard-lane halt; all external steps — repo creation, push, Pages enablement — belong to the USER). **Delegation:** none (production worker in the Ultron-Supreme run, coordinator-briefed).
- **What/why:** implemented the plan's T18 spec up to the halt line: `.github/workflows/deploy.yml` (GitHub Pages via Actions per D4), `README.md` (portfolio front page, fact-checked against the tree), `docs/ultron/t18-deploy-runbook.md` (the USER's ordered command sequence), plan.md T18 status flip + index row 18. Zero automated git/push anywhere — every external step is a written command for the user.
- **Workflow (exact step order per the T18 spec):** checkout → setup-node (npm cache) → `npm ci` → `npm test` → `npm run build` → configure-pages → upload-pages-artifact (`dist`) → deploy-pages; `permissions: contents: read, pages: write, id-token: write`; `concurrency: group pages, cancel-in-progress: true`; triggers `on: push → main` + `workflow_dispatch`.
- **Decisions (spec was silent — both documented in the workflow header):**
  - **CI gate includes `npm test`.** The plan's step list names only `npm ci` + `npm run build`; tests were added because vitest runs in ~4–10s, `npm run build` already embeds `tsc --noEmit` (typecheck is not a separate CI step for that reason), and every prior gate in this project ran the full suite. Failed-test output also reads cleaner as its own step than tangled into a build failure.
  - **Node 24** (`node-version: 24`). Current Active LTS (24.x, active since Oct 2025) AND the exact major this project was certified on locally (v24.8.0 produced the byte-identical build record below) — so CI reproduces the certified toolchain, not just an in-matrix one (Vite 8 engines: `^20.19.0 || >=22.12.0`).
  - **Action majors pinned to current stable, verified against `releases/latest` on 2026-08-27:** `actions/checkout@v7` (v7.0.1, Jul 2026), `actions/setup-node@v7` (v7.0.0, Jul 2026), `actions/configure-pages@v6` (v6.0.0, Mar 2026), `actions/upload-pages-artifact@v5` (v5.0.0, Apr 2026), `actions/deploy-pages@v5` (v5.0.0, Mar 2026). Major-floating tags (GitHub's own recommended pattern) pick up patch releases automatically.
  - **`workflow_dispatch` added** (not in the plan's step list): enables the runbook's manual re-deploy path and the "deterministic rebuild" acceptance re-check without requiring a push.
- **Runbook highlights (`docs/ultron/t18-deploy-runbook.md`):** ordering is create-repo → enable Pages (Settings → Pages → Source: GitHub Actions) → push, so the first run does not race the enablement (with the documented `gh run rerun` recovery if it does); web-UI primary with gh CLI variants (`gh repo create`, `gh api … /pages -X POST -f build_type=workflow`); expected URL shape `https://<user>.github.io/biome-generator/`; curl title-check (`<title>Procedural Biome Generator</title>`); 7-item smoke table (load+animation, Generate, dice+seed reproducibility, preset switch, share link on another device, PNG export, sliders bonus); constraints section carries the fixed `base: '/biome-generator/'` ↔ repo-name coupling and the optional post-deploy real-Safari re-check routed from T17/T16 (`t17-human-test-protocol.md` Section B item 1).
- **Files created:** `.github/workflows/deploy.yml`, `README.md`, `docs/ultron/t18-deploy-runbook.md`. **Modified:** `docs/ultron/plan.md` (T18 status → `prepared (awaiting user wizard-lane execution)` + index row 18), this log (T18-prep entry). **No production code, `vite.config.ts`, or `package.json` touched** — verified: workflow runs only existing scripts (`ci`-equivalent via `npm ci`, `test`, `build`); no new dependencies anywhere.
- **Verification:**
  - Workflow YAML parsed mechanically (js-yaml, throwaway npx invocation — nothing added to the project): structure printed and dry-run-checked — both jobs, `needs: build`, environment `github-pages` with `url: steps.deployment.outputs.page_url`, artifact `path: dist`, `node-version: 24`, `cache: npm`, permissions/concurrency exact.
  - README cross-checked against source: 12 biome names (`src/generation/biomes.ts`), Archipelago/Continent/Highlands (`src/presets/presets.ts`), `FINAL_RESOLUTION = 512` + 256² drag preview (`src/controller/map-controller.ts`, `src/generation/fields.ts`), `LUT_STEPS = 64` → 64×64/4096 cells (`src/render/compose.ts`), transferable-Float32Array protocol + detach guards (`src/worker/protocol.ts`, `client.ts`), `STATE_VERSION = 1` + replaceState semantics (`src/state/urlState.ts`), zero runtime deps (`package.json` devDependencies only), scripts `dev`/`test`/`build`/`preview`/`typecheck` all exist, ~2.8s animation = `DEFAULT_ANIMATION_CONFIG` 2800ms pin (T8/T16 record), dev URL under `/biome-generator/` (T1 302-redirect note). Screenshot is an honest evidence-frame reference (`docs/ultron/evidence/t16/export-chromium-desktop.png`, exists on disk) + explicit owner TODO — nothing fabricated.
  - **Gates on the delivered tree:** `npm test` → **213/213, 16 suites** · `npm run typecheck` → exit 0 · `npm run build` (clean `dist/`) → exit 0 with **`index-BaLHBJm7.js` 27.86 kB + `map.worker-DbpHG_aw.js` 3.96 kB + `index-BcTqoKYm.css` 3.72 kB — byte-identical names/sizes to the T16-fix Re-verification record, zero drift** (prep touched docs/workflow only, as intended). Worker chunk re-confirmed pure IIFE (first line `(function(){`); `dist/index.html` references assets under `/biome-generator/assets/` (base baked in — matches the runbook's repo-name constraint).
- **Discrepancy disclosed:** the task brief said the directory is "NOT a git repository yet", but an **empty** `.git` exists (T1's `git init -b main`: HEAD → `refs/heads/main`, zero refs, zero objects, no remotes — established by plain file reads, no git commands run). No conflict in practice: the runbook's Step 2 notes this and `git init -b main` is idempotent.
- **Risks for the verifier:** (a) `checkout@v7`/`setup-node@v7` are ~6-week-old majors — if GitHub's runners regress on them, dropping one major is the documented fallback; (b) first-run race between push and Pages enablement is mitigated by runbook ordering + `gh run rerun` recovery; (c) Node major pin floats to latest 24.x while certification used 24.8.0 — build determinism is anchored by the `package-lock.json` (npm ci) and vite/esbuild pins within it, not the Node patch version; (d) README's perf/median claims cite T16 evidence paths rather than restating full tables — spot-checkable against `docs/ultron/evidence/t16/`.

## T18-prep Verification — independent audit · Ultron-Supreme verifier

- **Verdict: NEEDS-FIX (workflow itself PASSES clean; all defects are in the user-executed docs — runbook + README), 2026-08-27.** No production code, workflow, or config changes required; the fix round is doc-only. The USER executes these files by hand, so fact errors in them are exactly the failure class this halt exists to prevent.
- **What passed (verified, not trusted):**
  - **deploy.yml:** parsed mechanically (throwaway `npx js-yaml`, cache-only, nothing added to the project) — valid YAML, zero tabs. Triggers `push: [main]` + `workflow_dispatch` exact; `permissions: contents: read, pages: write, id-token: write` exact; `concurrency: group pages, cancel-in-progress: true`; artifact `path: dist` matches Vite's default outDir (`vite.config.ts` sets no `outDir`); `node-version: 24` is inside Vite 8's declared engines (`node_modules/vite/package.json`: `^20.19.0 || >=22.12.0`); job graph internally consistent: `deploy.needs: build`, environment `github-pages` with `url: ${{ steps.deployment.outputs.page_url }}` referencing the step that carries `id: deployment` and `uses: actions/deploy-pages@v5` (whose documented output is `page_url`) — no undefined outputs anywhere. **All five action majors verified against live GitHub releases on 2026-08-27:** `checkout@v7` (v7.0.1 latest), `setup-node@v7` (v7.0.0 latest), `configure-pages@v6` (v6.0.0 latest), `upload-pages-artifact@v5` (v5.0.0 latest), `deploy-pages@v5` (v5.0.0 latest) — the worker's version claims were exact. `npm ci` has its `package-lock.json`. Nothing beyond the known Pages-enablement race (mitigated by runbook ordering) would fail on first run.
  - **Gates (verifier-run, clean `dist/` first):** `npm test` → **213/213, 16 suites**; `npm run typecheck` → exit 0; `npm run build` → exit 0 emitting **`index-BaLHBJm7.js` 27.86 kB + `map.worker-DbpHG_aw.js` 3.96 kB + `index-BcTqoKYm.css` 3.72 kB** — the exact T16-fix Re-verification record, zero drift. In-suite perf smoke printed `generateFields(512², defaults) wall time: 82.8 ms`, independently corroborating the README's ~78 ms raw-Node ballpark.
  - **Tree/file-touch audit (mtime proxy — no git commands run):** the only files modified in the T18 window (14:02–14:04) are exactly the disclosed set: `.github/workflows/deploy.yml`, `README.md`, `docs/ultron/t18-deploy-runbook.md`, `docs/ultron/plan.md`, `docs/ultron/production-log.md`. `state.md` (11:46) and `research/` untouched; newest production source is T16-fix-era (13:51). The `.git` scaffold claim re-verified by plain file reads: `HEAD → refs/heads/main`, empty refs, no remotes, no objects — `git init -b main` is idempotent and non-destructive on it, and `git add .` respects `.gitignore` (`node_modules/`, `dist/`).
  - **README fact-audit (checkable claims against source):** 12 biomes Deep Ocean → Snow (`biomes.ts` 34–46, ordered exactly); presets Archipelago/Continent/Highlands (`presets.ts`); 🎲/Generate/Export/Share/'Copied!' labels (`control-panel.ts` 208–228, 363); `FINAL_RESOLUTION = 512` + 256² preview (`map-controller.ts` 37/193); `LUT_STEPS = 64` → 64×64/4096 cells (`compose.ts` 21); 512-entry perm table + F2/G2 (`simplex.ts`); fBm 5 oct/lacunarity 2.0/gain 0.5 (`fbm.ts` 28–30); golden tests at 1e-10 (`simplex.test.ts` `toBeCloseTo(…, 10)`); ~2.8 s animation = 2×(1000+400) (`animation.ts`); replaceState-only URL writes (`urlState.ts`); `STATE_VERSION = 1` (`urlState.ts` 17); zero runtime deps (`package.json` devDependencies only); scripts dev/test/build/preview/typecheck all exist; screenshot file exists (evidence-frame, honestly TODO-marked); Vite 8 Node floor claim matches engines exactly. `dist/index.html` title is `<title>Procedural Biome Generator</title>` and assets are referenced under `/biome-generator/assets/` — the runbook's URL shape and curl expectation are correct.
- **Defects (fix before user execution; all doc-only):**
  - **D1 (runbook, medium): "gh CLI (installed on this machine)" is FALSE.** `which gh` / `command -v gh` fail; not in `/opt/homebrew/bin`, `/usr/local/bin`, any PATH dir, `~/.npm-global/bin`, or Spotlight (`mdfind`). Variant A (web UI, primary) is unaffected, but a user trusting the claim hits `zsh: command not found: gh`. Fix: relabel Variant B as "if you install gh: `brew install gh && gh auth login`" or drop the parenthetical.
  - **D2 (runbook, medium): Step 4 Variant B `gh api repos/{owner}/biome-generator/pages …` cannot resolve at that position.** Per the gh manual, `{owner}` expands from "the repository of the current directory or the repository specified in the GH_REPO environment variable" — at Step 4 the local repo has NO remote yet (remote is added in Step 5), so the placeholder has no source and the command fails even with gh installed. The `--jq .build_type` follow-up snippet in the same step shares the flaw. Fix: write the literal username, or move the gh-variant enablement after Step 5 (late enablement + `gh run rerun` recovery is already documented), or prefix with `GH_REPO=OWNER/biome-generator`.
  - **D3 (runbook, medium): smoke item 2 — "Clicking Generate re-runs the staged animation with a new map" — contradicts the app.** `generateButton` click → `controller.regenerate()` with no seed (`control-panel.ts` 487–489) → `regenerate()` defaults to the CURRENT seed (`map-controller.ts` 268–280) → determinism means the identical map, re-animated. As written, the check fails against a correctly working app and would burn the user's time / misreport acceptance. Fix the pass criteria: "re-plays the staged animation and settles on the same map (same seed ⇒ identical map — determinism); a NEW map requires 🎲 / a seed edit / a preset".
  - **D4 (README, minor): "[2nd, 99.95th] percentile span" is stale.** Current defaults are `elevationNormLow: 0.02, elevationNormHigh: 0.99999` (`fields.ts` 48–49) — T3c moved the top shoulder 0.9995 → 0.99999 (the file's own comment says so). Correct statement: "[2nd, 99.999th]".
  - **D5 (README, nit, non-blocking): the "~78 ms median" evidence pointer is imprecise.** The 78.3 ms raw-Node median lives in the production log (T16 Verification's independent re-measurement); the JSONs in `docs/ultron/evidence/t16/` show in-browser gen512 medians of 99–129 ms (also <300 PASS — the folder fully supports the sentence's zero-longtask half, verified: `totalLongtasks: 0` native envs; the two `1`s at 4× are the documented pre-phase module-eval longtask, outside "during drag and animation"). Recommend citing the log for the 78 or restating as "~0.1 s in-browser / ~78 ms raw-Node".
- **Plan fidelity:** the T18 spec's step list, permissions, artifact path and Pages-source step are all delivered; the two deviations (`npm test` gate step, `workflow_dispatch`) are documented in the workflow header + this log and judged acceptable — the suite runs in ~5 s, every prior gate ran it, and manual re-deploy supports the "deterministic rebuild" acceptance check.
- **Non-blocking follow-ups for the coordinator:** (a) Step 2 could mention the git-identity prerequisite (`git config --global user.name/user.email`) for a machine that has never committed — standard, likely already set; (b) Step 5's one-command `gh repo create … --source . --push` alternative only applies if Step 3 Variant B was skipped (it says "from Step 2's committed state", but the phrasing "skip the remote-add" can be misread as skipping only Step 5's remote-add after Step 3 already created the repo → "name already exists"); (c) "~1–2 min" for the first Actions run is optimistic but harmless as an estimate.

## T18-prep-fix — doc-only defect repairs · Production

- **Status:** executed 2026-08-27, all 4 verifier defects (D1–D4) + D5-related perf citation + the optional git-identity follow-up (a) applied. **Delegation:** none (production worker in the Ultron-Supreme run, coordinator-briefed). Scope guard honored: **only** `docs/ultron/t18-deploy-runbook.md`, `README.md`, `docs/ultron/plan.md` (T18 status + index row 18), and this log touched — no production code, `vite.config.ts`, `package.json`, `.github/workflows/deploy.yml`, `state.md`, or `research/`.
- **What/why:** the T18-prep verifier's NEEDS-FIX verdict named exactly the USER-executed surfaces (runbook + README) — fact errors there are the failure class the T18 wizard-lane halt exists to prevent. This entry records the repairs and the re-certification.

- **Exact edits (before → after):**
  1. **Runbook Step 3, Variant B heading + preamble (fixes D1):** `### Variant B: gh CLI (installed on this machine)` → `### Variant B: gh CLI (only if you install it first)` + a two-line preamble: gh is **not** installed on this machine; CLI route requires `brew install gh && gh auth login` first. The `gh auth status` / `gh repo create` commands in that block are unchanged.
  2. **Runbook Step 4, Variant B command (fixes D2):** `gh api repos/{owner}/biome-generator/pages --method POST -f build_type=workflow` (and the 409 follow-up `--jq .build_type` snippet) → `gh api repos/YOUR-USERNAME/biome-generator/pages …` in both, plus a three-line explanation: substitute your literal GitHub username (same placeholder as Step 5); gh's `{owner}` shorthand can't resolve here because it expands from the current directory's repo or `GH_REPO`, and the local repo has no remote until Step 5. Literal-username form chosen over the `GH_REPO=` env prefix — it matches the runbook's existing placeholder style (`YOUR-USERNAME` in Step 5's remote-add and Step 7's URL).
  3. **Runbook Step 8, smoke item 2 (fixes D3):** `Clicking Generate re-runs the staged animation with a new map` → `Pressing Generate re-plays the staged animation and settles on the same map (seed determinism); 🎲 produces a new map`. New-map paths (🎲 / seed entry / preset) remain covered by items 3–4.
  4. **Runbook Step 2, prerequisite note (verifier follow-up (a), opted in):** one block after the heading — on a machine that has never made a commit, if `git commit` has ever failed with an identity error, set `git config --global user.name "Your Name"` / `user.email "you@example.com"` once. Commands below it unchanged.
  5. **README engineering note (fixes D4):** `[2nd, 99.95th] percentile span` → `[2nd, 99.999th] percentile span` (matches `elevationNormLow: 0.02, elevationNormHigh: 0.99999` at `src/generation/fields.ts` 48–49).
  6. **README perf sentence (fixes D5, verifier's preferred option — the in-browser band):** `a full 512² generation measured a median of ~78ms against a 300ms budget (evidence in docs/ultron/evidence/t16/)` → `a full 512² generation measured in-browser medians of ~99–129ms across those engines (throttled runs included), well inside the 300ms budget (evidence in docs/ultron/evidence/t16/; a raw-Node re-measurement recorded ~78ms — see the T16 entry in docs/ultron/production-log.md)`. The zero-longtasks half of the sentence was verifier-confirmed and is untouched.
  7. **plan.md:** T18 status `prepared — needs-fix before user execution` → `prepared (verified after fix — awaiting user wizard-lane execution)` + a Fixed-2026-08-27 clause appended inside the status parenthetical; index row 18 → `🟨 prepared (verified after fix — awaiting user execution)`.

- **Fact re-verification (every touched claim, re-checked against the tree post-edit):**
  - **gh absence (edit 1):** `which gh` / `command -v gh` → not found (also absent from `/opt/homebrew/bin`, `/usr/local/bin`, Spotlight) — the relabel states reality.
  - **{owner} semantics (edit 2):** gh's documented placeholder expansion needs the current directory's repository or `GH_REPO`; the local `.git` has zero remotes (Step 5 adds the first) — the literal-username form stands alone. Placeholder consistency: `YOUR-USERNAME` now used uniformly in Step 4 (both commands), Step 5 (remote-add + https alternative), Step 7 (`open` + notes).
  - **Generate determinism (edit 3):** `src/ui/control-panel.ts` 487–489 — `generateButton` click → `ports.controller.regenerate()` with NO seed argument; `src/controller/map-controller.ts` 270–280 — `regenerate(seed?)` only overwrites `seedValue` when `seed !== undefined`, then issues `worker.generate(this.seedValue, this.fieldParams, FINAL_RESOLUTION)` → same seed + same params ⇒ byte-identical map (T15 digest-pinned), re-animated via the Generate-class animation-cancel + staged replay. New-map paths verified in the same files: 🎲 → `setSeed(randomSeed())` (control-panel.ts 480–485), preset → `applyPreset` (field-param overrides change the map), manual seed entry → parsed-int `setSeed`. Item 2's new wording states exactly this.
  - **Percentile defaults (edit 5):** `src/generation/fields.ts` 48–49 — `elevationNormLow: 0.02, elevationNormHigh: 0.99999`; 0.99999 quantile = 99.999th percentile ⇒ "[2nd, 99.999th]" is exact.
  - **Perf citation (edit 6):** `docs/ultron/evidence/t16/*.json` gen512 medians re-read — chromium-desktop 129, chromium-desktop-4x 108.1, chromium-mobile-4x 104.4, webkit-desktop 99, webkit-ios 105 ⇒ band 99–129 ms, "~99–129ms (throttled runs included)" exact; 78.3 ms raw-Node median re-confirmed at this log's T16 Verification entry; the `evidence/t16/` pointer now backs the in-browser claim and the log pointer backs the raw-Node claim.
  - **No new stale facts:** grep over README + runbook + plan.md — the only remaining "78ms"/"99.95" hits are the intentional corrected citations (README's log-cited raw-Node clause) and plan.md line 56's T3b-era `[2nd, 99.95th]` record, which is historically accurate for that task (T3c moved the shoulder afterward) and untouched.

- **Gates (this worker's run, clean `dist/` first — certify zero drift):** `npm test` → **213/213, 16 suites** (3.67 s) · `npm run typecheck` → exit 0 · `npm run build` → exit 0 emitting **`index-BaLHBJm7.js` 27.86 kB + `map.worker-DbpHG_aw.js` 3.96 kB + `index-BcTqoKYm.css` 3.72 kB** — names/sizes byte-identical to the T18-prep Verification record. Expected: doc-only edits cannot move code; the gates prove it.
- **Deviations:** none in scope; the optional follow-up (a) was explicitly included per the task brief. Verifier follow-ups (b) (Step 5 one-command phrasing) and (c) ("~1–2 min" estimate) were NOT in this brief's correction list and remain as the verifier left them — flagged here so they stay visible to the coordinator.

## T18-prep Re-verification — fix audit · Ultron-Supreme verifier (original defect holder)

- **Verdict: PASS — fix accepted; T18-prep is verified-prepared (awaiting user wizard-lane execution), 2026-08-27.** All D1–D5 resolutions plus the optional git-identity note confirmed at source; no new defects introduced.
- **Per-defect confirmation (against the edited files, not the fix entry's claims):**
  - **D1:** runbook Step 3 Variant B now reads "gh CLI (only if you install it first)" with the preamble "The GitHub CLI is **not** installed on this machine… `brew install gh && gh auth login`" — states reality (verifier re-confirmed gh absent in round 1; `brew` exists at `/opt/homebrew/bin`), commands below it unchanged and now correctly gated behind an install+auth step.
  - **D2:** both Step 4 Variant B commands now use the literal `YOUR-USERNAME` placeholder (consistent with Step 5's remote-add and Step 7's URL), and the three-line explanation of why `{owner}` can't resolve matches gh's documented placeholder semantics exactly (expands from the current-directory repo or `GH_REPO`; no remote exists until Step 5).
  - **D3:** smoke item 2 now reads "Pressing Generate re-plays the staged animation and settles on the same map (seed determinism); 🎲 produces a new map" — behaviorally exact: `generateButton` click → `regenerate()` with no seed argument (`control-panel.ts` 487–489) → `regenerate(seed?)` keeps the current seed (`map-controller.ts`) → identical deterministic map, re-animated; 🎲 → `setSeed(randomSeed())` → new map. The check now passes against a correctly working app.
  - **D4:** README now reads "[2nd, 99.999th] percentile span" — matches `fields.ts` defaults (`elevationNormLow: 0.02, elevationNormHigh: 0.99999`).
  - **D5:** README perf sentence now leads with "in-browser medians of ~99–129ms across those engines (throttled runs included)" citing `evidence/t16/`, with the ~78ms raw-Node figure re-cited to this log's T16 Verification entry. Verifier re-read all five evidence JSONs: gen512 medians 129 / 108.1 / 104.4 / 99 / 105 — the band is exact.
  - **Follow-up (a):** Step 2 git-identity prerequisite added with correct, non-destructive `git config --global` syntax.
- **No new stale facts:** full re-read of both edited files — every other region is byte-for-byte the round-1-audited text. Grep confirms the only remaining "99.95"/"78ms" hits are intentional: README's log-cited raw-Node clause, plan.md line 56's T3b-era historical record (accurate for that task; T3c's later shoulder move is documented in its own row), and plan.md's T16 records. Runbook's only `{owner}` hit is the explanation itself.
- **Gates (verifier-run, clean `dist/` first):** `npm test` → **213/213, 16 suites**; `npm run typecheck` → exit 0; `npm run build` → exit 0 emitting **`index-BaLHBJm7.js` 27.86 kB + `map.worker-DbpHG_aw.js` 3.96 kB + `index-BcTqoKYm.css` 3.72 kB** — byte-identical to the T16-fix/T18-prep record, zero drift (doc-only fix confirmed mechanically).
- **Scope audit (mtime proxy, no git commands):** files modified after the round-1 verification window = exactly the four disclosed: `docs/ultron/t18-deploy-runbook.md` (14:17), `README.md` (14:17), `docs/ultron/plan.md` (14:18), `docs/ultron/production-log.md` (14:19). No workflow, code, config, `state.md`, or `research/` touched.
- **Residual notes (non-blocking, unchanged, already visible via the T18-prep-fix entry's Deviations):** (b) Step 5's one-command `gh repo create … --source . --push` alternative presumes Step 3 Variant B was skipped ("from Step 2's committed state") and pushes before Pages enablement (recoverable per Step 6); (c) "~1–2 min" first-run estimate is optimistic. Neither blocks the user's primary (web-UI) path.

